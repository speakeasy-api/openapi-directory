import { SpeakeasyBase } from "../../../internal/utils";
/**
 * An error describing why a schema could not be fetched.
 */
export declare class BatchGetSchemaError extends SpeakeasyBase {
    code: string;
    message: string;
    name: string;
}
