/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import * as utils from "../internal/utils";
import * as operations from "./models/operations";
import { AxiosInstance, AxiosRequestConfig, AxiosResponse } from "axios";

export class CustomPrices {
  _defaultClient: AxiosInstance;
  _securityClient: AxiosInstance;
  _serverURL: string;
  _language: string;
  _sdkVersion: string;
  _genVersion: string;

  constructor(
    defaultClient: AxiosInstance,
    securityClient: AxiosInstance,
    serverURL: string,
    language: string,
    sdkVersion: string,
    genVersion: string
  ) {
    this._defaultClient = defaultClient;
    this._securityClient = securityClient;
    this._serverURL = serverURL;
    this._language = language;
    this._sdkVersion = sdkVersion;
    this._genVersion = genVersion;
  }

  /**
   * Get custom prices schema
   *
   * @remarks
   * Retrieves all custom price for all shopping scenarios
   */
  getVCustomPricesSessionSchema(
    req: operations.GetVCustomPricesSessionSchemaRequest,
    config?: AxiosRequestConfig
  ): Promise<operations.GetVCustomPricesSessionSchemaResponse> {
    if (!(req instanceof utils.SpeakeasyBase)) {
      req = new operations.GetVCustomPricesSessionSchemaRequest(req);
    }

    const baseURL: string = this._serverURL;
    const url: string =
      baseURL.replace(/\/$/, "") + "/_v/custom-prices/session/schema";

    const client: AxiosInstance = this._defaultClient;

    const headers = { ...utils.getHeadersFromRequest(req), ...config?.headers };

    const r = client.request({
      url: url,
      method: "get",
      headers: headers,
      ...config,
    });

    return r.then((httpRes: AxiosResponse) => {
      const contentType: string = httpRes?.headers?.["content-type"] ?? "";

      if (httpRes?.status == null)
        throw new Error(`status code not found in response: ${httpRes}`);
      const res: operations.GetVCustomPricesSessionSchemaResponse =
        new operations.GetVCustomPricesSessionSchemaResponse({
          statusCode: httpRes.status,
          contentType: contentType,
          rawResponse: httpRes,
        });
      switch (true) {
        case httpRes?.status == 200:
          if (utils.matchContentType(contentType, `application/json`)) {
            res.requestBody = utils.objectToClass(
              httpRes?.data,
              operations.GetVCustomPricesSessionSchemaRequestBody
            );
          }
          break;
      }

      return res;
    });
  }

  /**
   * Create or Update custom prices schema
   *
   * @remarks
   * Creates a new custom price for a shopping scenario or updates an existing one
   */
  postVCustomPricesSessionSchema(
    req: operations.PostVCustomPricesSessionSchemaRequest,
    config?: AxiosRequestConfig
  ): Promise<operations.PostVCustomPricesSessionSchemaResponse> {
    if (!(req instanceof utils.SpeakeasyBase)) {
      req = new operations.PostVCustomPricesSessionSchemaRequest(req);
    }

    const baseURL: string = this._serverURL;
    const url: string =
      baseURL.replace(/\/$/, "") + "/_v/custom-prices/session/schema";

    let [reqBodyHeaders, reqBody]: [object, any] = [{}, {}];

    try {
      [reqBodyHeaders, reqBody] = utils.serializeRequestBody(
        req,
        "requestBody",
        "json"
      );
    } catch (e: unknown) {
      if (e instanceof Error) {
        throw new Error(`Error serializing request body, cause: ${e.message}`);
      }
    }

    const client: AxiosInstance = this._defaultClient;

    const headers = {
      ...utils.getHeadersFromRequest(req),
      ...reqBodyHeaders,
      ...config?.headers,
    };

    const r = client.request({
      url: url,
      method: "post",
      headers: headers,
      data: reqBody,
      ...config,
    });

    return r.then((httpRes: AxiosResponse) => {
      const contentType: string = httpRes?.headers?.["content-type"] ?? "";

      if (httpRes?.status == null)
        throw new Error(`status code not found in response: ${httpRes}`);
      const res: operations.PostVCustomPricesSessionSchemaResponse =
        new operations.PostVCustomPricesSessionSchemaResponse({
          statusCode: httpRes.status,
          contentType: contentType,
          rawResponse: httpRes,
        });
      switch (true) {
        case httpRes?.status == 200:
          if (utils.matchContentType(contentType, `application/json`)) {
            res.requestBody = utils.objectToClass(
              httpRes?.data,
              operations.PostVCustomPricesSessionSchemaRequestBody
            );
          }
          break;
      }

      return res;
    });
  }
}
