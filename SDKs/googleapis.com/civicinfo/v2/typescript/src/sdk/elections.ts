/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import * as utils from "../internal/utils";
import * as operations from "./models/operations";
import * as shared from "./models/shared";
import { AxiosInstance, AxiosRequestConfig, AxiosResponse } from "axios";

export class Elections {
  _defaultClient: AxiosInstance;
  _securityClient: AxiosInstance;
  _serverURL: string;
  _language: string;
  _sdkVersion: string;
  _genVersion: string;

  constructor(
    defaultClient: AxiosInstance,
    securityClient: AxiosInstance,
    serverURL: string,
    language: string,
    sdkVersion: string,
    genVersion: string
  ) {
    this._defaultClient = defaultClient;
    this._securityClient = securityClient;
    this._serverURL = serverURL;
    this._language = language;
    this._sdkVersion = sdkVersion;
    this._genVersion = genVersion;
  }

  /**
   * List of available elections to query.
   */
  civicinfoElectionsElectionQuery(
    req: operations.CivicinfoElectionsElectionQueryRequest,
    config?: AxiosRequestConfig
  ): Promise<operations.CivicinfoElectionsElectionQueryResponse> {
    if (!(req instanceof utils.SpeakeasyBase)) {
      req = new operations.CivicinfoElectionsElectionQueryRequest(req);
    }

    const baseURL: string = this._serverURL;
    const url: string = baseURL.replace(/\/$/, "") + "/civicinfo/v2/elections";

    const client: AxiosInstance = this._defaultClient;

    const queryParams: string = utils.serializeQueryParams(req);

    const r = client.request({
      url: url + queryParams,
      method: "get",
      ...config,
    });

    return r.then((httpRes: AxiosResponse) => {
      const contentType: string = httpRes?.headers?.["content-type"] ?? "";

      if (httpRes?.status == null)
        throw new Error(`status code not found in response: ${httpRes}`);
      const res: operations.CivicinfoElectionsElectionQueryResponse =
        new operations.CivicinfoElectionsElectionQueryResponse({
          statusCode: httpRes.status,
          contentType: contentType,
          rawResponse: httpRes,
        });
      switch (true) {
        case httpRes?.status == 200:
          if (utils.matchContentType(contentType, `application/json`)) {
            res.electionsQueryResponse = utils.objectToClass(
              httpRes?.data,
              shared.ElectionsQueryResponse
            );
          }
          break;
      }

      return res;
    });
  }

  /**
   * Looks up information relevant to a voter based on the voter's registered address.
   */
  civicinfoElectionsVoterInfoQuery(
    req: operations.CivicinfoElectionsVoterInfoQueryRequest,
    config?: AxiosRequestConfig
  ): Promise<operations.CivicinfoElectionsVoterInfoQueryResponse> {
    if (!(req instanceof utils.SpeakeasyBase)) {
      req = new operations.CivicinfoElectionsVoterInfoQueryRequest(req);
    }

    const baseURL: string = this._serverURL;
    const url: string = baseURL.replace(/\/$/, "") + "/civicinfo/v2/voterinfo";

    const client: AxiosInstance = this._defaultClient;

    const queryParams: string = utils.serializeQueryParams(req);

    const r = client.request({
      url: url + queryParams,
      method: "get",
      ...config,
    });

    return r.then((httpRes: AxiosResponse) => {
      const contentType: string = httpRes?.headers?.["content-type"] ?? "";

      if (httpRes?.status == null)
        throw new Error(`status code not found in response: ${httpRes}`);
      const res: operations.CivicinfoElectionsVoterInfoQueryResponse =
        new operations.CivicinfoElectionsVoterInfoQueryResponse({
          statusCode: httpRes.status,
          contentType: contentType,
          rawResponse: httpRes,
        });
      switch (true) {
        case httpRes?.status == 200:
          if (utils.matchContentType(contentType, `application/json`)) {
            res.voterInfoResponse = utils.objectToClass(
              httpRes?.data,
              shared.VoterInfoResponse
            );
          }
          break;
      }

      return res;
    });
  }
}
