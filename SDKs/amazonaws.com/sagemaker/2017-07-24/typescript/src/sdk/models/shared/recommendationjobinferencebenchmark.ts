/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import { SpeakeasyBase, SpeakeasyMetadata } from "../../../internal/utils";
import { EndpointOutputConfiguration } from "./endpointoutputconfiguration";
import { InferenceMetrics } from "./inferencemetrics";
import { ModelConfiguration } from "./modelconfiguration";
import { RecommendationMetrics } from "./recommendationmetrics";
import { Expose, Type } from "class-transformer";

/**
 * The details for a specific benchmark from an Inference Recommender job.
 */
export class RecommendationJobInferenceBenchmark extends SpeakeasyBase {
  /**
   * The endpoint configuration made by Inference Recommender during a recommendation job.
   */
  @SpeakeasyMetadata()
  @Expose({ name: "EndpointConfiguration" })
  @Type(() => EndpointOutputConfiguration)
  endpointConfiguration?: EndpointOutputConfiguration;

  /**
   * The metrics for an existing endpoint compared in an Inference Recommender job.
   */
  @SpeakeasyMetadata()
  @Expose({ name: "EndpointMetrics" })
  @Type(() => InferenceMetrics)
  endpointMetrics?: InferenceMetrics;

  @SpeakeasyMetadata()
  @Expose({ name: "FailureReason" })
  failureReason?: string;

  /**
   * The metrics of recommendations.
   */
  @SpeakeasyMetadata()
  @Expose({ name: "Metrics" })
  @Type(() => RecommendationMetrics)
  metrics?: RecommendationMetrics;

  /**
   * Defines the model configuration. Includes the specification name and environment parameters.
   */
  @SpeakeasyMetadata()
  @Expose({ name: "ModelConfiguration" })
  @Type(() => ModelConfiguration)
  modelConfiguration: ModelConfiguration;
}
