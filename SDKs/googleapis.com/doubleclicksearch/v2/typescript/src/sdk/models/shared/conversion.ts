/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import { SpeakeasyBase, SpeakeasyMetadata } from "../../../internal/utils";
import { CustomDimension } from "./customdimension";
import { CustomMetric } from "./custommetric";
import { Expose, Type } from "class-transformer";

/**
 * A conversion containing data relevant to DoubleClick Search.
 */
export class Conversion extends SpeakeasyBase {
  /**
   * DS ad group ID.
   */
  @SpeakeasyMetadata()
  @Expose({ name: "adGroupId" })
  adGroupId?: string;

  /**
   * DS ad ID.
   */
  @SpeakeasyMetadata()
  @Expose({ name: "adId" })
  adId?: string;

  /**
   * DS advertiser ID.
   */
  @SpeakeasyMetadata()
  @Expose({ name: "advertiserId" })
  advertiserId?: string;

  /**
   * DS agency ID.
   */
  @SpeakeasyMetadata()
  @Expose({ name: "agencyId" })
  agencyId?: string;

  /**
   * Available to advertisers only after contacting DoubleClick Search customer support.
   */
  @SpeakeasyMetadata()
  @Expose({ name: "attributionModel" })
  attributionModel?: string;

  /**
   * DS campaign ID.
   */
  @SpeakeasyMetadata()
  @Expose({ name: "campaignId" })
  campaignId?: string;

  /**
   * Sales channel for the product. Acceptable values are: - "`local`": a physical store - "`online`": an online store
   */
  @SpeakeasyMetadata()
  @Expose({ name: "channel" })
  channel?: string;

  /**
   * DS click ID for the conversion.
   */
  @SpeakeasyMetadata()
  @Expose({ name: "clickId" })
  clickId?: string;

  /**
   * For offline conversions, advertisers provide this ID. Advertisers can specify any ID that is meaningful to them. Each conversion in a request must specify a unique ID, and the combination of ID and timestamp must be unique amongst all conversions within the advertiser. For online conversions, DS copies the `dsConversionId` or `floodlightOrderId` into this property depending on the advertiser's Floodlight instructions.
   */
  @SpeakeasyMetadata()
  @Expose({ name: "conversionId" })
  conversionId?: string;

  /**
   * The time at which the conversion was last modified, in epoch millis UTC.
   */
  @SpeakeasyMetadata()
  @Expose({ name: "conversionModifiedTimestamp" })
  conversionModifiedTimestamp?: string;

  /**
   * The time at which the conversion took place, in epoch millis UTC.
   */
  @SpeakeasyMetadata()
  @Expose({ name: "conversionTimestamp" })
  conversionTimestamp?: string;

  /**
   * Available to advertisers only after contacting DoubleClick Search customer support.
   */
  @SpeakeasyMetadata()
  @Expose({ name: "countMillis" })
  countMillis?: string;

  /**
   * DS criterion (keyword) ID.
   */
  @SpeakeasyMetadata()
  @Expose({ name: "criterionId" })
  criterionId?: string;

  /**
   * The currency code for the conversion's revenue. Should be in ISO 4217 alphabetic (3-char) format.
   */
  @SpeakeasyMetadata()
  @Expose({ name: "currencyCode" })
  currencyCode?: string;

  /**
   * Custom dimensions for the conversion, which can be used to filter data in a report.
   */
  @SpeakeasyMetadata({ elemType: CustomDimension })
  @Expose({ name: "customDimension" })
  @Type(() => CustomDimension)
  customDimension?: CustomDimension[];

  /**
   * Custom metrics for the conversion.
   */
  @SpeakeasyMetadata({ elemType: CustomMetric })
  @Expose({ name: "customMetric" })
  @Type(() => CustomMetric)
  customMetric?: CustomMetric[];

  /**
   * Customer ID of a client account in the new Search Ads 360 experience.
   */
  @SpeakeasyMetadata()
  @Expose({ name: "customerId" })
  customerId?: string;

  /**
   * The type of device on which the conversion occurred.
   */
  @SpeakeasyMetadata()
  @Expose({ name: "deviceType" })
  deviceType?: string;

  /**
   * ID that DoubleClick Search generates for each conversion.
   */
  @SpeakeasyMetadata()
  @Expose({ name: "dsConversionId" })
  dsConversionId?: string;

  /**
   * DS engine account ID.
   */
  @SpeakeasyMetadata()
  @Expose({ name: "engineAccountId" })
  engineAccountId?: string;

  /**
   * The Floodlight order ID provided by the advertiser for the conversion.
   */
  @SpeakeasyMetadata()
  @Expose({ name: "floodlightOrderId" })
  floodlightOrderId?: string;

  /**
   * ID that DS generates and uses to uniquely identify the inventory account that contains the product.
   */
  @SpeakeasyMetadata()
  @Expose({ name: "inventoryAccountId" })
  inventoryAccountId?: string;

  /**
   * The country registered for the Merchant Center feed that contains the product. Use an ISO 3166 code to specify a country.
   */
  @SpeakeasyMetadata()
  @Expose({ name: "productCountry" })
  productCountry?: string;

  /**
   * DS product group ID.
   */
  @SpeakeasyMetadata()
  @Expose({ name: "productGroupId" })
  productGroupId?: string;

  /**
   * The product ID (SKU).
   */
  @SpeakeasyMetadata()
  @Expose({ name: "productId" })
  productId?: string;

  /**
   * The language registered for the Merchant Center feed that contains the product. Use an ISO 639 code to specify a language.
   */
  @SpeakeasyMetadata()
  @Expose({ name: "productLanguage" })
  productLanguage?: string;

  /**
   * The quantity of this conversion, in millis.
   */
  @SpeakeasyMetadata()
  @Expose({ name: "quantityMillis" })
  quantityMillis?: string;

  /**
   * The revenue amount of this `TRANSACTION` conversion, in micros (value multiplied by 1000000, no decimal). For example, to specify a revenue value of "10" enter "10000000" (10 million) in your request.
   */
  @SpeakeasyMetadata()
  @Expose({ name: "revenueMicros" })
  revenueMicros?: string;

  /**
   * The numeric segmentation identifier (for example, DoubleClick Search Floodlight activity ID).
   */
  @SpeakeasyMetadata()
  @Expose({ name: "segmentationId" })
  segmentationId?: string;

  /**
   * The friendly segmentation identifier (for example, DoubleClick Search Floodlight activity name).
   */
  @SpeakeasyMetadata()
  @Expose({ name: "segmentationName" })
  segmentationName?: string;

  /**
   * The segmentation type of this conversion (for example, `FLOODLIGHT`).
   */
  @SpeakeasyMetadata()
  @Expose({ name: "segmentationType" })
  segmentationType?: string;

  /**
   * The state of the conversion, that is, either `ACTIVE` or `REMOVED`. Note: state DELETED is deprecated.
   */
  @SpeakeasyMetadata()
  @Expose({ name: "state" })
  state?: string;

  /**
   * The ID of the local store for which the product was advertised. Applicable only when the channel is "`local`".
   */
  @SpeakeasyMetadata()
  @Expose({ name: "storeId" })
  storeId?: string;

  /**
   * The type of the conversion, that is, either `ACTION` or `TRANSACTION`. An `ACTION` conversion is an action by the user that has no monetarily quantifiable value, while a `TRANSACTION` conversion is an action that does have a monetarily quantifiable value. Examples are email list signups (`ACTION`) versus ecommerce purchases (`TRANSACTION`).
   */
  @SpeakeasyMetadata()
  @Expose({ name: "type" })
  type?: string;
}
