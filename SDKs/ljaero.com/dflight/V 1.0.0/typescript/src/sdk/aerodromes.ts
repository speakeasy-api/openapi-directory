/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import * as utils from "../internal/utils";
import * as operations from "./models/operations";
import * as shared from "./models/shared";
import { AxiosInstance, AxiosRequestConfig, AxiosResponse } from "axios";

/**
 * #### Center points and contact information for all US landing facilities, including those with no associated controlled airspace:
 *
 * @remarks
 * * Public and private airports
 * * Public and private balloonports
 * * Public and private gliderports
 * * Public and private heliports
 * * Public and private seaplane bases
 * * Public and private ultralight bases
 */
export class Aerodromes {
  _defaultClient: AxiosInstance;
  _securityClient: AxiosInstance;
  _serverURL: string;
  _language: string;
  _sdkVersion: string;
  _genVersion: string;

  constructor(
    defaultClient: AxiosInstance,
    securityClient: AxiosInstance,
    serverURL: string,
    language: string,
    sdkVersion: string,
    genVersion: string
  ) {
    this._defaultClient = defaultClient;
    this._securityClient = securityClient;
    this._serverURL = serverURL;
    this._language = language;
    this._sdkVersion = sdkVersion;
    this._genVersion = genVersion;
  }

  /**
   * Retrieve aerodromes within given distance of location.
   *
   * @remarks
   * Retrieve aerodromes within given distance of a point. Request body parameters are:
   * * latitude:  WGS84 latitude coordinate of your selected point, in decimal degrees
   * * longitude:  WGS84 longitude coordinate of your selected point, in decimal degrees
   * * distance:  distance in meters (max allowed value is 25000)
   *
   * Successful requests return a GeoJSON FeatureCollection, with a separate Feature for each
   * Aerodrome found. All Features will include properties for the facility name, ident,
   * type, and operational status.
   */
  aerodromesByDistanceUsV1AerodromesDistanceQueryPost(
    req: operations.AerodromesByDistanceUsV1AerodromesDistanceQueryPostRequest,
    config?: AxiosRequestConfig
  ): Promise<operations.AerodromesByDistanceUsV1AerodromesDistanceQueryPostResponse> {
    if (!(req instanceof utils.SpeakeasyBase)) {
      req =
        new operations.AerodromesByDistanceUsV1AerodromesDistanceQueryPostRequest(
          req
        );
    }

    const baseURL: string = this._serverURL;
    const url: string =
      baseURL.replace(/\/$/, "") + "/us/v1/aerodromes/distance-query";

    let [reqBodyHeaders, reqBody]: [object, any] = [{}, {}];

    try {
      [reqBodyHeaders, reqBody] = utils.serializeRequestBody(
        req,
        "aerodromesByDistance",
        "json"
      );
    } catch (e: unknown) {
      if (e instanceof Error) {
        throw new Error(`Error serializing request body, cause: ${e.message}`);
      }
    }

    const client: AxiosInstance = this._defaultClient;

    const headers = {
      ...utils.getHeadersFromRequest(req),
      ...reqBodyHeaders,
      ...config?.headers,
    };
    if (reqBody == null || Object.keys(reqBody).length === 0)
      throw new Error("request body is required");

    const r = client.request({
      url: url,
      method: "post",
      headers: headers,
      data: reqBody,
      ...config,
    });

    return r.then((httpRes: AxiosResponse) => {
      const contentType: string = httpRes?.headers?.["content-type"] ?? "";

      if (httpRes?.status == null)
        throw new Error(`status code not found in response: ${httpRes}`);
      const res: operations.AerodromesByDistanceUsV1AerodromesDistanceQueryPostResponse =
        new operations.AerodromesByDistanceUsV1AerodromesDistanceQueryPostResponse(
          {
            statusCode: httpRes.status,
            contentType: contentType,
            rawResponse: httpRes,
          }
        );
      switch (true) {
        case httpRes?.status == 200:
          if (utils.matchContentType(contentType, `application/json`)) {
            res.aerodromeDistanceResponse = utils.objectToClass(
              httpRes?.data,
              shared.AerodromeDistanceResponse
            );
          }
          break;
        case httpRes?.status == 422:
          if (utils.matchContentType(contentType, `application/json`)) {
            res.httpValidationError = utils.objectToClass(
              httpRes?.data,
              shared.HTTPValidationError
            );
          }
          break;
      }

      return res;
    });
  }

  /**
   * Retrieve aerodromes located within given area.
   *
   * @remarks
   * Retrieve aerodromes located within given area. Request body parameters are:
   * * poly:  [GeoJSON Polygon](https://www.rfc-editor.org/rfc/rfc7946.html#appendix-A) defining the area. Max allowed area is 1000 km^2.
   *
   * Successful requests return a GeoJSON FeatureCollection, with a separate Feature for each
   * Aerodrome found. All Features will include properties for the facility name, ident,
   * type, and operational status.
   */
  aerodromesByPolyUsV1AerodromesPolygonQueryPost(
    req: operations.AerodromesByPolyUsV1AerodromesPolygonQueryPostRequest,
    config?: AxiosRequestConfig
  ): Promise<operations.AerodromesByPolyUsV1AerodromesPolygonQueryPostResponse> {
    if (!(req instanceof utils.SpeakeasyBase)) {
      req =
        new operations.AerodromesByPolyUsV1AerodromesPolygonQueryPostRequest(
          req
        );
    }

    const baseURL: string = this._serverURL;
    const url: string =
      baseURL.replace(/\/$/, "") + "/us/v1/aerodromes/polygon-query";

    let [reqBodyHeaders, reqBody]: [object, any] = [{}, {}];

    try {
      [reqBodyHeaders, reqBody] = utils.serializeRequestBody(
        req,
        "aerodromesByPolygon",
        "json"
      );
    } catch (e: unknown) {
      if (e instanceof Error) {
        throw new Error(`Error serializing request body, cause: ${e.message}`);
      }
    }

    const client: AxiosInstance = this._defaultClient;

    const headers = {
      ...utils.getHeadersFromRequest(req),
      ...reqBodyHeaders,
      ...config?.headers,
    };
    if (reqBody == null || Object.keys(reqBody).length === 0)
      throw new Error("request body is required");

    const r = client.request({
      url: url,
      method: "post",
      headers: headers,
      data: reqBody,
      ...config,
    });

    return r.then((httpRes: AxiosResponse) => {
      const contentType: string = httpRes?.headers?.["content-type"] ?? "";

      if (httpRes?.status == null)
        throw new Error(`status code not found in response: ${httpRes}`);
      const res: operations.AerodromesByPolyUsV1AerodromesPolygonQueryPostResponse =
        new operations.AerodromesByPolyUsV1AerodromesPolygonQueryPostResponse({
          statusCode: httpRes.status,
          contentType: contentType,
          rawResponse: httpRes,
        });
      switch (true) {
        case httpRes?.status == 200:
          if (utils.matchContentType(contentType, `application/json`)) {
            res.aerodromePolyResponse = utils.objectToClass(
              httpRes?.data,
              shared.AerodromePolyResponse
            );
          }
          break;
        case httpRes?.status == 422:
          if (utils.matchContentType(contentType, `application/json`)) {
            res.httpValidationError = utils.objectToClass(
              httpRes?.data,
              shared.HTTPValidationError
            );
          }
          break;
      }

      return res;
    });
  }

  /**
   * Retrieve aerodromes found along a route.
   *
   * @remarks
   * Retrieve aerodromes found along a route. Request body parameters are:
   * * route: [GeoJSON Linestring](https://www.rfc-editor.org/rfc/rfc7946.html#appendix-A) defining the route. Max allowed length is 50 km.
   *
   * Successful requests return a GeoJSON FeatureCollection, with a separate Feature for each
   * Aerodrome found. All Features will include properties for the facility name, ident,
   * type, and operational status.
   */
  aerodromesByRouteUsV1AerodromesRouteQueryPost(
    req: operations.AerodromesByRouteUsV1AerodromesRouteQueryPostRequest,
    config?: AxiosRequestConfig
  ): Promise<operations.AerodromesByRouteUsV1AerodromesRouteQueryPostResponse> {
    if (!(req instanceof utils.SpeakeasyBase)) {
      req = new operations.AerodromesByRouteUsV1AerodromesRouteQueryPostRequest(
        req
      );
    }

    const baseURL: string = this._serverURL;
    const url: string =
      baseURL.replace(/\/$/, "") + "/us/v1/aerodromes/route-query";

    let [reqBodyHeaders, reqBody]: [object, any] = [{}, {}];

    try {
      [reqBodyHeaders, reqBody] = utils.serializeRequestBody(
        req,
        "aerodromesByRoute",
        "json"
      );
    } catch (e: unknown) {
      if (e instanceof Error) {
        throw new Error(`Error serializing request body, cause: ${e.message}`);
      }
    }

    const client: AxiosInstance = this._defaultClient;

    const headers = {
      ...utils.getHeadersFromRequest(req),
      ...reqBodyHeaders,
      ...config?.headers,
    };
    if (reqBody == null || Object.keys(reqBody).length === 0)
      throw new Error("request body is required");

    const r = client.request({
      url: url,
      method: "post",
      headers: headers,
      data: reqBody,
      ...config,
    });

    return r.then((httpRes: AxiosResponse) => {
      const contentType: string = httpRes?.headers?.["content-type"] ?? "";

      if (httpRes?.status == null)
        throw new Error(`status code not found in response: ${httpRes}`);
      const res: operations.AerodromesByRouteUsV1AerodromesRouteQueryPostResponse =
        new operations.AerodromesByRouteUsV1AerodromesRouteQueryPostResponse({
          statusCode: httpRes.status,
          contentType: contentType,
          rawResponse: httpRes,
        });
      switch (true) {
        case httpRes?.status == 200:
          if (utils.matchContentType(contentType, `application/json`)) {
            res.aerodromeRouteResponse = utils.objectToClass(
              httpRes?.data,
              shared.AerodromeRouteResponse
            );
          }
          break;
        case httpRes?.status == 422:
          if (utils.matchContentType(contentType, `application/json`)) {
            res.httpValidationError = utils.objectToClass(
              httpRes?.data,
              shared.HTTPValidationError
            );
          }
          break;
      }

      return res;
    });
  }
}
