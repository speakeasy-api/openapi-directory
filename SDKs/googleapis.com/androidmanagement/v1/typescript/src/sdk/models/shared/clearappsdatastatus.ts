/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import {
  objectToClass,
  SpeakeasyBase,
  SpeakeasyMetadata,
} from "../../../internal/utils";
import { PerAppResult } from "./perappresult";
import { Expose, Transform } from "class-transformer";

/**
 * Status of the CLEAR_APP_DATA command to clear the data of specified apps from the device.
 */
export class ClearAppsDataStatus extends SpeakeasyBase {
  /**
   * The per-app results, a mapping from package names to the respective clearing result.
   */
  @SpeakeasyMetadata({ elemType: PerAppResult })
  @Expose({ name: "results" })
  @Transform(
    ({ value }) => {
      const obj: Record<string, PerAppResult> = {};
      for (const key in value) {
        obj[key] = objectToClass(value[key], PerAppResult);
      }
      return obj;
    },
    { toClassOnly: true }
  )
  results?: Record<string, PerAppResult>;
}
