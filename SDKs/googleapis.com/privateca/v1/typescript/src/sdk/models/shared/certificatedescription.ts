/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import { SpeakeasyBase, SpeakeasyMetadata } from "../../../internal/utils";
import { CertificateFingerprint } from "./certificatefingerprint";
import { KeyId } from "./keyid";
import { PublicKey } from "./publickey";
import { SubjectDescription } from "./subjectdescription";
import { X509Parameters } from "./x509parameters";
import { Expose, Type } from "class-transformer";

/**
 * A CertificateDescription describes an X.509 certificate or CSR that has been issued, as an alternative to using ASN.1 / X.509.
 */
export class CertificateDescription extends SpeakeasyBase {
  /**
   * Describes lists of issuer CA certificate URLs that appear in the "Authority Information Access" extension in the certificate.
   */
  @SpeakeasyMetadata()
  @Expose({ name: "aiaIssuingCertificateUrls" })
  aiaIssuingCertificateUrls?: string[];

  /**
   * A KeyId identifies a specific public key, usually by hashing the public key.
   */
  @SpeakeasyMetadata()
  @Expose({ name: "authorityKeyId" })
  @Type(() => KeyId)
  authorityKeyId?: KeyId;

  /**
   * A group of fingerprints for the x509 certificate.
   */
  @SpeakeasyMetadata()
  @Expose({ name: "certFingerprint" })
  @Type(() => CertificateFingerprint)
  certFingerprint?: CertificateFingerprint;

  /**
   * Describes a list of locations to obtain CRL information, i.e. the DistributionPoint.fullName described by https://tools.ietf.org/html/rfc5280#section-4.2.1.13
   */
  @SpeakeasyMetadata()
  @Expose({ name: "crlDistributionPoints" })
  crlDistributionPoints?: string[];

  /**
   * A PublicKey describes a public key.
   */
  @SpeakeasyMetadata()
  @Expose({ name: "publicKey" })
  @Type(() => PublicKey)
  publicKey?: PublicKey;

  /**
   * These values describe fields in an issued X.509 certificate such as the distinguished name, subject alternative names, serial number, and lifetime.
   */
  @SpeakeasyMetadata()
  @Expose({ name: "subjectDescription" })
  @Type(() => SubjectDescription)
  subjectDescription?: SubjectDescription;

  /**
   * A KeyId identifies a specific public key, usually by hashing the public key.
   */
  @SpeakeasyMetadata()
  @Expose({ name: "subjectKeyId" })
  @Type(() => KeyId)
  subjectKeyId?: KeyId;

  /**
   * An X509Parameters is used to describe certain fields of an X.509 certificate, such as the key usage fields, fields specific to CA certificates, certificate policy extensions and custom extensions.
   */
  @SpeakeasyMetadata()
  @Expose({ name: "x509Description" })
  @Type(() => X509Parameters)
  x509Description?: X509Parameters;
}
