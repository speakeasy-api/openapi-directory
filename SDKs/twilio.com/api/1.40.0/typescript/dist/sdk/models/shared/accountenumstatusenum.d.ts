/**
 * Only return Account resources with the given status. Can be `closed`, `suspended` or `active`.
 */
export declare enum AccountEnumStatusEnum {
    Active = "active",
    Suspended = "suspended",
    Closed = "closed"
}
