import { SpeakeasyBase } from "../../../internal/utils";
export declare class LocationIdSet extends SpeakeasyBase {
    /**
     * A non-empty list of location IDs. They must all be of the same location type (e.g., state).
     */
    locationIds?: string[];
}
