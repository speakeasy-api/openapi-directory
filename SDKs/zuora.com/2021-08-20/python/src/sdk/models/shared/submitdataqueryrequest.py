"""Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT."""

from __future__ import annotations
import dataclasses
from dataclasses_json import Undefined, dataclass_json
from enum import Enum
from sdk import utils
from typing import Optional

class SubmitDataQueryRequestCompressionEnum(str, Enum):
    r"""Specifies whether Zuora compresses the query results."""
    NONE = 'NONE'
    GZIP = 'GZIP'
    ZIP = 'ZIP'

class SubmitDataQueryRequestOutputTargetEnum(str, Enum):
    r"""Set this field to `S3`."""
    S3 = 'S3'


@dataclass_json(undefined=Undefined.EXCLUDE)
@dataclasses.dataclass
class SubmitDataQueryRequestOutput:
    r"""Additional information about the query results."""
    
    target: SubmitDataQueryRequestOutputTargetEnum = dataclasses.field(metadata={'dataclasses_json': { 'letter_case': utils.get_field_name('target') }})
    r"""Set this field to `S3`."""  
    
class SubmitDataQueryRequestOutputFormatEnum(str, Enum):
    r"""Specifies the format of the query results.
    
    * `JSON` - Each row in the query results will be a JSON object. The format of the query result file is [JSON Lines](http://jsonlines.org/).
    * `CSV` - Each row in the query results will be a comma-separated list of values.
    * `TSV` - Each row in the query results will be a tab-separated list of values.
    * `DSV` - Pass any character as your custom delimiter into the `columnSeparator` field.
    """
    JSON = 'JSON'
    CSV = 'CSV'
    TSV = 'TSV'
    DSV = 'DSV'

class SubmitDataQueryRequestSourceDataEnum(str, Enum):
    r"""Specifiy that data queries run against the live transactional databases at Zuora (Data Query Live), or run against the optimized, replicated database at 12 hours freshness for high volume extraction (Data Query Unlimited).
    
    **Note**: Data Query Unlimited is an Early Adopter feature.  If you want to have access to the feature, submit a request at [Zuora Global Support](http://support.zuora.com/). 
    
    * `DATAHUB` - Data queries run against the optimized, replicated database at 12 hours freshness for high volume extraction (Data Query Unlimited). Data Query Unlimited is an Early Adopter feature.  If you want to have access to the feature, submit a request at [Zuora Global Support](http://support.zuora.com/). 
    * `LIVE` - Data queries run against the live transactional databases at Zuora (Data Query Live).
    
    The default value is `LIVE`.
    """
    DATAHUB = 'DATAHUB'
    LIVE = 'LIVE'


@dataclass_json(undefined=Undefined.EXCLUDE)
@dataclasses.dataclass
class SubmitDataQueryRequest:
    
    compression: SubmitDataQueryRequestCompressionEnum = dataclasses.field(metadata={'dataclasses_json': { 'letter_case': utils.get_field_name('compression') }})
    r"""Specifies whether Zuora compresses the query results."""  
    output: SubmitDataQueryRequestOutput = dataclasses.field(metadata={'dataclasses_json': { 'letter_case': utils.get_field_name('output') }})
    r"""Additional information about the query results."""  
    output_format: SubmitDataQueryRequestOutputFormatEnum = dataclasses.field(metadata={'dataclasses_json': { 'letter_case': utils.get_field_name('outputFormat') }})
    r"""Specifies the format of the query results.
    
    * `JSON` - Each row in the query results will be a JSON object. The format of the query result file is [JSON Lines](http://jsonlines.org/).
    * `CSV` - Each row in the query results will be a comma-separated list of values.
    * `TSV` - Each row in the query results will be a tab-separated list of values.
    * `DSV` - Pass any character as your custom delimiter into the `columnSeparator` field.
    """  
    query: str = dataclasses.field(metadata={'dataclasses_json': { 'letter_case': utils.get_field_name('query') }})
    r"""The query to perform. See [SQL Queries in Data Query](https://knowledgecenter.zuora.com/DC_Developers/BA_Data_Query/BA_SQL_Queries_in_Data_Query) for more information."""  
    column_separator: Optional[str] = dataclasses.field(default=None, metadata={'dataclasses_json': { 'letter_case': utils.get_field_name('columnSeparator'), 'exclude': lambda f: f is None }})
    r"""The column separator. Only applicable if the `outputFormat` is `DSV`."""  
    encryption_key: Optional[str] = dataclasses.field(default=None, metadata={'dataclasses_json': { 'letter_case': utils.get_field_name('encryptionKey'), 'exclude': lambda f: f is None }})
    r"""Base-64 encoded public key of an RSA key-pair.
    
    Note that Data Query only supports 1024-bit RSA keys.
    
    If you set this field, Zuora encrypts the query results using the provided public key. You must use the corresponding private key to decrypt the query results.
    """  
    read_deleted: Optional[bool] = dataclasses.field(default=None, metadata={'dataclasses_json': { 'letter_case': utils.get_field_name('readDeleted'), 'exclude': lambda f: f is None }})
    r"""Indicates whether the query will retrieve only the deleted record. If `readDeleted` is set to `false` or it is not included in the request body, the query will retrieve only the non-deleted records. If it is set to `true`, only the deleted records will be retrieved.
    
    Note that Data Query is subject to Zuora Data Retention Policy. The retention period of deleted data is 30 days. You can only retrieve deleted data for 30 days through Data Query.
    """  
    source_data: Optional[SubmitDataQueryRequestSourceDataEnum] = dataclasses.field(default=None, metadata={'dataclasses_json': { 'letter_case': utils.get_field_name('sourceData'), 'exclude': lambda f: f is None }})
    r"""Specifiy that data queries run against the live transactional databases at Zuora (Data Query Live), or run against the optimized, replicated database at 12 hours freshness for high volume extraction (Data Query Unlimited).
    
    **Note**: Data Query Unlimited is an Early Adopter feature.  If you want to have access to the feature, submit a request at [Zuora Global Support](http://support.zuora.com/). 
    
    * `DATAHUB` - Data queries run against the optimized, replicated database at 12 hours freshness for high volume extraction (Data Query Unlimited). Data Query Unlimited is an Early Adopter feature.  If you want to have access to the feature, submit a request at [Zuora Global Support](http://support.zuora.com/). 
    * `LIVE` - Data queries run against the live transactional databases at Zuora (Data Query Live).
    
    The default value is `LIVE`.
    """  
    use_index_join: Optional[bool] = dataclasses.field(default=None, metadata={'dataclasses_json': { 'letter_case': utils.get_field_name('useIndexJoin'), 'exclude': lambda f: f is None }})
    r"""Indicates whether to use Index Join. Index join is useful when you have a specific reference value in your WHERE clause to index another large table by. See [Use Index Join](https://knowledgecenter.zuora.com/DC_Developers/BA_Data_Query/Best_practices_of_Data_Query#Use_Index_Join) for more information."""  
    