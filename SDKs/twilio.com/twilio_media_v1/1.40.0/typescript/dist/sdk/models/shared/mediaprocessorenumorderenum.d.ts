/**
 * The sort order of the list by `date_created`. Can be: `asc` (ascending) or `desc` (descending) with `desc` as the default.
 */
export declare enum MediaProcessorEnumOrderEnum {
    Asc = "asc",
    Desc = "desc"
}
