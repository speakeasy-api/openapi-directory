/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import {
  objectToClass,
  SpeakeasyBase,
  SpeakeasyMetadata,
} from "../../../internal/utils";
import { AutoscalingPolicyCpuUtilization } from "./autoscalingpolicycpuutilization";
import { AutoscalingPolicyCustomMetricUtilization } from "./autoscalingpolicycustommetricutilization";
import { AutoscalingPolicyLoadBalancingUtilization } from "./autoscalingpolicyloadbalancingutilization";
import { AutoscalingPolicyScaleInControl } from "./autoscalingpolicyscaleincontrol";
import { AutoscalingPolicyScalingSchedule } from "./autoscalingpolicyscalingschedule";
import { Expose, Transform, Type } from "class-transformer";

/**
 * Defines operating mode for this policy.
 */
export enum AutoscalingPolicyModeEnum {
  Off = "OFF",
  On = "ON",
  OnlyScaleOut = "ONLY_SCALE_OUT",
  OnlyUp = "ONLY_UP",
}

/**
 * Cloud Autoscaler policy.
 */
export class AutoscalingPolicy extends SpeakeasyBase {
  /**
   * The number of seconds that the autoscaler waits before it starts collecting information from a new instance. This prevents the autoscaler from collecting information when the instance is initializing, during which the collected usage would not be reliable. The default time autoscaler waits is 60 seconds. Virtual machine initialization times might vary because of numerous factors. We recommend that you test how long an instance may take to initialize. To do this, create an instance and time the startup process.
   */
  @SpeakeasyMetadata()
  @Expose({ name: "coolDownPeriodSec" })
  coolDownPeriodSec?: number;

  /**
   * CPU utilization policy.
   */
  @SpeakeasyMetadata()
  @Expose({ name: "cpuUtilization" })
  @Type(() => AutoscalingPolicyCpuUtilization)
  cpuUtilization?: AutoscalingPolicyCpuUtilization;

  /**
   * Configuration parameters of autoscaling based on a custom metric.
   */
  @SpeakeasyMetadata({ elemType: AutoscalingPolicyCustomMetricUtilization })
  @Expose({ name: "customMetricUtilizations" })
  @Type(() => AutoscalingPolicyCustomMetricUtilization)
  customMetricUtilizations?: AutoscalingPolicyCustomMetricUtilization[];

  /**
   * Configuration parameters of autoscaling based on load balancing.
   */
  @SpeakeasyMetadata()
  @Expose({ name: "loadBalancingUtilization" })
  @Type(() => AutoscalingPolicyLoadBalancingUtilization)
  loadBalancingUtilization?: AutoscalingPolicyLoadBalancingUtilization;

  /**
   * The maximum number of instances that the autoscaler can scale out to. This is required when creating or updating an autoscaler. The maximum number of replicas must not be lower than minimal number of replicas.
   */
  @SpeakeasyMetadata()
  @Expose({ name: "maxNumReplicas" })
  maxNumReplicas?: number;

  /**
   * The minimum number of replicas that the autoscaler can scale in to. This cannot be less than 0. If not provided, autoscaler chooses a default value depending on maximum number of instances allowed.
   */
  @SpeakeasyMetadata()
  @Expose({ name: "minNumReplicas" })
  minNumReplicas?: number;

  /**
   * Defines operating mode for this policy.
   */
  @SpeakeasyMetadata()
  @Expose({ name: "mode" })
  mode?: AutoscalingPolicyModeEnum;

  /**
   * Configuration that allows for slower scale in so that even if Autoscaler recommends an abrupt scale in of a MIG, it will be throttled as specified by the parameters below.
   */
  @SpeakeasyMetadata()
  @Expose({ name: "scaleInControl" })
  @Type(() => AutoscalingPolicyScaleInControl)
  scaleInControl?: AutoscalingPolicyScaleInControl;

  /**
   * Scaling schedules defined for an autoscaler. Multiple schedules can be set on an autoscaler, and they can overlap. During overlapping periods the greatest min_required_replicas of all scaling schedules is applied. Up to 128 scaling schedules are allowed.
   */
  @SpeakeasyMetadata({ elemType: AutoscalingPolicyScalingSchedule })
  @Expose({ name: "scalingSchedules" })
  @Transform(
    ({ value }) => {
      const obj: Record<string, AutoscalingPolicyScalingSchedule> = {};
      for (const key in value) {
        obj[key] = objectToClass(value[key], AutoscalingPolicyScalingSchedule);
      }
      return obj;
    },
    { toClassOnly: true }
  )
  scalingSchedules?: Record<string, AutoscalingPolicyScalingSchedule>;
}
