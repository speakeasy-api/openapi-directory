/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import * as utils from "../internal/utils";
import * as operations from "./models/operations";
import * as shared from "./models/shared";
import { AxiosInstance, AxiosRequestConfig, AxiosResponse } from "axios";

/**
 * In Probely findings are connected to vulnerability definitions.
 *
 * @remarks
 * This is how we keep track what type of vulnerability it is, it also
 * includes a name and description.
 *
 */
export class Vulnerabilities {
  _defaultClient: AxiosInstance;
  _securityClient: AxiosInstance;
  _serverURL: string;
  _language: string;
  _sdkVersion: string;
  _genVersion: string;

  constructor(
    defaultClient: AxiosInstance,
    securityClient: AxiosInstance,
    serverURL: string,
    language: string,
    sdkVersion: string,
    genVersion: string
  ) {
    this._defaultClient = defaultClient;
    this._securityClient = securityClient;
    this._serverURL = serverURL;
    this._language = language;
    this._sdkVersion = sdkVersion;
    this._genVersion = genVersion;
  }

  /**
   * List vulnerability definitions
   */
  getVulnerabilityDefinitions(
    req: operations.GetVulnerabilityDefinitionsRequest,
    config?: AxiosRequestConfig
  ): Promise<operations.GetVulnerabilityDefinitionsResponse> {
    if (!(req instanceof utils.SpeakeasyBase)) {
      req = new operations.GetVulnerabilityDefinitionsRequest(req);
    }

    const baseURL: string = this._serverURL;
    const url: string =
      baseURL.replace(/\/$/, "") + "/vulnerability_definitions/";

    const client: AxiosInstance = this._securityClient || this._defaultClient;

    const queryParams: string = utils.serializeQueryParams(req);

    const r = client.request({
      url: url + queryParams,
      method: "get",
      ...config,
    });

    return r.then((httpRes: AxiosResponse) => {
      const contentType: string = httpRes?.headers?.["content-type"] ?? "";

      if (httpRes?.status == null)
        throw new Error(`status code not found in response: ${httpRes}`);
      const res: operations.GetVulnerabilityDefinitionsResponse =
        new operations.GetVulnerabilityDefinitionsResponse({
          statusCode: httpRes.status,
          contentType: contentType,
          rawResponse: httpRes,
        });
      switch (true) {
        case httpRes?.status == 200:
          if (utils.matchContentType(contentType, `application/json`)) {
            res.getVulnerabilityDefinitions200ApplicationJSONObject =
              utils.objectToClass(
                httpRes?.data,
                operations.GetVulnerabilityDefinitions200ApplicationJSON
              );
          }
          break;
        case httpRes?.status == 401:
          if (utils.matchContentType(contentType, `application/json`)) {
            res.getVulnerabilityDefinitions401ApplicationJSONObject =
              utils.objectToClass(
                httpRes?.data,
                operations.GetVulnerabilityDefinitions401ApplicationJSON
              );
          }
          break;
      }

      return res;
    });
  }

  /**
   * Retrieve vulnerability definition
   */
  getVulnerabilityDefinitionsId(
    req: operations.GetVulnerabilityDefinitionsIdRequest,
    config?: AxiosRequestConfig
  ): Promise<operations.GetVulnerabilityDefinitionsIdResponse> {
    if (!(req instanceof utils.SpeakeasyBase)) {
      req = new operations.GetVulnerabilityDefinitionsIdRequest(req);
    }

    const baseURL: string = this._serverURL;
    const url: string = utils.generateURL(
      baseURL,
      "/vulnerability_definitions/{id}/",
      req
    );

    const client: AxiosInstance = this._securityClient || this._defaultClient;

    const r = client.request({
      url: url,
      method: "get",
      ...config,
    });

    return r.then((httpRes: AxiosResponse) => {
      const contentType: string = httpRes?.headers?.["content-type"] ?? "";

      if (httpRes?.status == null)
        throw new Error(`status code not found in response: ${httpRes}`);
      const res: operations.GetVulnerabilityDefinitionsIdResponse =
        new operations.GetVulnerabilityDefinitionsIdResponse({
          statusCode: httpRes.status,
          contentType: contentType,
          rawResponse: httpRes,
        });
      switch (true) {
        case httpRes?.status == 200:
          if (utils.matchContentType(contentType, `application/json`)) {
            res.vulnerabilityDefinition = utils.objectToClass(
              httpRes?.data,
              shared.VulnerabilityDefinition
            );
          }
          break;
        case httpRes?.status == 401:
          if (utils.matchContentType(contentType, `application/json`)) {
            res.getVulnerabilityDefinitionsId401ApplicationJSONObject =
              utils.objectToClass(
                httpRes?.data,
                operations.GetVulnerabilityDefinitionsId401ApplicationJSON
              );
          }
          break;
      }

      return res;
    });
  }
}
