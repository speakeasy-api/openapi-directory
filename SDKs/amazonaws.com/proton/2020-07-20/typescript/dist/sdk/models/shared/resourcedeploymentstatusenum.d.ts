/**
 * The state that a PR-based deployment can be updated to.
 */
export declare enum ResourceDeploymentStatusEnum {
    InProgress = "IN_PROGRESS",
    Failed = "FAILED",
    Succeeded = "SUCCEEDED"
}
