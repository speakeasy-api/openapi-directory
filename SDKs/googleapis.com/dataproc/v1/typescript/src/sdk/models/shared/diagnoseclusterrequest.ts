/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import { SpeakeasyBase, SpeakeasyMetadata } from "../../../internal/utils";
import { Interval } from "./interval";
import { Expose, Type } from "class-transformer";

/**
 * A request to collect cluster diagnostic information.
 */
export class DiagnoseClusterRequest extends SpeakeasyBase {
  /**
   * Represents a time interval, encoded as a Timestamp start (inclusive) and a Timestamp end (exclusive).The start must be less than or equal to the end. When the start equals the end, the interval is empty (matches no time). When both start and end are unspecified, the interval matches any time.
   */
  @SpeakeasyMetadata()
  @Expose({ name: "diagnosisInterval" })
  @Type(() => Interval)
  diagnosisInterval?: Interval;

  /**
   * Optional. DEPRECATED Specifies the job on which diagnosis is to be performed. Format: projects/{project}/regions/{region}/jobs/{job}
   */
  @SpeakeasyMetadata()
  @Expose({ name: "job" })
  job?: string;

  /**
   * Optional. Specifies a list of jobs on which diagnosis is to be performed. Format: projects/{project}/regions/{region}/jobs/{job}
   */
  @SpeakeasyMetadata()
  @Expose({ name: "jobs" })
  jobs?: string[];

  /**
   * Optional. DEPRECATED Specifies the yarn application on which diagnosis is to be performed.
   */
  @SpeakeasyMetadata()
  @Expose({ name: "yarnApplicationId" })
  yarnApplicationId?: string;

  /**
   * Optional. Specifies a list of yarn applications on which diagnosis is to be performed.
   */
  @SpeakeasyMetadata()
  @Expose({ name: "yarnApplicationIds" })
  yarnApplicationIds?: string[];
}
