


export enum SmsStatusEnum {
    Delivered = "delivered",
    Expired = "expired",
    Failed = "failed",
    Rejected = "rejected",
    Accepted = "accepted",
    Buffered = "buffered",
    Unknown = "unknown"
}
