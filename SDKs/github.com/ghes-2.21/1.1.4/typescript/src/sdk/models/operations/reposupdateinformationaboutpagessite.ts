/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import { SpeakeasyBase, SpeakeasyMetadata } from "../../../internal/utils";
import * as shared from "../shared";
import { AxiosResponse } from "axios";
import { Expose } from "class-transformer";

/**
 * Update the source for the repository. Must include the branch name and path.
 */
export enum ReposUpdateInformationAboutPagesSiteRequestBody4Source2Enum {
  GhPages = "gh-pages",
  Master = "master",
  MasterDocs = "master /docs",
}

/**
 * Update the source for the repository. Must include the branch name, and may optionally specify the subdirectory `/docs`. Possible values are `"gh-pages"`, `"master"`, and `"master /docs"`.
 */
export enum ReposUpdateInformationAboutPagesSiteRequestBody4Source1Enum {
  GhPages = "gh-pages",
  Master = "master",
  MasterDocs = "master /docs",
}

export class ReposUpdateInformationAboutPagesSiteRequestBody4 extends SpeakeasyBase {
  /**
   * Specify whether HTTPS should be enforced for the repository.
   */
  @SpeakeasyMetadata()
  @Expose({ name: "https_enforced" })
  httpsEnforced: boolean;

  /**
   * Configures access controls for the GitHub Pages site. If public is set to `true`, the site is accessible to anyone on the internet. If set to `false`, the site will only be accessible to users who have at least `read` access to the repository that published the site. This includes anyone in your Enterprise if the repository is set to `internal` visibility. This feature is only available to repositories in an organization on an Enterprise plan.
   */
  @SpeakeasyMetadata()
  @Expose({ name: "public" })
  public?: boolean;

  /**
   * Update the source for the repository. Must include the branch name, and may optionally specify the subdirectory `/docs`. Possible values are `"gh-pages"`, `"master"`, and `"master /docs"`.
   */
  @SpeakeasyMetadata()
  @Expose({ name: "source" })
  source?: any;
}

/**
 * Update the source for the repository. Must include the branch name and path.
 */
export enum ReposUpdateInformationAboutPagesSiteRequestBody3Source2Enum {
  GhPages = "gh-pages",
  Master = "master",
  MasterDocs = "master /docs",
}

/**
 * Update the source for the repository. Must include the branch name, and may optionally specify the subdirectory `/docs`. Possible values are `"gh-pages"`, `"master"`, and `"master /docs"`.
 */
export enum ReposUpdateInformationAboutPagesSiteRequestBody3Source1Enum {
  GhPages = "gh-pages",
  Master = "master",
  MasterDocs = "master /docs",
}

export class ReposUpdateInformationAboutPagesSiteRequestBody3 extends SpeakeasyBase {
  /**
   * Specify whether HTTPS should be enforced for the repository.
   */
  @SpeakeasyMetadata()
  @Expose({ name: "https_enforced" })
  httpsEnforced?: boolean;

  /**
   * Configures access controls for the GitHub Pages site. If public is set to `true`, the site is accessible to anyone on the internet. If set to `false`, the site will only be accessible to users who have at least `read` access to the repository that published the site. This includes anyone in your Enterprise if the repository is set to `internal` visibility. This feature is only available to repositories in an organization on an Enterprise plan.
   */
  @SpeakeasyMetadata()
  @Expose({ name: "public" })
  public: boolean;

  /**
   * Update the source for the repository. Must include the branch name, and may optionally specify the subdirectory `/docs`. Possible values are `"gh-pages"`, `"master"`, and `"master /docs"`.
   */
  @SpeakeasyMetadata()
  @Expose({ name: "source" })
  source?: any;
}

/**
 * Update the source for the repository. Must include the branch name and path.
 */
export enum ReposUpdateInformationAboutPagesSiteRequestBody2Source2Enum {
  GhPages = "gh-pages",
  Master = "master",
  MasterDocs = "master /docs",
}

/**
 * Update the source for the repository. Must include the branch name, and may optionally specify the subdirectory `/docs`. Possible values are `"gh-pages"`, `"master"`, and `"master /docs"`.
 */
export enum ReposUpdateInformationAboutPagesSiteRequestBody2Source1Enum {
  GhPages = "gh-pages",
  Master = "master",
  MasterDocs = "master /docs",
}

export class ReposUpdateInformationAboutPagesSiteRequestBody2 extends SpeakeasyBase {
  /**
   * Specify whether HTTPS should be enforced for the repository.
   */
  @SpeakeasyMetadata()
  @Expose({ name: "https_enforced" })
  httpsEnforced?: boolean;

  /**
   * Configures access controls for the GitHub Pages site. If public is set to `true`, the site is accessible to anyone on the internet. If set to `false`, the site will only be accessible to users who have at least `read` access to the repository that published the site. This includes anyone in your Enterprise if the repository is set to `internal` visibility. This feature is only available to repositories in an organization on an Enterprise plan.
   */
  @SpeakeasyMetadata()
  @Expose({ name: "public" })
  public?: boolean;

  /**
   * Update the source for the repository. Must include the branch name, and may optionally specify the subdirectory `/docs`. Possible values are `"gh-pages"`, `"master"`, and `"master /docs"`.
   */
  @SpeakeasyMetadata()
  @Expose({ name: "source" })
  source?: any;
}

/**
 * Update the source for the repository. Must include the branch name and path.
 */
export enum ReposUpdateInformationAboutPagesSiteRequestBody1Source2Enum {
  GhPages = "gh-pages",
  Master = "master",
  MasterDocs = "master /docs",
}

/**
 * Update the source for the repository. Must include the branch name, and may optionally specify the subdirectory `/docs`. Possible values are `"gh-pages"`, `"master"`, and `"master /docs"`.
 */
export enum ReposUpdateInformationAboutPagesSiteRequestBody1Source1Enum {
  GhPages = "gh-pages",
  Master = "master",
  MasterDocs = "master /docs",
}

export class ReposUpdateInformationAboutPagesSiteRequestBody1 extends SpeakeasyBase {
  /**
   * Specify whether HTTPS should be enforced for the repository.
   */
  @SpeakeasyMetadata()
  @Expose({ name: "https_enforced" })
  httpsEnforced?: boolean;

  /**
   * Configures access controls for the GitHub Pages site. If public is set to `true`, the site is accessible to anyone on the internet. If set to `false`, the site will only be accessible to users who have at least `read` access to the repository that published the site. This includes anyone in your Enterprise if the repository is set to `internal` visibility. This feature is only available to repositories in an organization on an Enterprise plan.
   */
  @SpeakeasyMetadata()
  @Expose({ name: "public" })
  public?: boolean;

  /**
   * Update the source for the repository. Must include the branch name, and may optionally specify the subdirectory `/docs`. Possible values are `"gh-pages"`, `"master"`, and `"master /docs"`.
   */
  @SpeakeasyMetadata()
  @Expose({ name: "source" })
  source: any;
}

export class ReposUpdateInformationAboutPagesSiteRequest extends SpeakeasyBase {
  @SpeakeasyMetadata({ data: "request, media_type=application/json" })
  requestBody: any;

  @SpeakeasyMetadata({
    data: "pathParam, style=simple;explode=false;name=owner",
  })
  owner: string;

  @SpeakeasyMetadata({
    data: "pathParam, style=simple;explode=false;name=repo",
  })
  repo: string;
}

export class ReposUpdateInformationAboutPagesSiteResponse extends SpeakeasyBase {
  @SpeakeasyMetadata()
  contentType: string;

  @SpeakeasyMetadata()
  statusCode: number;

  @SpeakeasyMetadata()
  rawResponse?: AxiosResponse;

  /**
   * Bad Request
   */
  @SpeakeasyMetadata()
  basicError?: shared.BasicError;

  /**
   * Bad Request
   */
  @SpeakeasyMetadata()
  scimError?: shared.ScimError;

  /**
   * Validation failed
   */
  @SpeakeasyMetadata()
  validationError?: shared.ValidationError;
}
