/**
 * Status to filter by, with possible values `started`, `ended` or `failed`.
 */
export declare enum MediaProcessorEnumStatusEnum {
    Failed = "failed",
    Started = "started",
    Ended = "ended"
}
