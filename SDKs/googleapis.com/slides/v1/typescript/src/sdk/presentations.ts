/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import * as utils from "../internal/utils";
import * as operations from "./models/operations";
import * as shared from "./models/shared";
import { AxiosInstance, AxiosRequestConfig, AxiosResponse } from "axios";

export class Presentations {
  _defaultClient: AxiosInstance;
  _securityClient: AxiosInstance;
  _serverURL: string;
  _language: string;
  _sdkVersion: string;
  _genVersion: string;

  constructor(
    defaultClient: AxiosInstance,
    securityClient: AxiosInstance,
    serverURL: string,
    language: string,
    sdkVersion: string,
    genVersion: string
  ) {
    this._defaultClient = defaultClient;
    this._securityClient = securityClient;
    this._serverURL = serverURL;
    this._language = language;
    this._sdkVersion = sdkVersion;
    this._genVersion = genVersion;
  }

  /**
   * Applies one or more updates to the presentation. Each request is validated before being applied. If any request is not valid, then the entire request will fail and nothing will be applied. Some requests have replies to give you some information about how they are applied. Other requests do not need to return information; these each return an empty reply. The order of replies matches that of the requests. For example, suppose you call batchUpdate with four updates, and only the third one returns information. The response would have two empty replies: the reply to the third request, and another empty reply, in that order. Because other users may be editing the presentation, the presentation might not exactly reflect your changes: your changes may be altered with respect to collaborator changes. If there are no collaborators, the presentation should reflect your changes. In any case, the updates in your request are guaranteed to be applied together atomically.
   */
  slidesPresentationsBatchUpdate(
    req: operations.SlidesPresentationsBatchUpdateRequest,
    security: operations.SlidesPresentationsBatchUpdateSecurity,
    config?: AxiosRequestConfig
  ): Promise<operations.SlidesPresentationsBatchUpdateResponse> {
    if (!(req instanceof utils.SpeakeasyBase)) {
      req = new operations.SlidesPresentationsBatchUpdateRequest(req);
    }

    const baseURL: string = this._serverURL;
    const url: string = utils.generateURL(
      baseURL,
      "/v1/presentations/{presentationId}:batchUpdate",
      req
    );

    let [reqBodyHeaders, reqBody]: [object, any] = [{}, {}];

    try {
      [reqBodyHeaders, reqBody] = utils.serializeRequestBody(
        req,
        "batchUpdatePresentationRequest",
        "json"
      );
    } catch (e: unknown) {
      if (e instanceof Error) {
        throw new Error(`Error serializing request body, cause: ${e.message}`);
      }
    }
    if (!(security instanceof utils.SpeakeasyBase)) {
      security = new operations.SlidesPresentationsBatchUpdateSecurity(
        security
      );
    }
    const client: AxiosInstance = utils.createSecurityClient(
      this._defaultClient,
      security
    );

    const headers = { ...reqBodyHeaders, ...config?.headers };
    const queryParams: string = utils.serializeQueryParams(req);

    const r = client.request({
      url: url + queryParams,
      method: "post",
      headers: headers,
      data: reqBody,
      ...config,
    });

    return r.then((httpRes: AxiosResponse) => {
      const contentType: string = httpRes?.headers?.["content-type"] ?? "";

      if (httpRes?.status == null)
        throw new Error(`status code not found in response: ${httpRes}`);
      const res: operations.SlidesPresentationsBatchUpdateResponse =
        new operations.SlidesPresentationsBatchUpdateResponse({
          statusCode: httpRes.status,
          contentType: contentType,
          rawResponse: httpRes,
        });
      switch (true) {
        case httpRes?.status == 200:
          if (utils.matchContentType(contentType, `application/json`)) {
            res.batchUpdatePresentationResponse = utils.objectToClass(
              httpRes?.data,
              shared.BatchUpdatePresentationResponse
            );
          }
          break;
      }

      return res;
    });
  }

  /**
   * Creates a blank presentation using the title given in the request. If a `presentationId` is provided, it is used as the ID of the new presentation. Otherwise, a new ID is generated. Other fields in the request, including any provided content, are ignored. Returns the created presentation.
   */
  slidesPresentationsCreate(
    req: operations.SlidesPresentationsCreateRequest,
    security: operations.SlidesPresentationsCreateSecurity,
    config?: AxiosRequestConfig
  ): Promise<operations.SlidesPresentationsCreateResponse> {
    if (!(req instanceof utils.SpeakeasyBase)) {
      req = new operations.SlidesPresentationsCreateRequest(req);
    }

    const baseURL: string = this._serverURL;
    const url: string = baseURL.replace(/\/$/, "") + "/v1/presentations";

    let [reqBodyHeaders, reqBody]: [object, any] = [{}, {}];

    try {
      [reqBodyHeaders, reqBody] = utils.serializeRequestBody(
        req,
        "presentation",
        "json"
      );
    } catch (e: unknown) {
      if (e instanceof Error) {
        throw new Error(`Error serializing request body, cause: ${e.message}`);
      }
    }
    if (!(security instanceof utils.SpeakeasyBase)) {
      security = new operations.SlidesPresentationsCreateSecurity(security);
    }
    const client: AxiosInstance = utils.createSecurityClient(
      this._defaultClient,
      security
    );

    const headers = { ...reqBodyHeaders, ...config?.headers };
    const queryParams: string = utils.serializeQueryParams(req);

    const r = client.request({
      url: url + queryParams,
      method: "post",
      headers: headers,
      data: reqBody,
      ...config,
    });

    return r.then((httpRes: AxiosResponse) => {
      const contentType: string = httpRes?.headers?.["content-type"] ?? "";

      if (httpRes?.status == null)
        throw new Error(`status code not found in response: ${httpRes}`);
      const res: operations.SlidesPresentationsCreateResponse =
        new operations.SlidesPresentationsCreateResponse({
          statusCode: httpRes.status,
          contentType: contentType,
          rawResponse: httpRes,
        });
      switch (true) {
        case httpRes?.status == 200:
          if (utils.matchContentType(contentType, `application/json`)) {
            res.presentation = utils.objectToClass(
              httpRes?.data,
              shared.Presentation
            );
          }
          break;
      }

      return res;
    });
  }

  /**
   * Gets the latest version of the specified presentation.
   */
  slidesPresentationsGet(
    req: operations.SlidesPresentationsGetRequest,
    security: operations.SlidesPresentationsGetSecurity,
    config?: AxiosRequestConfig
  ): Promise<operations.SlidesPresentationsGetResponse> {
    if (!(req instanceof utils.SpeakeasyBase)) {
      req = new operations.SlidesPresentationsGetRequest(req);
    }

    const baseURL: string = this._serverURL;
    const url: string = utils.generateURL(
      baseURL,
      "/v1/presentations/{presentationId}",
      req
    );
    if (!(security instanceof utils.SpeakeasyBase)) {
      security = new operations.SlidesPresentationsGetSecurity(security);
    }
    const client: AxiosInstance = utils.createSecurityClient(
      this._defaultClient,
      security
    );

    const queryParams: string = utils.serializeQueryParams(req);

    const r = client.request({
      url: url + queryParams,
      method: "get",
      ...config,
    });

    return r.then((httpRes: AxiosResponse) => {
      const contentType: string = httpRes?.headers?.["content-type"] ?? "";

      if (httpRes?.status == null)
        throw new Error(`status code not found in response: ${httpRes}`);
      const res: operations.SlidesPresentationsGetResponse =
        new operations.SlidesPresentationsGetResponse({
          statusCode: httpRes.status,
          contentType: contentType,
          rawResponse: httpRes,
        });
      switch (true) {
        case httpRes?.status == 200:
          if (utils.matchContentType(contentType, `application/json`)) {
            res.presentation = utils.objectToClass(
              httpRes?.data,
              shared.Presentation
            );
          }
          break;
      }

      return res;
    });
  }

  /**
   * Gets the latest version of the specified page in the presentation.
   */
  slidesPresentationsPagesGet(
    req: operations.SlidesPresentationsPagesGetRequest,
    security: operations.SlidesPresentationsPagesGetSecurity,
    config?: AxiosRequestConfig
  ): Promise<operations.SlidesPresentationsPagesGetResponse> {
    if (!(req instanceof utils.SpeakeasyBase)) {
      req = new operations.SlidesPresentationsPagesGetRequest(req);
    }

    const baseURL: string = this._serverURL;
    const url: string = utils.generateURL(
      baseURL,
      "/v1/presentations/{presentationId}/pages/{pageObjectId}",
      req
    );
    if (!(security instanceof utils.SpeakeasyBase)) {
      security = new operations.SlidesPresentationsPagesGetSecurity(security);
    }
    const client: AxiosInstance = utils.createSecurityClient(
      this._defaultClient,
      security
    );

    const queryParams: string = utils.serializeQueryParams(req);

    const r = client.request({
      url: url + queryParams,
      method: "get",
      ...config,
    });

    return r.then((httpRes: AxiosResponse) => {
      const contentType: string = httpRes?.headers?.["content-type"] ?? "";

      if (httpRes?.status == null)
        throw new Error(`status code not found in response: ${httpRes}`);
      const res: operations.SlidesPresentationsPagesGetResponse =
        new operations.SlidesPresentationsPagesGetResponse({
          statusCode: httpRes.status,
          contentType: contentType,
          rawResponse: httpRes,
        });
      switch (true) {
        case httpRes?.status == 200:
          if (utils.matchContentType(contentType, `application/json`)) {
            res.page = utils.objectToClass(httpRes?.data, shared.Page);
          }
          break;
      }

      return res;
    });
  }

  /**
   * Generates a thumbnail of the latest version of the specified page in the presentation and returns a URL to the thumbnail image. This request counts as an [expensive read request](/slides/limits) for quota purposes.
   */
  slidesPresentationsPagesGetThumbnail(
    req: operations.SlidesPresentationsPagesGetThumbnailRequest,
    security: operations.SlidesPresentationsPagesGetThumbnailSecurity,
    config?: AxiosRequestConfig
  ): Promise<operations.SlidesPresentationsPagesGetThumbnailResponse> {
    if (!(req instanceof utils.SpeakeasyBase)) {
      req = new operations.SlidesPresentationsPagesGetThumbnailRequest(req);
    }

    const baseURL: string = this._serverURL;
    const url: string = utils.generateURL(
      baseURL,
      "/v1/presentations/{presentationId}/pages/{pageObjectId}/thumbnail",
      req
    );
    if (!(security instanceof utils.SpeakeasyBase)) {
      security = new operations.SlidesPresentationsPagesGetThumbnailSecurity(
        security
      );
    }
    const client: AxiosInstance = utils.createSecurityClient(
      this._defaultClient,
      security
    );

    const queryParams: string = utils.serializeQueryParams(req);

    const r = client.request({
      url: url + queryParams,
      method: "get",
      ...config,
    });

    return r.then((httpRes: AxiosResponse) => {
      const contentType: string = httpRes?.headers?.["content-type"] ?? "";

      if (httpRes?.status == null)
        throw new Error(`status code not found in response: ${httpRes}`);
      const res: operations.SlidesPresentationsPagesGetThumbnailResponse =
        new operations.SlidesPresentationsPagesGetThumbnailResponse({
          statusCode: httpRes.status,
          contentType: contentType,
          rawResponse: httpRes,
        });
      switch (true) {
        case httpRes?.status == 200:
          if (utils.matchContentType(contentType, `application/json`)) {
            res.thumbnail = utils.objectToClass(
              httpRes?.data,
              shared.Thumbnail
            );
          }
          break;
      }

      return res;
    });
  }
}
