# Me

### Available Operations

* [CreateAPICredential](#createapicredential) - Create api credentials
* [DeleteAPICredential](#deleteapicredential) - Delete api credentials
* [DisableAPICredentials](#disableapicredentials) - Disable specified API credentials
* [EnableAPICredentials](#enableapicredentials) - Enable specified API credentials
* [FindAPICredentials](#findapicredentials) - Find api credentials
* [GetAccount](#getaccount) - Find account details
* [GetAPICredential](#getapicredential) - Find a specific api credential
* [GetBillingPlanUsage](#getbillingplanusage) - Find plan usage
* [GetCallerIds](#getcallerids) - Find caller ids
* [GetCreditUsage](#getcreditusage) - Find credit usage
* [SendVerificationCodeToCallerID](#sendverificationcodetocallerid) - Create a caller id
* [VerifyCallerID](#verifycallerid) - Verify a caller id

## CreateAPICredential

Creates an API credentials for the CallFire API. This endpoint requires full CallFire account credentials to be used, authenticated using Basic Authentication. At the moment user provides only the name for the credentials. The generated credentials can be used to access any CallFire APIs. For authentication use account credentials.

### Example Usage

```go
package main

import(
	"context"
	"log"
	"openapi"
	"openapi/pkg/models/shared"
	"openapi/pkg/models/operations"
)

func main() {
    s := sdk.New()

    ctx := context.Background()
    res, err := s.Me.CreateAPICredential(ctx, shared.APICredentialInput{
        Enabled: sdk.Bool(false),
        ID: sdk.Int64(15606),
        Name: sdk.String("Ms. Duane O'Conner"),
    }, operations.CreateAPICredentialSecurity{
        Password: "YOUR_PASSWORD_HERE",
        Username: "YOUR_USERNAME_HERE",
    })
    if err != nil {
        log.Fatal(err)
    }

    if res.APICredential != nil {
        // handle response
    }
}
```

## DeleteAPICredential

Deletes a specified API credential. Currently, removes the ability to access the API. Only ACCOUNT_HOLDER can invoke this API. For authentication use account credentials.

### Example Usage

```go
package main

import(
	"context"
	"log"
	"openapi"
	"openapi/pkg/models/operations"
)

func main() {
    s := sdk.New()

    ctx := context.Background()
    res, err := s.Me.DeleteAPICredential(ctx, operations.DeleteAPICredentialRequest{
        ID: 32465,
    }, operations.DeleteAPICredentialSecurity{
        Password: "YOUR_PASSWORD_HERE",
        Username: "YOUR_USERNAME_HERE",
    })
    if err != nil {
        log.Fatal(err)
    }

    if res.StatusCode == http.StatusOK {
        // handle response
    }
}
```

## DisableAPICredentials

Disables a specified API credential. Currently, removes the ability to access the API. Only ACCOUNT_HOLDER can invoke this API. For authentication use account credentials.

### Example Usage

```go
package main

import(
	"context"
	"log"
	"openapi"
	"openapi/pkg/models/operations"
)

func main() {
    s := sdk.New()

    ctx := context.Background()
    res, err := s.Me.DisableAPICredentials(ctx, operations.DisableAPICredentialsRequest{
        ID: 221161,
    }, operations.DisableAPICredentialsSecurity{
        Password: "YOUR_PASSWORD_HERE",
        Username: "YOUR_USERNAME_HERE",
    })
    if err != nil {
        log.Fatal(err)
    }

    if res.StatusCode == http.StatusOK {
        // handle response
    }
}
```

## EnableAPICredentials

Enables a specified API credential. Currently, adds the ability to access the API. Only ACCOUNT_HOLDER can invoke this API. For authentication use account credentials.

### Example Usage

```go
package main

import(
	"context"
	"log"
	"openapi"
	"openapi/pkg/models/operations"
)

func main() {
    s := sdk.New()

    ctx := context.Background()
    res, err := s.Me.EnableAPICredentials(ctx, operations.EnableAPICredentialsRequest{
        ID: 580152,
    }, operations.EnableAPICredentialsSecurity{
        Password: "YOUR_PASSWORD_HERE",
        Username: "YOUR_USERNAME_HERE",
    })
    if err != nil {
        log.Fatal(err)
    }

    if res.StatusCode == http.StatusOK {
        // handle response
    }
}
```

## FindAPICredentials

Searches for all credentials generated by user. Returns a paged list of the API credentials. Only ACCOUNT_HOLDER can invoke this API. For authentication use account credentials.

### Example Usage

```go
package main

import(
	"context"
	"log"
	"openapi"
	"openapi/pkg/models/operations"
)

func main() {
    s := sdk.New()

    ctx := context.Background()
    res, err := s.Me.FindAPICredentials(ctx, operations.FindAPICredentialsRequest{
        Fields: sdk.String("numquam"),
        Limit: sdk.Int(771089),
        Name: sdk.String("Loretta Anderson DVM"),
        Offset: sdk.Int(618480),
    }, operations.FindAPICredentialsSecurity{
        Password: "YOUR_PASSWORD_HERE",
        Username: "YOUR_USERNAME_HERE",
    })
    if err != nil {
        log.Fatal(err)
    }

    if res.APICredentialPage != nil {
        // handle response
    }
}
```

## GetAccount

Searches for the user account details. Details include name, email, and basic account permissions. For authentication use api credentials.

### Example Usage

```go
package main

import(
	"context"
	"log"
	"openapi"
	"openapi/pkg/models/operations"
)

func main() {
    s := sdk.New()

    ctx := context.Background()
    res, err := s.Me.GetAccount(ctx, operations.GetAccountRequest{
        Fields: sdk.String("velit"),
    }, operations.GetAccountSecurity{
        Password: "YOUR_PASSWORD_HERE",
        Username: "YOUR_USERNAME_HERE",
    })
    if err != nil {
        log.Fatal(err)
    }

    if res.Account != nil {
        // handle response
    }
}
```

## GetAPICredential

Returns an API credential instance for a given api credential id. Only ACCOUNT_HOLDER can invoke this API. For authentication use account credentials.

### Example Usage

```go
package main

import(
	"context"
	"log"
	"openapi"
	"openapi/pkg/models/operations"
)

func main() {
    s := sdk.New()

    ctx := context.Background()
    res, err := s.Me.GetAPICredential(ctx, operations.GetAPICredentialRequest{
        Fields: sdk.String("voluptatibus"),
        ID: 374323,
    }, operations.GetAPICredentialSecurity{
        Password: "YOUR_PASSWORD_HERE",
        Username: "YOUR_USERNAME_HERE",
    })
    if err != nil {
        log.Fatal(err)
    }

    if res.APICredential != nil {
        // handle response
    }
}
```

## GetBillingPlanUsage

Searches for the data of a billing plan usage for the user. Returns the data of a billing plan usage for the current month. For authentication use api credentials.

### Example Usage

```go
package main

import(
	"context"
	"log"
	"openapi"
	"openapi/pkg/models/operations"
)

func main() {
    s := sdk.New()

    ctx := context.Background()
    res, err := s.Me.GetBillingPlanUsage(ctx, operations.GetBillingPlanUsageSecurity{
        Password: "YOUR_PASSWORD_HERE",
        Username: "YOUR_USERNAME_HERE",
    })
    if err != nil {
        log.Fatal(err)
    }

    if res.BillingPlanUsage != nil {
        // handle response
    }
}
```

## GetCallerIds

Returns a list of verified caller ids. If the number is not shown in the list, then it is not verified. In this case sending of a verification code is required. For authentication use api credentials.

### Example Usage

```go
package main

import(
	"context"
	"log"
	"openapi"
	"openapi/pkg/models/operations"
)

func main() {
    s := sdk.New()

    ctx := context.Background()
    res, err := s.Me.GetCallerIds(ctx, operations.GetCallerIdsSecurity{
        Password: "YOUR_PASSWORD_HERE",
        Username: "YOUR_USERNAME_HERE",
    })
    if err != nil {
        log.Fatal(err)
    }

    if res.CallerIDList != nil {
        // handle response
    }
}
```

## GetCreditUsage

Find credit usage for the user. Returns credits usage for time period specified or if unspecified then total for all time. For authentication use api credentials.

### Example Usage

```go
package main

import(
	"context"
	"log"
	"openapi"
	"openapi/pkg/models/operations"
)

func main() {
    s := sdk.New()

    ctx := context.Background()
    res, err := s.Me.GetCreditUsage(ctx, operations.GetCreditUsageRequest{
        IntervalBegin: sdk.Int64(990345),
        IntervalEnd: sdk.Int64(45659),
    }, operations.GetCreditUsageSecurity{
        Password: "YOUR_PASSWORD_HERE",
        Username: "YOUR_USERNAME_HERE",
    })
    if err != nil {
        log.Fatal(err)
    }

    if res.CreditUsage != nil {
        // handle response
    }
}
```

## SendVerificationCodeToCallerID

Generates and sends a verification code to the phone number provided in the path. The verification code is delivered via a phone call. This code needs to be submitted to the verify caller id API endpoint to complete verification. For authentication use api credentials.

### Example Usage

```go
package main

import(
	"context"
	"log"
	"openapi"
	"openapi/pkg/models/operations"
)

func main() {
    s := sdk.New()

    ctx := context.Background()
    res, err := s.Me.SendVerificationCodeToCallerID(ctx, operations.SendVerificationCodeToCallerIDRequest{
        Callerid: "ea",
    }, operations.SendVerificationCodeToCallerIDSecurity{
        Password: "YOUR_PASSWORD_HERE",
        Username: "YOUR_USERNAME_HERE",
    })
    if err != nil {
        log.Fatal(err)
    }

    if res.StatusCode == http.StatusOK {
        // handle response
    }
}
```

## VerifyCallerID

With the verification code received from the Create caller id endpoint, a call to this endpoint is required to finish verification. For authentication use api credentials.

### Example Usage

```go
package main

import(
	"context"
	"log"
	"openapi"
	"openapi/pkg/models/operations"
	"openapi/pkg/models/shared"
)

func main() {
    s := sdk.New()

    ctx := context.Background()
    res, err := s.Me.VerifyCallerID(ctx, operations.VerifyCallerIDRequest{
        CallerIDVerificationRequest: &shared.CallerIDVerificationRequest{
            VerificationCode: sdk.String("quaerat"),
        },
        Callerid: "consequuntur",
    }, operations.VerifyCallerIDSecurity{
        Password: "YOUR_PASSWORD_HERE",
        Username: "YOUR_USERNAME_HERE",
    })
    if err != nil {
        log.Fatal(err)
    }

    if res.VerifyCallerID200ApplicationJSONBoolean != nil {
        // handle response
    }
}
```
