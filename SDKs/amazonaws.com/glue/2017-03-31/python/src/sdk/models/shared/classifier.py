"""Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT."""

from __future__ import annotations
import dataclasses
from ..shared import csvclassifier as shared_csvclassifier
from ..shared import grokclassifier as shared_grokclassifier
from ..shared import jsonclassifier as shared_jsonclassifier
from ..shared import xmlclassifier as shared_xmlclassifier
from dataclasses_json import Undefined, dataclass_json
from sdk import utils
from typing import Optional


@dataclass_json(undefined=Undefined.EXCLUDE)
@dataclasses.dataclass
class Classifier:
    r"""<p>Classifiers are triggered during a crawl task. A classifier checks whether a given file is in a format it can handle. If it is, the classifier creates a schema in the form of a <code>StructType</code> object that matches that data format.</p> <p>You can use the standard classifiers that Glue provides, or you can write your own classifiers to best categorize your data sources and specify the appropriate schemas to use for them. A classifier can be a <code>grok</code> classifier, an <code>XML</code> classifier, a <code>JSON</code> classifier, or a custom <code>CSV</code> classifier, as specified in one of the fields in the <code>Classifier</code> object.</p>"""
    
    csv_classifier: Optional[shared_csvclassifier.CsvClassifier] = dataclasses.field(default=None, metadata={'dataclasses_json': { 'letter_case': utils.get_field_name('CsvClassifier'), 'exclude': lambda f: f is None }})  
    grok_classifier: Optional[shared_grokclassifier.GrokClassifier] = dataclasses.field(default=None, metadata={'dataclasses_json': { 'letter_case': utils.get_field_name('GrokClassifier'), 'exclude': lambda f: f is None }})  
    json_classifier: Optional[shared_jsonclassifier.JSONClassifier] = dataclasses.field(default=None, metadata={'dataclasses_json': { 'letter_case': utils.get_field_name('JsonClassifier'), 'exclude': lambda f: f is None }})  
    xml_classifier: Optional[shared_xmlclassifier.XMLClassifier] = dataclasses.field(default=None, metadata={'dataclasses_json': { 'letter_case': utils.get_field_name('XMLClassifier'), 'exclude': lambda f: f is None }})  
    