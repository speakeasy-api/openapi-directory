/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import { SpeakeasyBase, SpeakeasyMetadata } from "../../../internal/utils";
import { AdBreak } from "./adbreak";
import { EditAtom } from "./editatom";
import { ElementaryStream } from "./elementarystream";
import { Input } from "./input";
import { Manifest } from "./manifest";
import { MuxStream } from "./muxstream";
import { Output } from "./output";
import { Overlay } from "./overlay";
import { PubsubDestination } from "./pubsubdestination";
import { SpriteSheet } from "./spritesheet";
import { Expose, Type } from "class-transformer";

/**
 * Job configuration
 */
export class JobConfig extends SpeakeasyBase {
  /**
   * List of ad breaks. Specifies where to insert ad break tags in the output manifests.
   */
  @SpeakeasyMetadata({ elemType: AdBreak })
  @Expose({ name: "adBreaks" })
  @Type(() => AdBreak)
  adBreaks?: AdBreak[];

  /**
   * List of `Edit atom`s. Defines the ultimate timeline of the resulting file or manifest.
   */
  @SpeakeasyMetadata({ elemType: EditAtom })
  @Expose({ name: "editList" })
  @Type(() => EditAtom)
  editList?: EditAtom[];

  /**
   * List of elementary streams.
   */
  @SpeakeasyMetadata({ elemType: ElementaryStream })
  @Expose({ name: "elementaryStreams" })
  @Type(() => ElementaryStream)
  elementaryStreams?: ElementaryStream[];

  /**
   * List of input assets stored in Cloud Storage.
   */
  @SpeakeasyMetadata({ elemType: Input })
  @Expose({ name: "inputs" })
  @Type(() => Input)
  inputs?: Input[];

  /**
   * List of output manifests.
   */
  @SpeakeasyMetadata({ elemType: Manifest })
  @Expose({ name: "manifests" })
  @Type(() => Manifest)
  manifests?: Manifest[];

  /**
   * List of multiplexing settings for output streams.
   */
  @SpeakeasyMetadata({ elemType: MuxStream })
  @Expose({ name: "muxStreams" })
  @Type(() => MuxStream)
  muxStreams?: MuxStream[];

  /**
   * Location of output file(s) in a Cloud Storage bucket.
   */
  @SpeakeasyMetadata()
  @Expose({ name: "output" })
  @Type(() => Output)
  output?: Output;

  /**
   * List of overlays on the output video, in descending Z-order.
   */
  @SpeakeasyMetadata({ elemType: Overlay })
  @Expose({ name: "overlays" })
  @Type(() => Overlay)
  overlays?: Overlay[];

  /**
   * A Pub/Sub destination.
   */
  @SpeakeasyMetadata()
  @Expose({ name: "pubsubDestination" })
  @Type(() => PubsubDestination)
  pubsubDestination?: PubsubDestination;

  /**
   * List of output sprite sheets.
   */
  @SpeakeasyMetadata({ elemType: SpriteSheet })
  @Expose({ name: "spriteSheets" })
  @Type(() => SpriteSheet)
  spriteSheets?: SpriteSheet[];
}
