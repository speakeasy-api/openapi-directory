/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import * as utils from "../internal/utils";
import * as operations from "./models/operations";
import * as shared from "./models/shared";
import { AxiosInstance, AxiosRequestConfig, AxiosResponse } from "axios";

export class UsersFeed {
  _defaultClient: AxiosInstance;
  _securityClient: AxiosInstance;
  _serverURL: string;
  _language: string;
  _sdkVersion: string;
  _genVersion: string;

  constructor(
    defaultClient: AxiosInstance,
    securityClient: AxiosInstance,
    serverURL: string,
    language: string,
    sdkVersion: string,
    genVersion: string
  ) {
    this._defaultClient = defaultClient;
    this._securityClient = securityClient;
    this._serverURL = serverURL;
    this._language = language;
    this._sdkVersion = sdkVersion;
    this._genVersion = genVersion;
  }

  /**
   * Get all videos in a user's feed
   */
  getFeed(
    req: operations.GetFeedRequest,
    security: operations.GetFeedSecurity,
    config?: AxiosRequestConfig
  ): Promise<operations.GetFeedResponse> {
    if (!(req instanceof utils.SpeakeasyBase)) {
      req = new operations.GetFeedRequest(req);
    }

    const baseURL: string = this._serverURL;
    const url: string = utils.generateURL(
      baseURL,
      "/users/{user_id}/feed",
      req
    );
    if (!(security instanceof utils.SpeakeasyBase)) {
      security = new operations.GetFeedSecurity(security);
    }
    const client: AxiosInstance = utils.createSecurityClient(
      this._defaultClient,
      security
    );

    const queryParams: string = utils.serializeQueryParams(req);

    const r = client.request({
      url: url + queryParams,
      method: "get",
      ...config,
    });

    return r.then((httpRes: AxiosResponse) => {
      const contentType: string = httpRes?.headers?.["content-type"] ?? "";

      if (httpRes?.status == null)
        throw new Error(`status code not found in response: ${httpRes}`);
      const res: operations.GetFeedResponse = new operations.GetFeedResponse({
        statusCode: httpRes.status,
        contentType: contentType,
        rawResponse: httpRes,
      });
      switch (true) {
        case httpRes?.status == 200:
          if (
            utils.matchContentType(
              contentType,
              `application/vnd.vimeo.activity+json`
            )
          ) {
            res.activity31s = [];
            const resFieldDepth: number = utils.getResFieldDepth(res);
            res.activity31s = utils.objectToClass(
              httpRes?.data,
              shared.Activity31,
              resFieldDepth
            );
          }
          break;
      }

      return res;
    });
  }

  /**
   * Get all videos in a user's feed
   */
  getFeedAlt1(
    req: operations.GetFeedAlt1Request,
    security: operations.GetFeedAlt1Security,
    config?: AxiosRequestConfig
  ): Promise<operations.GetFeedAlt1Response> {
    if (!(req instanceof utils.SpeakeasyBase)) {
      req = new operations.GetFeedAlt1Request(req);
    }

    const baseURL: string = this._serverURL;
    const url: string = baseURL.replace(/\/$/, "") + "/me/feed";
    if (!(security instanceof utils.SpeakeasyBase)) {
      security = new operations.GetFeedAlt1Security(security);
    }
    const client: AxiosInstance = utils.createSecurityClient(
      this._defaultClient,
      security
    );

    const queryParams: string = utils.serializeQueryParams(req);

    const r = client.request({
      url: url + queryParams,
      method: "get",
      ...config,
    });

    return r.then((httpRes: AxiosResponse) => {
      const contentType: string = httpRes?.headers?.["content-type"] ?? "";

      if (httpRes?.status == null)
        throw new Error(`status code not found in response: ${httpRes}`);
      const res: operations.GetFeedAlt1Response =
        new operations.GetFeedAlt1Response({
          statusCode: httpRes.status,
          contentType: contentType,
          rawResponse: httpRes,
        });
      switch (true) {
        case httpRes?.status == 200:
          if (
            utils.matchContentType(
              contentType,
              `application/vnd.vimeo.activity+json`
            )
          ) {
            res.activity31s = [];
            const resFieldDepth: number = utils.getResFieldDepth(res);
            res.activity31s = utils.objectToClass(
              httpRes?.data,
              shared.Activity31,
              resFieldDepth
            );
          }
          break;
      }

      return res;
    });
  }
}
