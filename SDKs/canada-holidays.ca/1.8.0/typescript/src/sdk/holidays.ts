/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import * as utils from "../internal/utils";
import * as operations from "./models/operations";
import { AxiosInstance, AxiosRequestConfig, AxiosResponse } from "axios";

/**
 * Get holiday(s) with associated provinces
 */
export class Holidays {
  _defaultClient: AxiosInstance;
  _securityClient: AxiosInstance;
  _serverURL: string;
  _language: string;
  _sdkVersion: string;
  _genVersion: string;

  constructor(
    defaultClient: AxiosInstance,
    securityClient: AxiosInstance,
    serverURL: string,
    language: string,
    sdkVersion: string,
    genVersion: string
  ) {
    this._defaultClient = defaultClient;
    this._securityClient = securityClient;
    this._serverURL = serverURL;
    this._language = language;
    this._sdkVersion = sdkVersion;
    this._genVersion = genVersion;
  }

  /**
   * Get a holiday by id
   *
   * @remarks
   * Returns one Canadian statutory holiday by integer id. Returns a 404 response for invalid ids.
   */
  holiday(
    req: operations.HolidayRequest,
    config?: AxiosRequestConfig
  ): Promise<operations.HolidayResponse> {
    if (!(req instanceof utils.SpeakeasyBase)) {
      req = new operations.HolidayRequest(req);
    }

    const baseURL: string = this._serverURL;
    const url: string = utils.generateURL(
      baseURL,
      "/api/v1/holidays/{holidayId}",
      req
    );

    const client: AxiosInstance = this._defaultClient;

    const queryParams: string = utils.serializeQueryParams(req);

    const r = client.request({
      url: url + queryParams,
      method: "get",
      ...config,
    });

    return r.then((httpRes: AxiosResponse) => {
      const contentType: string = httpRes?.headers?.["content-type"] ?? "";

      if (httpRes?.status == null)
        throw new Error(`status code not found in response: ${httpRes}`);
      const res: operations.HolidayResponse = new operations.HolidayResponse({
        statusCode: httpRes.status,
        contentType: contentType,
        rawResponse: httpRes,
      });
      switch (true) {
        case httpRes?.status == 200:
          if (utils.matchContentType(contentType, `application/json`)) {
            res.holiday200ApplicationJSONObject = utils.objectToClass(
              httpRes?.data,
              operations.Holiday200ApplicationJSON
            );
          }
          break;
        case httpRes?.status == 400:
          if (utils.matchContentType(contentType, `application/json`)) {
            res.holiday400ApplicationJSONObject = utils.objectToClass(
              httpRes?.data,
              operations.Holiday400ApplicationJSON
            );
          }
          break;
      }

      return res;
    });
  }

  /**
   * Get all holidays
   *
   * @remarks
   * Returns Canadian public holidays. Each holiday lists the regions that observe it.
   */
  holidays(
    req: operations.HolidaysRequest,
    config?: AxiosRequestConfig
  ): Promise<operations.HolidaysResponse> {
    if (!(req instanceof utils.SpeakeasyBase)) {
      req = new operations.HolidaysRequest(req);
    }

    const baseURL: string = this._serverURL;
    const url: string = baseURL.replace(/\/$/, "") + "/api/v1/holidays";

    const client: AxiosInstance = this._defaultClient;

    const queryParams: string = utils.serializeQueryParams(req);

    const r = client.request({
      url: url + queryParams,
      method: "get",
      ...config,
    });

    return r.then((httpRes: AxiosResponse) => {
      const contentType: string = httpRes?.headers?.["content-type"] ?? "";

      if (httpRes?.status == null)
        throw new Error(`status code not found in response: ${httpRes}`);
      const res: operations.HolidaysResponse = new operations.HolidaysResponse({
        statusCode: httpRes.status,
        contentType: contentType,
        rawResponse: httpRes,
      });
      switch (true) {
        case httpRes?.status == 200:
          if (utils.matchContentType(contentType, `application/json`)) {
            res.holidays200ApplicationJSONObject = utils.objectToClass(
              httpRes?.data,
              operations.Holidays200ApplicationJSON
            );
          }
          break;
      }

      return res;
    });
  }
}
