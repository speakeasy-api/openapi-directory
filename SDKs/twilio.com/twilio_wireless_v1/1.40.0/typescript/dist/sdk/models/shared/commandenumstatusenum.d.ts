/**
 * The status of the resources to read. Can be: `queued`, `sent`, `delivered`, `received`, or `failed`.
 */
export declare enum CommandEnumStatusEnum {
    Queued = "queued",
    Sent = "sent",
    Delivered = "delivered",
    Received = "received",
    Failed = "failed"
}
