"""Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT."""

from __future__ import annotations
import dataclasses
import requests as requests_http
from ..shared import error401 as shared_error401
from ..shared import error500 as shared_error500
from ..shared import nhevent as shared_nhevent
from typing import Optional


@dataclasses.dataclass
class GetNhEventsRequest:
    
    accept_version: str = dataclasses.field(metadata={'header': { 'field_name': 'Accept-Version', 'style': 'simple', 'explode': False }})
    r"""The version of the API you are calling, written as `1.0.0`. This is specified as required as good practice, but it is not actually enforced by the API. If you do not specify a version, you will be served the latest version, which may eventually result in breaking changes."""  
    x_api_key: str = dataclasses.field(metadata={'header': { 'field_name': 'X-API-KEY', 'style': 'simple', 'explode': False }})
    r"""Your UUID secret key, granted to you by the Nookipedia team. Required for accessing the API."""  
    date_: Optional[str] = dataclasses.field(default=None, metadata={'query_param': { 'field_name': 'date', 'style': 'form', 'explode': True }})
    r"""Specify a specific date (in the current or next year) to retrieve events for. Accepts many date formats, such as `YYYY-MM-DD` or `Month Day, Year`, as well as `today` to retrieve the current day's events (UTC time)."""  
    day: Optional[int] = dataclasses.field(default=None, metadata={'query_param': { 'field_name': 'day', 'style': 'form', 'explode': True }})
    r"""Specify the day of the month to retrieve events for."""  
    month: Optional[str] = dataclasses.field(default=None, metadata={'query_param': { 'field_name': 'month', 'style': 'form', 'explode': True }})
    r"""Specify the month to retrieve events for (accepts multiple formats, such as `Oct`, `October`, or `10`). Most likely want to use alongside `year`, otherwise events in both the current and next year are returned."""  
    year: Optional[str] = dataclasses.field(default=None, metadata={'query_param': { 'field_name': 'year', 'style': 'form', 'explode': True }})
    r"""Specify the year to retrieve events for. Must be the current or next year."""  
    

@dataclasses.dataclass
class GetNhEventsResponse:
    
    content_type: str = dataclasses.field()  
    status_code: int = dataclasses.field()  
    error401: Optional[shared_error401.Error401] = dataclasses.field(default=None)
    r"""Failed to authenticate user from `X-API-KEY`."""  
    error500: Optional[shared_error500.Error500] = dataclasses.field(default=None)
    r"""There was an error fetching the requested data."""  
    nh_events: Optional[list[shared_nhevent.NHEvent]] = dataclasses.field(default=None)
    r"""A JSON array of events."""  
    raw_response: Optional[requests_http.Response] = dataclasses.field(default=None)  
    