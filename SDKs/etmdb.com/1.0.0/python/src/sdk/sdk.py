

import requests

from . import utils

from .cinema import Cinema
from .cinema_detail import CinemaDetail
from .cinema_schedule import CinemaSchedule
from .cinema_shedule_showtime import CinemaSheduleShowtime
from .company import Company
from .company_credits import CompanyCredits
from .filmography import Filmography
from .filmography_type import FilmographyType
from .genre import Genre
from .genre_type import GenreType
from .job import Job
from .media import Media
from .movie import Movie
from .movie_cast import MovieCast
from .news import News
from .people import People
from .showtime import Showtime
from .watchlist import Watchlist


SERVERS = [
	"https://etmdb.com",
]


class SDK:
    
    cinema: Cinema
    cinema_detail: CinemaDetail
    cinema_schedule: CinemaSchedule
    cinema_shedule_showtime: CinemaSheduleShowtime
    company: Company
    company_credits: CompanyCredits
    filmography: Filmography
    filmography_type: FilmographyType
    genre: Genre
    genre_type: GenreType
    job: Job
    media: Media
    movie: Movie
    movie_cast: MovieCast
    news: News
    people: People
    showtime: Showtime
    watchlist: Watchlist

    _client: requests.Session
    _security_client: requests.Session
    
    _server_url: str = SERVERS[0]
    _language: str = "python"
    _sdk_version: str = "0.0.1"
    _gen_version: str = "internal"

    def __init__(self) -> None:
        self._client = requests.Session()
        self._security_client = requests.Session()
        self._init_sdks()


    def config_server_url(self, server_url: str, params: dict[str, str]):
        if params is not None:
            self._server_url = utils.replace_parameters(server_url, params)
        else:
            self._server_url = server_url

        self._init_sdks()
    

    def config_client(self, client: requests.Session):
        self._client = client
        self._init_sdks()
    
    
    def _init_sdks(self):
        
        self.cinema = Cinema(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.cinema_detail = CinemaDetail(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.cinema_schedule = CinemaSchedule(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.cinema_shedule_showtime = CinemaSheduleShowtime(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.company = Company(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.company_credits = CompanyCredits(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.filmography = Filmography(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.filmography_type = FilmographyType(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.genre = Genre(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.genre_type = GenreType(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.job = Job(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.media = Media(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.movie = Movie(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.movie_cast = MovieCast(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.news = News(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.people = People(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.showtime = Showtime(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.watchlist = Watchlist(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
    
    