"""Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT."""

from __future__ import annotations
import dataclasses
import requests as requests_http
from typing import Any, Optional


@dataclasses.dataclass
class PostV2ActivitiesCallsJSONRequestBody:
    
    person_id: int = dataclasses.field(metadata={'form': { 'field_name': 'person_id' }})
    r"""The ID of the person whom this call will be logged for"""  
    action_id: Optional[int] = dataclasses.field(default=None, metadata={'form': { 'field_name': 'action_id' }})
    r"""Action that this call is being logged for. This will validate that the action is still
    valid before completing it. The same action can never be successfully passed twice to this endpoint. The
    action must have a type of 'phone'.
    """  
    crm_params: Optional[dict[str, Any]] = dataclasses.field(default=None, metadata={'form': { 'field_name': 'crm_params', 'json': True }})
    r"""CRM specific parameters. Some parameters are required on a per-team basis. Consume the
    CrmActivityFields endpoint to receive a list of valid parameters. The \"field\" property
    is passed as the key of this object, and the value of this object is the value that you
    would like to set.
    
    If CrmActivityField has a non-null value, then that value must be submitted, or excluded
    from API calls, as these values are automatically applied.
    """  
    disposition: Optional[str] = dataclasses.field(default=None, metadata={'form': { 'field_name': 'disposition' }})
    r"""The disposition of the call. Can be required on a per-team basis. Must be present in the disposition list."""  
    duration: Optional[int] = dataclasses.field(default=None, metadata={'form': { 'field_name': 'duration' }})
    r"""The length of the call, in seconds"""  
    linked_call_data_record_ids: Optional[list[int]] = dataclasses.field(default=None, metadata={'form': { 'field_name': 'linked_call_data_record_ids' }})
    r"""CallDataRecord associations that will become linked to the created call. It is possible to pass
    multiple CallDataRecord ids in this field; this can be used to represent multiple phone calls
    that made up a single call.
    
    Any call data record that is used must not already be linked to a call. It is not possible
    to link a call data record to multiple calls, and it is not possible to re-assign a call data
    record to a different call.
    """  
    notes: Optional[str] = dataclasses.field(default=None, metadata={'form': { 'field_name': 'notes' }})
    r"""Notes to log for the call. This is similar to the notes endpoint, but ensures that the notes get synced to the user's CRM"""  
    sentiment: Optional[str] = dataclasses.field(default=None, metadata={'form': { 'field_name': 'sentiment' }})
    r"""The sentiment of the call. Can be required on a per-team basis. Must be present in the sentiment list."""  
    to: Optional[str] = dataclasses.field(default=None, metadata={'form': { 'field_name': 'to' }})
    r"""The phone number that was called"""  
    user_guid: Optional[str] = dataclasses.field(default=None, metadata={'form': { 'field_name': 'user_guid' }})
    r"""Guid of the user whom this call should be logged for. Defaults to the authenticated user. Only team admins can pass another user's guid"""  
    

@dataclasses.dataclass
class PostV2ActivitiesCallsJSONResponse:
    
    content_type: str = dataclasses.field()  
    status_code: int = dataclasses.field()  
    body: Optional[bytes] = dataclasses.field(default=None)  
    raw_response: Optional[requests_http.Response] = dataclasses.field(default=None)  
    