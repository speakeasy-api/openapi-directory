/**
 * Filter used to query Verification Attempts by communication channel. Valid values are `SMS` and `CALL`
 */
export declare enum VerificationAttemptEnumChannelsEnum {
    Sms = "sms",
    Call = "call",
    Email = "email",
    Whatsapp = "whatsapp"
}
