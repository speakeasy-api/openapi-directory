/**
 * Search only for sms with the corresponding status.
 */
export declare enum SmsStatusEnum {
    Delivered = "delivered",
    Expired = "expired",
    Failed = "failed",
    Rejected = "rejected",
    Accepted = "accepted",
    Buffered = "buffered",
    Unknown = "unknown"
}
