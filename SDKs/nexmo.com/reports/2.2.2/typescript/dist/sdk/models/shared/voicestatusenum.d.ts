/**
 * Search only for voice call with the corresponding status.
 */
export declare enum VoiceStatusEnum {
    Answered = "answered",
    Machine = "machine",
    Error = "error",
    Failed = "failed"
}
