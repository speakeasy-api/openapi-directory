/**
 * Status to filter by, with possible values `created`, `started`, `ended`, or `failed`.
 */
export declare enum PlayerStreamerEnumStatusEnum {
    Created = "created",
    Started = "started",
    Ended = "ended",
    Failed = "failed"
}
