/**
 * A status enum indicating whether a screening is still pending review, has been rejected, or has been cleared.
 */
export declare enum WatchlistScreeningStatusEnum {
    Rejected = "rejected",
    PendingReview = "pending_review",
    Cleared = "cleared"
}
