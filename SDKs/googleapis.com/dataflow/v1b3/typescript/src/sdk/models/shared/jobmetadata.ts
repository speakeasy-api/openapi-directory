/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import { SpeakeasyBase, SpeakeasyMetadata } from "../../../internal/utils";
import { BigQueryIODetails } from "./bigqueryiodetails";
import { BigTableIODetails } from "./bigtableiodetails";
import { DatastoreIODetails } from "./datastoreiodetails";
import { FileIODetails } from "./fileiodetails";
import { PubSubIODetails } from "./pubsubiodetails";
import { SDKVersion } from "./sdkversion";
import { SpannerIODetails } from "./spanneriodetails";
import { Expose, Type } from "class-transformer";

/**
 * Metadata available primarily for filtering jobs. Will be included in the ListJob response and Job SUMMARY view.
 */
export class JobMetadata extends SpeakeasyBase {
  /**
   * Identification of a Cloud Bigtable source used in the Dataflow job.
   */
  @SpeakeasyMetadata({ elemType: BigTableIODetails })
  @Expose({ name: "bigTableDetails" })
  @Type(() => BigTableIODetails)
  bigTableDetails?: BigTableIODetails[];

  /**
   * Identification of a BigQuery source used in the Dataflow job.
   */
  @SpeakeasyMetadata({ elemType: BigQueryIODetails })
  @Expose({ name: "bigqueryDetails" })
  @Type(() => BigQueryIODetails)
  bigqueryDetails?: BigQueryIODetails[];

  /**
   * Identification of a Datastore source used in the Dataflow job.
   */
  @SpeakeasyMetadata({ elemType: DatastoreIODetails })
  @Expose({ name: "datastoreDetails" })
  @Type(() => DatastoreIODetails)
  datastoreDetails?: DatastoreIODetails[];

  /**
   * Identification of a File source used in the Dataflow job.
   */
  @SpeakeasyMetadata({ elemType: FileIODetails })
  @Expose({ name: "fileDetails" })
  @Type(() => FileIODetails)
  fileDetails?: FileIODetails[];

  /**
   * Identification of a Pub/Sub source used in the Dataflow job.
   */
  @SpeakeasyMetadata({ elemType: PubSubIODetails })
  @Expose({ name: "pubsubDetails" })
  @Type(() => PubSubIODetails)
  pubsubDetails?: PubSubIODetails[];

  /**
   * The version of the SDK used to run the job.
   */
  @SpeakeasyMetadata()
  @Expose({ name: "sdkVersion" })
  @Type(() => SDKVersion)
  sdkVersion?: SDKVersion;

  /**
   * Identification of a Spanner source used in the Dataflow job.
   */
  @SpeakeasyMetadata({ elemType: SpannerIODetails })
  @Expose({ name: "spannerDetails" })
  @Type(() => SpannerIODetails)
  spannerDetails?: SpannerIODetails[];

  /**
   * List of display properties to help UI filter jobs.
   */
  @SpeakeasyMetadata()
  @Expose({ name: "userDisplayProperties" })
  userDisplayProperties?: Record<string, string>;
}
