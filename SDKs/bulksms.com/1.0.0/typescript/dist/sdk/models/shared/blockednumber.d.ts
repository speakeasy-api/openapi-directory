import { SpeakeasyBase } from "../../../internal/utils";
/**
 * A list of BlockedNumber objects
 */
export declare class BlockedNumber extends SpeakeasyBase {
    /**
     * A unique identifier that is assigned when the BlockedNumber is created.
     */
    id: number;
    phoneNumber: string;
}
