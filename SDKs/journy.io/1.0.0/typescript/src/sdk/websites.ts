/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import * as utils from "../internal/utils";
import * as operations from "./models/operations";
import { AxiosInstance, AxiosRequestConfig, AxiosResponse } from "axios";

/**
 * Endpoints for managing websites.
 */
export class Websites {
  _defaultClient: AxiosInstance;
  _securityClient: AxiosInstance;
  _serverURL: string;
  _language: string;
  _sdkVersion: string;
  _genVersion: string;

  constructor(
    defaultClient: AxiosInstance,
    securityClient: AxiosInstance,
    serverURL: string,
    language: string,
    sdkVersion: string,
    genVersion: string
  ) {
    this._defaultClient = defaultClient;
    this._securityClient = securityClient;
    this._serverURL = serverURL;
    this._language = language;
    this._sdkVersion = sdkVersion;
    this._genVersion = genVersion;
  }

  /**
   * Get snippet for a website
   *
   * @remarks
   * Endpoint used to get a snippet for a website.
   */
  getTrackingSnippet(
    req: operations.GetTrackingSnippetRequest,
    config?: AxiosRequestConfig
  ): Promise<operations.GetTrackingSnippetResponse> {
    if (!(req instanceof utils.SpeakeasyBase)) {
      req = new operations.GetTrackingSnippetRequest(req);
    }

    const baseURL: string = this._serverURL;
    const url: string = baseURL.replace(/\/$/, "") + "/tracking/snippet";

    const client: AxiosInstance = this._defaultClient;

    const queryParams: string = utils.serializeQueryParams(req);

    const r = client.request({
      url: url + queryParams,
      method: "get",
      ...config,
    });

    return r.then((httpRes: AxiosResponse) => {
      const contentType: string = httpRes?.headers?.["content-type"] ?? "";

      if (httpRes?.status == null)
        throw new Error(`status code not found in response: ${httpRes}`);
      const res: operations.GetTrackingSnippetResponse =
        new operations.GetTrackingSnippetResponse({
          statusCode: httpRes.status,
          contentType: contentType,
          rawResponse: httpRes,
          headers: utils.getHeadersFromResponse(httpRes.headers),
        });
      switch (true) {
        case httpRes?.status == 200:
          if (utils.matchContentType(contentType, `application/json`)) {
            res.getTrackingSnippet200ApplicationJSONObject =
              utils.objectToClass(
                httpRes?.data,
                operations.GetTrackingSnippet200ApplicationJSON
              );
          }
          break;
        case httpRes?.status == 400:
          if (utils.matchContentType(contentType, `application/json`)) {
            res.getTrackingSnippet400ApplicationJSONObject =
              utils.objectToClass(
                httpRes?.data,
                operations.GetTrackingSnippet400ApplicationJSON
              );
          }
          break;
        case httpRes?.status == 401:
          if (utils.matchContentType(contentType, `application/json`)) {
            res.getTrackingSnippet401ApplicationJSONObject =
              utils.objectToClass(
                httpRes?.data,
                operations.GetTrackingSnippet401ApplicationJSON
              );
          }
          break;
        case httpRes?.status == 403:
          if (utils.matchContentType(contentType, `application/json`)) {
            res.getTrackingSnippet403ApplicationJSONObject =
              utils.objectToClass(
                httpRes?.data,
                operations.GetTrackingSnippet403ApplicationJSON
              );
          }
          break;
        case httpRes?.status == 404:
          if (utils.matchContentType(contentType, `application/json`)) {
            res.getTrackingSnippet404ApplicationJSONObject =
              utils.objectToClass(
                httpRes?.data,
                operations.GetTrackingSnippet404ApplicationJSON
              );
          }
          break;
        case httpRes?.status == 429:
          if (utils.matchContentType(contentType, `application/json`)) {
            res.getTrackingSnippet429ApplicationJSONObject =
              utils.objectToClass(
                httpRes?.data,
                operations.GetTrackingSnippet429ApplicationJSON
              );
          }
          break;
        case httpRes?.status == 500:
          if (utils.matchContentType(contentType, `application/json`)) {
            res.getTrackingSnippet500ApplicationJSONObject =
              utils.objectToClass(
                httpRes?.data,
                operations.GetTrackingSnippet500ApplicationJSON
              );
          }
          break;
      }

      return res;
    });
  }
}
