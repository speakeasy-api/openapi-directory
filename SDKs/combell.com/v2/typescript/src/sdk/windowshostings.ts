/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import * as utils from "../internal/utils";
import * as operations from "./models/operations";
import * as shared from "./models/shared";
import { AxiosInstance, AxiosRequestConfig, AxiosResponse } from "axios";

/**
 * Manage your windows hostings.
 */
export class WindowsHostings {
  _defaultClient: AxiosInstance;
  _securityClient: AxiosInstance;
  _serverURL: string;
  _language: string;
  _sdkVersion: string;
  _genVersion: string;

  constructor(
    defaultClient: AxiosInstance,
    securityClient: AxiosInstance,
    serverURL: string,
    language: string,
    sdkVersion: string,
    genVersion: string
  ) {
    this._defaultClient = defaultClient;
    this._securityClient = securityClient;
    this._serverURL = serverURL;
    this._language = language;
    this._sdkVersion = sdkVersion;
    this._genVersion = genVersion;
  }

  /**
   * Windows hosting detail
   */
  getWindowsHosting(
    req: operations.GetWindowsHostingRequest,
    config?: AxiosRequestConfig
  ): Promise<operations.GetWindowsHostingResponse> {
    if (!(req instanceof utils.SpeakeasyBase)) {
      req = new operations.GetWindowsHostingRequest(req);
    }

    const baseURL: string = this._serverURL;
    const url: string = utils.generateURL(
      baseURL,
      "/windowshostings/{domainName}",
      req
    );

    const client: AxiosInstance = this._defaultClient;

    const queryParams: string = utils.serializeQueryParams(req);

    const r = client.request({
      url: url + queryParams,
      method: "get",
      ...config,
    });

    return r.then((httpRes: AxiosResponse) => {
      const contentType: string = httpRes?.headers?.["content-type"] ?? "";

      if (httpRes?.status == null)
        throw new Error(`status code not found in response: ${httpRes}`);
      const res: operations.GetWindowsHostingResponse =
        new operations.GetWindowsHostingResponse({
          statusCode: httpRes.status,
          contentType: contentType,
          rawResponse: httpRes,
        });
      switch (true) {
        case httpRes?.status == 200:
          if (utils.matchContentType(contentType, `application/json`)) {
            res.windowsHostingDetail = utils.objectToClass(
              httpRes?.data,
              shared.WindowsHostingDetail
            );
          }
          break;
      }

      return res;
    });
  }

  /**
   * Overview of windows hostings
   */
  getWindowsHostings(
    req: operations.GetWindowsHostingsRequest,
    config?: AxiosRequestConfig
  ): Promise<operations.GetWindowsHostingsResponse> {
    if (!(req instanceof utils.SpeakeasyBase)) {
      req = new operations.GetWindowsHostingsRequest(req);
    }

    const baseURL: string = this._serverURL;
    const url: string = baseURL.replace(/\/$/, "") + "/windowshostings";

    const client: AxiosInstance = this._defaultClient;

    const queryParams: string = utils.serializeQueryParams(req);

    const r = client.request({
      url: url + queryParams,
      method: "get",
      ...config,
    });

    return r.then((httpRes: AxiosResponse) => {
      const contentType: string = httpRes?.headers?.["content-type"] ?? "";

      if (httpRes?.status == null)
        throw new Error(`status code not found in response: ${httpRes}`);
      const res: operations.GetWindowsHostingsResponse =
        new operations.GetWindowsHostingsResponse({
          statusCode: httpRes.status,
          contentType: contentType,
          rawResponse: httpRes,
          headers: utils.getHeadersFromResponse(httpRes.headers),
        });
      switch (true) {
        case httpRes?.status == 200:
          if (utils.matchContentType(contentType, `application/json`)) {
            res.windowsHostings = [];
            const resFieldDepth: number = utils.getResFieldDepth(res);
            res.windowsHostings = utils.objectToClass(
              httpRes?.data,
              shared.WindowsHosting,
              resFieldDepth
            );
          }
          break;
      }

      return res;
    });
  }
}
