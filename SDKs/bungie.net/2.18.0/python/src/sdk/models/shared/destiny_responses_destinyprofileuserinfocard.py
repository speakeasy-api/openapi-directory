"""Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT."""

from __future__ import annotations
import dataclasses
from ..shared import destiny_entities_items_destinyitemcomponent as shared_destiny_entities_items_destinyitemcomponent
from datetime import datetime
from enum import Enum
from typing import Optional


@dataclasses.dataclass
class DestinyResponsesDestinyProfileUserInfoCardPlatformSilver:
    r"""This is the silver available on this Profile across any platforms on which they have purchased silver.
     This is only available if you are requesting yourself.
    """
    
    platform_silver: Optional[dict[str, shared_destiny_entities_items_destinyitemcomponent.DestinyEntitiesItemsDestinyItemComponent]] = dataclasses.field(default=None)
    r"""If a Profile is played on multiple platforms, this is the silver they have for each platform, keyed by Membership Type."""  
    
class DestinyResponsesDestinyProfileUserInfoCardUnpairedGameVersionsEnum(str, Enum):
    r"""If this profile is not in a cross save pairing, this will return the game versions that we believe this profile has access to.
     For the time being, we will not return this information for any membership that is in a cross save pairing. The gist is that, once the pairing occurs, we do not currently have a consistent way to get that information for the profile's original Platform, and thus gameVersions would be too inconsistent (based on the last platform they happened to play on) for the info to be useful.
     If we ever can get this data, this field will be deprecated and replaced with data on the DestinyLinkedProfileResponse itself, with game versions per linked Platform. But since we can't get that, we have this as a stop-gap measure for getting the data in the only situation that we currently need it.
    """
    ZERO = '0'
    ONE = '1'
    TWO = '2'
    FOUR = '4'
    EIGHT = '8'
    SIXTEEN = '16'
    THIRTY_TWO = '32'
    SIXTY_FOUR = '64'
    ONE_HUNDRED_AND_TWENTY_EIGHT = '128'
    TWO_HUNDRED_AND_FIFTY_SIX = '256'
    FIVE_HUNDRED_AND_TWELVE = '512'


@dataclasses.dataclass
class DestinyResponsesDestinyProfileUserInfoCard:
    
    applicable_membership_types: Optional[list[int]] = dataclasses.field(default=None)
    r"""The list of Membership Types indicating the platforms on which this Membership can be used.
     Not in Cross Save = its original membership type. Cross Save Primary = Any membership types it is overridding, and its original membership type Cross Save Overridden = Empty list
    """  
    bungie_global_display_name: Optional[str] = dataclasses.field(default=None)
    r"""The bungie global display name, if set."""  
    bungie_global_display_name_code: Optional[int] = dataclasses.field(default=None)
    r"""The bungie global display name code, if set."""  
    cross_save_override: Optional[int] = dataclasses.field(default=None)
    r"""If there is a cross save override in effect, this value will tell you the type that is overridding this one."""  
    date_last_played: Optional[datetime] = dataclasses.field(default=None)  
    display_name: Optional[str] = dataclasses.field(default=None)
    r"""Display Name the player has chosen for themselves. The display name is optional when the data type is used as input to a platform API."""  
    icon_path: Optional[str] = dataclasses.field(default=None)
    r"""URL the Icon if available."""  
    is_cross_save_primary: Optional[bool] = dataclasses.field(default=None)
    r"""If true, this account is hooked up as the \\"Primary\\" cross save account for one or more platforms."""  
    is_overridden: Optional[bool] = dataclasses.field(default=None)
    r"""If this profile is being overridden/obscured by Cross Save, this will be set to true. We will still return the profile for display purposes where users need to know the info: it is up to any given area of the app/site to determine if this profile should still be shown."""  
    is_public: Optional[bool] = dataclasses.field(default=None)
    r"""If True, this is a public user membership."""  
    membership_id: Optional[int] = dataclasses.field(default=None)
    r"""Membership ID as they user is known in the Accounts service"""  
    membership_type: Optional[int] = dataclasses.field(default=None)
    r"""Type of the membership. Not necessarily the native type."""  
    platform_silver: Optional[DestinyResponsesDestinyProfileUserInfoCardPlatformSilver] = dataclasses.field(default=None)
    r"""This is the silver available on this Profile across any platforms on which they have purchased silver.
     This is only available if you are requesting yourself.
    """  
    supplemental_display_name: Optional[str] = dataclasses.field(default=None)
    r"""A platform specific additional display name - ex: psn Real Name, bnet Unique Name, etc."""  
    unpaired_game_versions: Optional[DestinyResponsesDestinyProfileUserInfoCardUnpairedGameVersionsEnum] = dataclasses.field(default=None)
    r"""If this profile is not in a cross save pairing, this will return the game versions that we believe this profile has access to.
     For the time being, we will not return this information for any membership that is in a cross save pairing. The gist is that, once the pairing occurs, we do not currently have a consistent way to get that information for the profile's original Platform, and thus gameVersions would be too inconsistent (based on the last platform they happened to play on) for the info to be useful.
     If we ever can get this data, this field will be deprecated and replaced with data on the DestinyLinkedProfileResponse itself, with game versions per linked Platform. But since we can't get that, we have this as a stop-gap measure for getting the data in the only situation that we currently need it.
    """  
    