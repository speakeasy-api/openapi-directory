/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import * as utils from "../internal/utils";
import * as operations from "./models/operations";
import * as shared from "./models/shared";
import { AxiosInstance, AxiosRequestConfig, AxiosResponse } from "axios";

export class CompanyController {
  _defaultClient: AxiosInstance;
  _securityClient: AxiosInstance;
  _serverURL: string;
  _language: string;
  _sdkVersion: string;
  _genVersion: string;

  constructor(
    defaultClient: AxiosInstance,
    securityClient: AxiosInstance,
    serverURL: string,
    language: string,
    sdkVersion: string,
    genVersion: string
  ) {
    this._defaultClient = defaultClient;
    this._securityClient = securityClient;
    this._serverURL = serverURL;
    this._language = language;
    this._sdkVersion = sdkVersion;
    this._genVersion = genVersion;
  }

  /**
   * All branches defined for a company
   */
  companyControllerGetBranches(
    req: operations.CompanyControllerGetBranchesRequest,
    config?: AxiosRequestConfig
  ): Promise<operations.CompanyControllerGetBranchesResponse> {
    if (!(req instanceof utils.SpeakeasyBase)) {
      req = new operations.CompanyControllerGetBranchesRequest(req);
    }

    const baseURL: string = this._serverURL;
    const url: string = utils.generateURL(
      baseURL,
      "/v3/diary/{shortName}/company/branches",
      req
    );

    const client: AxiosInstance = this._defaultClient;

    const queryParams: string = utils.serializeQueryParams(req);

    const r = client.request({
      url: url + queryParams,
      method: "get",
      ...config,
    });

    return r.then((httpRes: AxiosResponse) => {
      const contentType: string = httpRes?.headers?.["content-type"] ?? "";

      if (httpRes?.status == null)
        throw new Error(`status code not found in response: ${httpRes}`);
      const res: operations.CompanyControllerGetBranchesResponse =
        new operations.CompanyControllerGetBranchesResponse({
          statusCode: httpRes.status,
          contentType: contentType,
          rawResponse: httpRes,
        });
      switch (true) {
        case httpRes?.status == 200:
          if (utils.matchContentType(contentType, `application/json`)) {
            res.advertisingBranchModelResults = utils.objectToClass(
              httpRes?.data,
              shared.AdvertisingBranchModelResults
            );
          }
          if (utils.matchContentType(contentType, `text/json`)) {
            res.advertisingBranchModelResults = utils.objectToClass(
              httpRes?.data,
              shared.AdvertisingBranchModelResults
            );
          }
          if (utils.matchContentType(contentType, `application/xml`)) {
            const resBody: string = JSON.stringify(httpRes?.data, null, 0);
            const out: Uint8Array = new Uint8Array(resBody.length);
            for (let i = 0; i < resBody.length; i++)
              out[i] = resBody.charCodeAt(i);
            res.body = out;
          }
          if (utils.matchContentType(contentType, `text/xml`)) {
            const resBody: string = JSON.stringify(httpRes?.data, null, 0);
            const out: Uint8Array = new Uint8Array(resBody.length);
            for (let i = 0; i < resBody.length; i++)
              out[i] = resBody.charCodeAt(i);
            res.body = out;
          }
          break;
      }

      return res;
    });
  }

  /**
   * Get a specific branch given its unique Object ID (OID)
   */
  getV3DiaryShortNameCompanyBranchesBranchID(
    req: operations.GetV3DiaryShortNameCompanyBranchesBranchIDRequest,
    config?: AxiosRequestConfig
  ): Promise<operations.GetV3DiaryShortNameCompanyBranchesBranchIDResponse> {
    if (!(req instanceof utils.SpeakeasyBase)) {
      req = new operations.GetV3DiaryShortNameCompanyBranchesBranchIDRequest(
        req
      );
    }

    const baseURL: string = this._serverURL;
    const url: string = utils.generateURL(
      baseURL,
      "/v3/diary/{shortName}/company/branches/{branchID}",
      req
    );

    const client: AxiosInstance = this._defaultClient;

    const r = client.request({
      url: url,
      method: "get",
      ...config,
    });

    return r.then((httpRes: AxiosResponse) => {
      const contentType: string = httpRes?.headers?.["content-type"] ?? "";

      if (httpRes?.status == null)
        throw new Error(`status code not found in response: ${httpRes}`);
      const res: operations.GetV3DiaryShortNameCompanyBranchesBranchIDResponse =
        new operations.GetV3DiaryShortNameCompanyBranchesBranchIDResponse({
          statusCode: httpRes.status,
          contentType: contentType,
          rawResponse: httpRes,
        });
      switch (true) {
        case httpRes?.status == 200:
          if (utils.matchContentType(contentType, `application/json`)) {
            res.advertisingBranchModel = utils.objectToClass(
              httpRes?.data,
              shared.AdvertisingBranchModel
            );
          }
          if (utils.matchContentType(contentType, `text/json`)) {
            res.advertisingBranchModel = utils.objectToClass(
              httpRes?.data,
              shared.AdvertisingBranchModel
            );
          }
          if (utils.matchContentType(contentType, `application/xml`)) {
            const resBody: string = JSON.stringify(httpRes?.data, null, 0);
            const out: Uint8Array = new Uint8Array(resBody.length);
            for (let i = 0; i < resBody.length; i++)
              out[i] = resBody.charCodeAt(i);
            res.body = out;
          }
          if (utils.matchContentType(contentType, `text/xml`)) {
            const resBody: string = JSON.stringify(httpRes?.data, null, 0);
            const out: Uint8Array = new Uint8Array(resBody.length);
            for (let i = 0; i < resBody.length; i++)
              out[i] = resBody.charCodeAt(i);
            res.body = out;
          }
          break;
      }

      return res;
    });
  }
}
