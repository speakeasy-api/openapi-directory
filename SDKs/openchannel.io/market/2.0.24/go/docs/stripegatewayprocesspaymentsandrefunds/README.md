# StripeGatewayProcessPaymentsAndRefunds

### Available Operations

* [DeleteStripeGatewayDeveloperDeveloperIDAccountsStripeID](#deletestripegatewaydeveloperdeveloperidaccountsstripeid) - Disconnects a developer's Stripe account
* [DeleteStripeGatewayUserUserIDCardsCardID](#deletestripegatewayuseruseridcardscardid) - Removes a credit card for a user
* [GetStripeGatewayDeveloperDeveloperIDAccounts](#getstripegatewaydeveloperdeveloperidaccounts) - Returns a developers connected Stripe accounts
* [GetStripeGatewayUserUserIDCards](#getstripegatewayuseruseridcards) - Returns credit cards for this user
* [PostStripeGatewayDeveloperDeveloperIDAccounts](#poststripegatewaydeveloperdeveloperidaccounts) - Generate a temporary URL to allow a developer to connect their Stripe account
* [PostStripeGatewayUserUserIDCards](#poststripegatewayuseruseridcards) - Adds credit card for this user
* [PostStripeGatewayUserUserIDCardsCardID](#poststripegatewayuseruseridcardscardid) - Updates a credit card for this user

## DeleteStripeGatewayDeveloperDeveloperIDAccountsStripeID


- Results are returned for the market provided within the basic authentication credentials


### Example Usage

```go
package main

import(
	"context"
	"log"
	"openapi"
	"openapi/pkg/models/operations"
)

func main() {
    s := sdk.New(
        sdk.WithSecurity(shared.Security{
            Password: "YOUR_PASSWORD_HERE",
            Username: "YOUR_USERNAME_HERE",
        }),
    )

    ctx := context.Background()
    res, err := s.StripeGatewayProcessPaymentsAndRefunds.DeleteStripeGatewayDeveloperDeveloperIDAccountsStripeID(ctx, operations.DeleteStripeGatewayDeveloperDeveloperIDAccountsStripeIDRequest{
        DeveloperID: "suscipit",
        StripeID: "natus",
    })
    if err != nil {
        log.Fatal(err)
    }

    if res.StatusCode == http.StatusOK {
        // handle response
    }
}
```

## DeleteStripeGatewayUserUserIDCardsCardID

Removes a credit card for a user

### Example Usage

```go
package main

import(
	"context"
	"log"
	"openapi"
	"openapi/pkg/models/operations"
)

func main() {
    s := sdk.New(
        sdk.WithSecurity(shared.Security{
            Password: "YOUR_PASSWORD_HERE",
            Username: "YOUR_USERNAME_HERE",
        }),
    )

    ctx := context.Background()
    res, err := s.StripeGatewayProcessPaymentsAndRefunds.DeleteStripeGatewayUserUserIDCardsCardID(ctx, operations.DeleteStripeGatewayUserUserIDCardsCardIDRequest{
        CardID: "nobis",
        UserID: "eum",
    })
    if err != nil {
        log.Fatal(err)
    }

    if res.StatusCode == http.StatusOK {
        // handle response
    }
}
```

## GetStripeGatewayDeveloperDeveloperIDAccounts

- Results are returned for the market provided within the basic authentication credentials


### Example Usage

```go
package main

import(
	"context"
	"log"
	"openapi"
	"openapi/pkg/models/operations"
)

func main() {
    s := sdk.New(
        sdk.WithSecurity(shared.Security{
            Password: "YOUR_PASSWORD_HERE",
            Username: "YOUR_USERNAME_HERE",
        }),
    )

    ctx := context.Background()
    res, err := s.StripeGatewayProcessPaymentsAndRefunds.GetStripeGatewayDeveloperDeveloperIDAccounts(ctx, operations.GetStripeGatewayDeveloperDeveloperIDAccountsRequest{
        DeveloperID: "vero",
    })
    if err != nil {
        log.Fatal(err)
    }

    if res.StatusCode == http.StatusOK {
        // handle response
    }
}
```

## GetStripeGatewayUserUserIDCards

- Results are returned for the market provided within the basic authentication credentials


### Example Usage

```go
package main

import(
	"context"
	"log"
	"openapi"
	"openapi/pkg/models/operations"
)

func main() {
    s := sdk.New(
        sdk.WithSecurity(shared.Security{
            Password: "YOUR_PASSWORD_HERE",
            Username: "YOUR_USERNAME_HERE",
        }),
    )

    ctx := context.Background()
    res, err := s.StripeGatewayProcessPaymentsAndRefunds.GetStripeGatewayUserUserIDCards(ctx, operations.GetStripeGatewayUserUserIDCardsRequest{
        UserID: "aspernatur",
    })
    if err != nil {
        log.Fatal(err)
    }

    if res.StatusCode == http.StatusOK {
        // handle response
    }
}
```

## PostStripeGatewayDeveloperDeveloperIDAccounts

- Results are returned for the market provided within the basic authentication credentials 
- The URL generated by this method is only valid for 48 hours.


### Example Usage

```go
package main

import(
	"context"
	"log"
	"openapi"
	"openapi/pkg/models/operations"
)

func main() {
    s := sdk.New(
        sdk.WithSecurity(shared.Security{
            Password: "YOUR_PASSWORD_HERE",
            Username: "YOUR_USERNAME_HERE",
        }),
    )

    ctx := context.Background()
    res, err := s.StripeGatewayProcessPaymentsAndRefunds.PostStripeGatewayDeveloperDeveloperIDAccounts(ctx, operations.PostStripeGatewayDeveloperDeveloperIDAccountsRequest{
        DeveloperID: "architecto",
        RedirectURL: "magnam",
    })
    if err != nil {
        log.Fatal(err)
    }

    if res.StatusCode == http.StatusOK {
        // handle response
    }
}
```

## PostStripeGatewayUserUserIDCards

- Results are returned for the market provided within the basic authentication credentials


### Example Usage

```go
package main

import(
	"context"
	"log"
	"openapi"
	"openapi/pkg/models/operations"
)

func main() {
    s := sdk.New(
        sdk.WithSecurity(shared.Security{
            Password: "YOUR_PASSWORD_HERE",
            Username: "YOUR_USERNAME_HERE",
        }),
    )

    ctx := context.Background()
    res, err := s.StripeGatewayProcessPaymentsAndRefunds.PostStripeGatewayUserUserIDCards(ctx, operations.PostStripeGatewayUserUserIDCardsRequest{
        IsDefault: sdk.Bool(false),
        Token: "et",
        UserID: "excepturi",
    })
    if err != nil {
        log.Fatal(err)
    }

    if res.StatusCode == http.StatusOK {
        // handle response
    }
}
```

## PostStripeGatewayUserUserIDCardsCardID


- Results are returned for the market provided within the basic authentication credentials


### Example Usage

```go
package main

import(
	"context"
	"log"
	"openapi"
	"openapi/pkg/models/operations"
)

func main() {
    s := sdk.New(
        sdk.WithSecurity(shared.Security{
            Password: "YOUR_PASSWORD_HERE",
            Username: "YOUR_USERNAME_HERE",
        }),
    )

    ctx := context.Background()
    res, err := s.StripeGatewayProcessPaymentsAndRefunds.PostStripeGatewayUserUserIDCardsCardID(ctx, operations.PostStripeGatewayUserUserIDCardsCardIDRequest{
        AddressCity: sdk.String("ullam"),
        AddressCountry: sdk.String("provident"),
        AddressLine1: sdk.String("quos"),
        AddressLine2: sdk.String("sint"),
        AddressState: sdk.String("accusantium"),
        AddressZip: sdk.String("mollitia"),
        CardID: "reiciendis",
        IsDefault: sdk.Bool(false),
        UserID: "mollitia",
    })
    if err != nil {
        log.Fatal(err)
    }

    if res.StatusCode == http.StatusOK {
        // handle response
    }
}
```
