/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import * as utils from "../internal/utils";
import * as operations from "./models/operations";
import * as shared from "./models/shared";
import { AxiosInstance, AxiosRequestConfig, AxiosResponse } from "axios";

export class PicoLoadmanagementGroup {
  _defaultClient: AxiosInstance;
  _securityClient: AxiosInstance;
  _serverURL: string;
  _language: string;
  _sdkVersion: string;
  _genVersion: string;

  constructor(
    defaultClient: AxiosInstance,
    securityClient: AxiosInstance,
    serverURL: string,
    language: string,
    sdkVersion: string,
    genVersion: string
  ) {
    this._defaultClient = defaultClient;
    this._securityClient = securityClient;
    this._serverURL = serverURL;
    this._language = language;
    this._sdkVersion = sdkVersion;
    this._genVersion = genVersion;
  }

  /**
   * GET: api/pico/loadmanagementgroup
   *
   *             Returns a pico load management group by it's id
   */
  picoLoadmanagementGroupGet(
    req: operations.PicoLoadmanagementGroupGetRequest,
    config?: AxiosRequestConfig
  ): Promise<operations.PicoLoadmanagementGroupGetResponse> {
    if (!(req instanceof utils.SpeakeasyBase)) {
      req = new operations.PicoLoadmanagementGroupGetRequest(req);
    }

    const baseURL: string = this._serverURL;
    const url: string = utils.generateURL(
      baseURL,
      "/api/pico/loadmanagementgroup/{id}",
      req
    );

    const client: AxiosInstance = this._defaultClient;

    const r = client.request({
      url: url,
      method: "get",
      ...config,
    });

    return r.then((httpRes: AxiosResponse) => {
      const contentType: string = httpRes?.headers?.["content-type"] ?? "";

      if (httpRes?.status == null)
        throw new Error(`status code not found in response: ${httpRes}`);
      const res: operations.PicoLoadmanagementGroupGetResponse =
        new operations.PicoLoadmanagementGroupGetResponse({
          statusCode: httpRes.status,
          contentType: contentType,
          rawResponse: httpRes,
        });
      switch (true) {
        case httpRes?.status == 200:
          if (utils.matchContentType(contentType, `application/json`)) {
            res.picoLoadmanagementGroupDto = utils.objectToClass(
              httpRes?.data,
              shared.PicoLoadmanagementGroupDto
            );
          }
          if (utils.matchContentType(contentType, `text/json`)) {
            res.picoLoadmanagementGroupDto = utils.objectToClass(
              httpRes?.data,
              shared.PicoLoadmanagementGroupDto
            );
          }
          if (utils.matchContentType(contentType, `application/xml`)) {
            const resBody: string = JSON.stringify(httpRes?.data, null, 0);
            const out: Uint8Array = new Uint8Array(resBody.length);
            for (let i = 0; i < resBody.length; i++)
              out[i] = resBody.charCodeAt(i);
            res.body = out;
          }
          if (utils.matchContentType(contentType, `text/xml`)) {
            const resBody: string = JSON.stringify(httpRes?.data, null, 0);
            const out: Uint8Array = new Uint8Array(resBody.length);
            for (let i = 0; i < resBody.length; i++)
              out[i] = resBody.charCodeAt(i);
            res.body = out;
          }
          break;
      }

      return res;
    });
  }

  /**
   * GET: api/pico/loadmanagementgroup
   *
   *             Returns all available load management groups
   */
  getApiPicoLoadmanagementgroup(
    config?: AxiosRequestConfig
  ): Promise<operations.GetApiPicoLoadmanagementgroupResponse> {
    const baseURL: string = this._serverURL;
    const url: string =
      baseURL.replace(/\/$/, "") + "/api/pico/loadmanagementgroup";

    const client: AxiosInstance = this._defaultClient;

    const r = client.request({
      url: url,
      method: "get",
      ...config,
    });

    return r.then((httpRes: AxiosResponse) => {
      const contentType: string = httpRes?.headers?.["content-type"] ?? "";

      if (httpRes?.status == null)
        throw new Error(`status code not found in response: ${httpRes}`);
      const res: operations.GetApiPicoLoadmanagementgroupResponse =
        new operations.GetApiPicoLoadmanagementgroupResponse({
          statusCode: httpRes.status,
          contentType: contentType,
          rawResponse: httpRes,
        });
      switch (true) {
        case httpRes?.status == 200:
          if (utils.matchContentType(contentType, `application/json`)) {
            res.picoLoadmanagementGroupDtos = [];
            const resFieldDepth: number = utils.getResFieldDepth(res);
            res.picoLoadmanagementGroupDtos = utils.objectToClass(
              httpRes?.data,
              shared.PicoLoadmanagementGroupDto,
              resFieldDepth
            );
          }
          if (utils.matchContentType(contentType, `text/json`)) {
            res.picoLoadmanagementGroupDtos = [];
            const resFieldDepth: number = utils.getResFieldDepth(res);
            res.picoLoadmanagementGroupDtos = utils.objectToClass(
              httpRes?.data,
              shared.PicoLoadmanagementGroupDto,
              resFieldDepth
            );
          }
          if (utils.matchContentType(contentType, `application/xml`)) {
            const resBody: string = JSON.stringify(httpRes?.data, null, 0);
            const out: Uint8Array = new Uint8Array(resBody.length);
            for (let i = 0; i < resBody.length; i++)
              out[i] = resBody.charCodeAt(i);
            res.body = out;
          }
          if (utils.matchContentType(contentType, `text/xml`)) {
            const resBody: string = JSON.stringify(httpRes?.data, null, 0);
            const out: Uint8Array = new Uint8Array(resBody.length);
            for (let i = 0; i < resBody.length; i++)
              out[i] = resBody.charCodeAt(i);
            res.body = out;
          }
          break;
      }

      return res;
    });
  }
}
