/**
 * Sets the state of the code scanning alert. Can be one of `open` or `dismissed`. You must provide `dismissed_reason` when you set the state to `dismissed`.
 */
export declare enum CodeScanningAlertSetStateEnum {
    Open = "open",
    Dismissed = "dismissed"
}
