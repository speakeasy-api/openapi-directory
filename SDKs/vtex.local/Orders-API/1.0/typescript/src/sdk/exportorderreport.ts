/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import * as utils from "../internal/utils";
import * as operations from "./models/operations";
import * as shared from "./models/shared";
import { AxiosInstance, AxiosRequestConfig, AxiosResponse } from "axios";

export class ExportOrderReport {
  _defaultClient: AxiosInstance;
  _securityClient: AxiosInstance;
  _serverURL: string;
  _language: string;
  _sdkVersion: string;
  _genVersion: string;

  constructor(
    defaultClient: AxiosInstance,
    securityClient: AxiosInstance,
    serverURL: string,
    language: string,
    sdkVersion: string,
    genVersion: string
  ) {
    this._defaultClient = defaultClient;
    this._securityClient = securityClient;
    this._serverURL = serverURL;
    this._language = language;
    this._sdkVersion = sdkVersion;
    this._genVersion = genVersion;
  }

  /**
   * Export order report with status 'Completed'
   *
   * @remarks
   * Retrieves a list of all order reports that are `completed`, by `accountName`. Be aware that the scope of the export log is per user.
   *
   * > This endpoint is for VTEX internal use, and it is not meant to be used by clients.
   */
  statusCompleted(
    req: operations.StatusCompletedRequest,
    config?: AxiosRequestConfig
  ): Promise<operations.StatusCompletedResponse> {
    if (!(req instanceof utils.SpeakeasyBase)) {
      req = new operations.StatusCompletedRequest(req);
    }

    const baseURL: string = this._serverURL;
    const url: string =
      baseURL.replace(/\/$/, "") + "/api/oms/pvt/admin/reports/completed";

    const client: AxiosInstance = this._securityClient || this._defaultClient;

    const headers = { ...utils.getHeadersFromRequest(req), ...config?.headers };

    const r = client.request({
      url: url,
      method: "get",
      headers: headers,
      ...config,
    });

    return r.then((httpRes: AxiosResponse) => {
      const contentType: string = httpRes?.headers?.["content-type"] ?? "";

      if (httpRes?.status == null)
        throw new Error(`status code not found in response: ${httpRes}`);
      const res: operations.StatusCompletedResponse =
        new operations.StatusCompletedResponse({
          statusCode: httpRes.status,
          contentType: contentType,
          rawResponse: httpRes,
          headers: utils.getHeadersFromResponse(httpRes.headers),
        });
      switch (true) {
        case httpRes?.status == 200:
          if (
            utils.matchContentType(
              contentType,
              `application/json; charset=utf-8`
            )
          ) {
            res.exportCompletedResponses = [];
            const resFieldDepth: number = utils.getResFieldDepth(res);
            res.exportCompletedResponses = utils.objectToClass(
              httpRes?.data,
              shared.ExportCompletedResponse,
              resFieldDepth
            );
          }
          break;
      }

      return res;
    });
  }

  /**
   * Export order report with status 'In Progress'
   *
   * @remarks
   * Retrieves a list of all order reports that are `in progress`, by `accountName`. Be aware that the scope of the export log is per user.
   *
   * > This endpoint is for VTEX internal use, and it is not meant to be used by clients.
   */
  statusInProgress(
    req: operations.StatusInProgressRequest,
    config?: AxiosRequestConfig
  ): Promise<operations.StatusInProgressResponse> {
    if (!(req instanceof utils.SpeakeasyBase)) {
      req = new operations.StatusInProgressRequest(req);
    }

    const baseURL: string = this._serverURL;
    const url: string =
      baseURL.replace(/\/$/, "") + "/api/oms/pvt/admin/reports/inprogress";

    const client: AxiosInstance = this._securityClient || this._defaultClient;

    const headers = { ...utils.getHeadersFromRequest(req), ...config?.headers };

    const r = client.request({
      url: url,
      method: "get",
      headers: headers,
      ...config,
    });

    return r.then((httpRes: AxiosResponse) => {
      const contentType: string = httpRes?.headers?.["content-type"] ?? "";

      if (httpRes?.status == null)
        throw new Error(`status code not found in response: ${httpRes}`);
      const res: operations.StatusInProgressResponse =
        new operations.StatusInProgressResponse({
          statusCode: httpRes.status,
          contentType: contentType,
          rawResponse: httpRes,
          headers: utils.getHeadersFromResponse(httpRes.headers),
        });
      switch (true) {
        case httpRes?.status == 200:
          if (
            utils.matchContentType(
              contentType,
              `application/json; charset=utf-8`
            )
          ) {
            res.exportInProgressResponses = [];
            const resFieldDepth: number = utils.getResFieldDepth(res);
            res.exportInProgressResponses = utils.objectToClass(
              httpRes?.data,
              shared.ExportInProgressResponse,
              resFieldDepth
            );
          }
          break;
      }

      return res;
    });
  }
}
