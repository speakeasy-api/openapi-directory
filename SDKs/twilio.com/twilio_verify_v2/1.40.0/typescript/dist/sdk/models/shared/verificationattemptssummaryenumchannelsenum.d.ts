/**
 * Filter Verification Attempts considered on the summary aggregation by communication channel. Valid values are `SMS` and `CALL`
 */
export declare enum VerificationAttemptsSummaryEnumChannelsEnum {
    Sms = "sms",
    Call = "call",
    Email = "email",
    Whatsapp = "whatsapp"
}
