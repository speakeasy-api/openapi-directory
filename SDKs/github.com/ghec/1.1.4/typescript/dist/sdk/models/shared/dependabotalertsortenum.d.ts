/**
 * The property by which to sort the results.
 *
 * @remarks
 * `created` means when the alert was created.
 * `updated` means when the alert's state last changed.
 */
export declare enum DependabotAlertSortEnum {
    Created = "created",
    Updated = "updated"
}
