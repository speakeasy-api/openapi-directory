"""Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT."""

from __future__ import annotations
import dataclasses
from ..shared import webappicon as shared_webappicon
from dataclasses_json import Undefined, dataclass_json
from enum import Enum
from sdk import utils
from typing import Optional

class WebAppDisplayModeEnum(str, Enum):
    r"""The display mode of the web app. Possible values include: - \\"minimalUi\\", the device's status bar, navigation bar, the app's URL, and a refresh button are visible when the app is open. For HTTP URLs, you can only select this option. - \\"standalone\\", the device's status bar and navigation bar are visible when the app is open. - \\"fullScreen\\", the app opens in full screen mode, hiding the device's status and navigation bars. All browser UI elements, page URL, system status bar and back button are not visible, and the web app takes up the entirety of the available display area."""
    DISPLAY_MODE_UNSPECIFIED = 'displayModeUnspecified'
    MINIMAL_UI = 'minimalUi'
    STANDALONE = 'standalone'
    FULL_SCREEN = 'fullScreen'


@dataclass_json(undefined=Undefined.EXCLUDE)
@dataclasses.dataclass
class WebApp:
    r"""A WebApps resource represents a web app created for an enterprise. Web apps are published to managed Google Play and can be distributed like other Android apps. On a user's device, a web app opens its specified URL."""
    
    display_mode: Optional[WebAppDisplayModeEnum] = dataclasses.field(default=None, metadata={'dataclasses_json': { 'letter_case': utils.get_field_name('displayMode'), 'exclude': lambda f: f is None }})
    r"""The display mode of the web app. Possible values include: - \\"minimalUi\\", the device's status bar, navigation bar, the app's URL, and a refresh button are visible when the app is open. For HTTP URLs, you can only select this option. - \\"standalone\\", the device's status bar and navigation bar are visible when the app is open. - \\"fullScreen\\", the app opens in full screen mode, hiding the device's status and navigation bars. All browser UI elements, page URL, system status bar and back button are not visible, and the web app takes up the entirety of the available display area."""  
    icons: Optional[list[shared_webappicon.WebAppIcon]] = dataclasses.field(default=None, metadata={'dataclasses_json': { 'letter_case': utils.get_field_name('icons'), 'exclude': lambda f: f is None }})
    r"""A list of icons representing this website. If absent, a default icon (for create) or the current icon (for update) will be used."""  
    is_published: Optional[bool] = dataclasses.field(default=None, metadata={'dataclasses_json': { 'letter_case': utils.get_field_name('isPublished'), 'exclude': lambda f: f is None }})
    r"""A flag whether the app has been published to the Play store yet."""  
    start_url: Optional[str] = dataclasses.field(default=None, metadata={'dataclasses_json': { 'letter_case': utils.get_field_name('startUrl'), 'exclude': lambda f: f is None }})
    r"""The start URL, i.e. the URL that should load when the user opens the application."""  
    title: Optional[str] = dataclasses.field(default=None, metadata={'dataclasses_json': { 'letter_case': utils.get_field_name('title'), 'exclude': lambda f: f is None }})
    r"""The title of the web app as displayed to the user (e.g., amongst a list of other applications, or as a label for an icon)."""  
    version_code: Optional[str] = dataclasses.field(default=None, metadata={'dataclasses_json': { 'letter_case': utils.get_field_name('versionCode'), 'exclude': lambda f: f is None }})
    r"""The current version of the app. Note that the version can automatically increase during the lifetime of the web app, while Google does internal housekeeping to keep the web app up-to-date."""  
    web_app_id: Optional[str] = dataclasses.field(default=None, metadata={'dataclasses_json': { 'letter_case': utils.get_field_name('webAppId'), 'exclude': lambda f: f is None }})
    r"""The ID of the application. A string of the form \\"app:<package name>\\" where the package name always starts with the prefix \\"com.google.enterprise.webapp.\\" followed by a random id."""  
    