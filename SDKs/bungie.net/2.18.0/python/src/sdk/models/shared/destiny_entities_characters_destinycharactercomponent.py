"""Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT."""

from __future__ import annotations
import dataclasses
from ..shared import destiny_destinyprogressionresetentry as shared_destiny_destinyprogressionresetentry
from datetime import datetime
from typing import Optional


@dataclasses.dataclass
class DestinyEntitiesCharactersDestinyCharacterComponentEmblemColor:
    r"""Represents a color whose RGBA values are all represented as values between 0 and 255."""
    
    alpha: Optional[str] = dataclasses.field(default=None)  
    blue: Optional[str] = dataclasses.field(default=None)  
    green: Optional[str] = dataclasses.field(default=None)  
    red: Optional[str] = dataclasses.field(default=None)  
    

@dataclasses.dataclass
class DestinyEntitiesCharactersDestinyCharacterComponentLevelProgression:
    r"""Information about a current character's status with a Progression. A progression is a value that can increase with activity and has levels. Think Character Level and Reputation Levels. Combine this \\"live\\" data with the related DestinyProgressionDefinition for a full picture of the Progression."""
    
    current_progress: Optional[int] = dataclasses.field(default=None)
    r"""This is the total amount of progress obtained overall for this progression (for instance, the total amount of Character Level experience earned)"""  
    current_reset_count: Optional[int] = dataclasses.field(default=None)
    r"""The number of resets of this progression you've executed this season, if applicable to this progression."""  
    daily_limit: Optional[int] = dataclasses.field(default=None)
    r"""If this progression has a daily limit, this is that limit."""  
    daily_progress: Optional[int] = dataclasses.field(default=None)
    r"""The amount of progress earned today for this progression."""  
    level: Optional[int] = dataclasses.field(default=None)
    r"""This is the level of the progression (for instance, the Character Level)."""  
    level_cap: Optional[int] = dataclasses.field(default=None)
    r"""This is the maximum possible level you can achieve for this progression (for example, the maximum character level obtainable)"""  
    next_level_at: Optional[int] = dataclasses.field(default=None)
    r"""The total amount of progression (i.e. \\"Experience\\") needed in order to reach the next level."""  
    progression_hash: Optional[int] = dataclasses.field(default=None)
    r"""The hash identifier of the Progression in question. Use it to look up the DestinyProgressionDefinition in static data."""  
    progress_to_next_level: Optional[int] = dataclasses.field(default=None)
    r"""The amount of progression (i.e. \\"Experience\\") needed to reach the next level of this Progression. Jeez, progression is such an overloaded word."""  
    reward_item_states: Optional[list[int]] = dataclasses.field(default=None)
    r"""Information about historical rewards for this progression, if there is any data for it."""  
    season_resets: Optional[list[shared_destiny_destinyprogressionresetentry.DestinyDestinyProgressionResetEntry]] = dataclasses.field(default=None)
    r"""Information about historical resets of this progression, if there is any data for it."""  
    step_index: Optional[int] = dataclasses.field(default=None)
    r"""Progressions define their levels in \\"steps\\". Since the last step may be repeatable, the user may be at a higher level than the actual Step achieved in the progression. Not necessarily useful, but potentially interesting for those cruising the API. Relate this to the \\"steps\\" property of the DestinyProgression to see which step the user is on, if you care about that. (Note that this is Content Version dependent since it refers to indexes.)"""  
    weekly_limit: Optional[int] = dataclasses.field(default=None)
    r"""If this progression has a weekly limit, this is that limit."""  
    weekly_progress: Optional[int] = dataclasses.field(default=None)
    r"""The amount of progress earned toward this progression in the current week."""  
    

@dataclasses.dataclass
class DestinyEntitiesCharactersDestinyCharacterComponent:
    r"""This component contains base properties of the character. You'll probably want to always request this component, but hey you do you."""
    
    base_character_level: Optional[int] = dataclasses.field(default=None)
    r"""The \\"base\\" level of your character, not accounting for any light level."""  
    character_id: Optional[int] = dataclasses.field(default=None)
    r"""The unique identifier for the character."""  
    class_hash: Optional[int] = dataclasses.field(default=None)
    r"""Use this hash to look up the character's DestinyClassDefinition."""  
    class_type: Optional[int] = dataclasses.field(default=None)
    r"""Mostly for historical purposes at this point, this is an enumeration for the character's class.
    It'll be preferable in the general case to look up the related definition: but for some people this was too convenient to remove.
    """  
    date_last_played: Optional[datetime] = dataclasses.field(default=None)
    r"""The last date that the user played Destiny."""  
    emblem_background_path: Optional[str] = dataclasses.field(default=None)
    r"""A shortcut path to the user's currently equipped emblem background image. If you're just showing summary info for a user, this is more convenient than examining their equipped emblem and looking up the definition."""  
    emblem_color: Optional[DestinyEntitiesCharactersDestinyCharacterComponentEmblemColor] = dataclasses.field(default=None)
    r"""A shortcut for getting the background color of the user's currently equipped emblem without having to do a DestinyInventoryItemDefinition lookup."""  
    emblem_hash: Optional[int] = dataclasses.field(default=None)
    r"""The hash of the currently equipped emblem for the user. Can be used to look up the DestinyInventoryItemDefinition."""  
    emblem_path: Optional[str] = dataclasses.field(default=None)
    r"""A shortcut path to the user's currently equipped emblem image. If you're just showing summary info for a user, this is more convenient than examining their equipped emblem and looking up the definition."""  
    gender_hash: Optional[int] = dataclasses.field(default=None)
    r"""Use this hash to look up the character's DestinyGenderDefinition."""  
    gender_type: Optional[int] = dataclasses.field(default=None)
    r"""Mostly for historical purposes at this point, this is an enumeration for the character's Gender.
    It'll be preferable in the general case to look up the related definition: but for some people this was too convenient to remove. And yeah, it's an enumeration and not a boolean. Fight me.
    """  
    level_progression: Optional[DestinyEntitiesCharactersDestinyCharacterComponentLevelProgression] = dataclasses.field(default=None)
    r"""The progression that indicates your character's level. Not their light level, but their character level: you know, the thing you max out a couple hours in and then ignore for the sake of light level."""  
    light: Optional[int] = dataclasses.field(default=None)
    r"""The user's calculated \\"Light Level\\". Light level is an indicator of your power that mostly matters in the end game, once you've reached the maximum character level: it's a level that's dependent on the average Attack/Defense power of your items."""  
    membership_id: Optional[int] = dataclasses.field(default=None)
    r"""Every Destiny Profile has a membershipId. This is provided on the character as well for convenience."""  
    membership_type: Optional[int] = dataclasses.field(default=None)
    r"""membershipType tells you the platform on which the character plays. Examine the BungieMembershipType enumeration for possible values."""  
    minutes_played_this_session: Optional[int] = dataclasses.field(default=None)
    r"""If the user is currently playing, this is how long they've been playing."""  
    minutes_played_total: Optional[int] = dataclasses.field(default=None)
    r"""If this value is 525,600, then they played Destiny for a year. Or they're a very dedicated Rent fan. Note that this includes idle time, not just time spent actually in activities shooting things."""  
    percent_to_next_level: Optional[float] = dataclasses.field(default=None)
    r"""A number between 0 and 100, indicating the whole and fractional % remaining to get to the next character level."""  
    race_hash: Optional[int] = dataclasses.field(default=None)
    r"""Use this hash to look up the character's DestinyRaceDefinition."""  
    race_type: Optional[int] = dataclasses.field(default=None)
    r"""Mostly for historical purposes at this point, this is an enumeration for the character's race.
    It'll be preferable in the general case to look up the related definition: but for some people this was too convenient to remove.
    """  
    stats: Optional[dict[str, int]] = dataclasses.field(default=None)
    r"""Your character's stats, such as Agility, Resilience, etc... *not* historical stats.
    You'll have to call a different endpoint for those.
    """  
    title_record_hash: Optional[int] = dataclasses.field(default=None)
    r"""If this Character has a title assigned to it, this is the identifier of the DestinyRecordDefinition that has that title information."""  
    