/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import * as utils from "../internal/utils";
import * as operations from "./models/operations";
import * as shared from "./models/shared";
import { AxiosInstance, AxiosRequestConfig, AxiosResponse } from "axios";

export class EarlyDayMotions {
  _defaultClient: AxiosInstance;
  _securityClient: AxiosInstance;
  _serverURL: string;
  _language: string;
  _sdkVersion: string;
  _genVersion: string;

  constructor(
    defaultClient: AxiosInstance,
    securityClient: AxiosInstance,
    serverURL: string,
    language: string,
    sdkVersion: string,
    genVersion: string
  ) {
    this._defaultClient = defaultClient;
    this._securityClient = securityClient;
    this._serverURL = serverURL;
    this._language = language;
    this._sdkVersion = sdkVersion;
    this._genVersion = genVersion;
  }

  /**
   * Returns a single Early Day Motion by ID
   *
   * @remarks
   * Get a single Early Day Motion which has the ID specified.
   */
  publishedEarlyDayMotionGet(
    req: operations.PublishedEarlyDayMotionGetRequest,
    config?: AxiosRequestConfig
  ): Promise<operations.PublishedEarlyDayMotionGetResponse> {
    if (!(req instanceof utils.SpeakeasyBase)) {
      req = new operations.PublishedEarlyDayMotionGetRequest(req);
    }

    const baseURL: string = this._serverURL;
    const url: string = utils.generateURL(baseURL, "/EarlyDayMotion/{id}", req);

    const client: AxiosInstance = this._defaultClient;

    const r = client.request({
      url: url,
      method: "get",
      ...config,
    });

    return r.then((httpRes: AxiosResponse) => {
      const contentType: string = httpRes?.headers?.["content-type"] ?? "";

      if (httpRes?.status == null)
        throw new Error(`status code not found in response: ${httpRes}`);
      const res: operations.PublishedEarlyDayMotionGetResponse =
        new operations.PublishedEarlyDayMotionGetResponse({
          statusCode: httpRes.status,
          contentType: contentType,
          rawResponse: httpRes,
        });
      switch (true) {
        case httpRes?.status == 200:
          if (utils.matchContentType(contentType, `application/json`)) {
            res.apiResponseListPublishedWrittenQuestion = utils.objectToClass(
              httpRes?.data,
              shared.ApiResponseListPublishedWrittenQuestion
            );
          }
          if (utils.matchContentType(contentType, `text/json`)) {
            res.apiResponseListPublishedWrittenQuestion = utils.objectToClass(
              httpRes?.data,
              shared.ApiResponseListPublishedWrittenQuestion
            );
          }
          break;
        case [400, 404].includes(httpRes?.status):
          if (utils.matchContentType(contentType, `application/json`)) {
            res.apiResponseObject = utils.objectToClass(
              httpRes?.data,
              shared.ApiResponseObject
            );
          }
          if (utils.matchContentType(contentType, `text/json`)) {
            res.apiResponseObject = utils.objectToClass(
              httpRes?.data,
              shared.ApiResponseObject
            );
          }
          break;
      }

      return res;
    });
  }

  /**
   * Returns a list of Early Day Motions
   *
   * @remarks
   * Get a list of Early Day Motions which meet the specified criteria. Only supports Published and Withdrawn status.
   */
  getEarlyDayMotionsList(
    req: operations.GetEarlyDayMotionsListRequest,
    config?: AxiosRequestConfig
  ): Promise<operations.GetEarlyDayMotionsListResponse> {
    if (!(req instanceof utils.SpeakeasyBase)) {
      req = new operations.GetEarlyDayMotionsListRequest(req);
    }

    const baseURL: string = this._serverURL;
    const url: string = baseURL.replace(/\/$/, "") + "/EarlyDayMotions/list";

    const client: AxiosInstance = this._defaultClient;

    const queryParams: string = utils.serializeQueryParams(req);

    const r = client.request({
      url: url + queryParams,
      method: "get",
      ...config,
    });

    return r.then((httpRes: AxiosResponse) => {
      const contentType: string = httpRes?.headers?.["content-type"] ?? "";

      if (httpRes?.status == null)
        throw new Error(`status code not found in response: ${httpRes}`);
      const res: operations.GetEarlyDayMotionsListResponse =
        new operations.GetEarlyDayMotionsListResponse({
          statusCode: httpRes.status,
          contentType: contentType,
          rawResponse: httpRes,
        });
      switch (true) {
        case httpRes?.status == 200:
          if (utils.matchContentType(contentType, `application/json`)) {
            res.apiResponseListPublishedWrittenQuestion = utils.objectToClass(
              httpRes?.data,
              shared.ApiResponseListPublishedWrittenQuestion
            );
          }
          if (utils.matchContentType(contentType, `text/json`)) {
            res.apiResponseListPublishedWrittenQuestion = utils.objectToClass(
              httpRes?.data,
              shared.ApiResponseListPublishedWrittenQuestion
            );
          }
          if (utils.matchContentType(contentType, `application/xml`)) {
            const resBody: string = JSON.stringify(httpRes?.data, null, 0);
            const out: Uint8Array = new Uint8Array(resBody.length);
            for (let i = 0; i < resBody.length; i++)
              out[i] = resBody.charCodeAt(i);
            res.body = out;
          }
          if (utils.matchContentType(contentType, `text/xml`)) {
            const resBody: string = JSON.stringify(httpRes?.data, null, 0);
            const out: Uint8Array = new Uint8Array(resBody.length);
            for (let i = 0; i < resBody.length; i++)
              out[i] = resBody.charCodeAt(i);
            res.body = out;
          }
          break;
        case httpRes?.status == 400:
          if (utils.matchContentType(contentType, `application/xml`)) {
            const resBody: string = JSON.stringify(httpRes?.data, null, 0);
            const out: Uint8Array = new Uint8Array(resBody.length);
            for (let i = 0; i < resBody.length; i++)
              out[i] = resBody.charCodeAt(i);
            res.body = out;
          }
          if (utils.matchContentType(contentType, `text/xml`)) {
            const resBody: string = JSON.stringify(httpRes?.data, null, 0);
            const out: Uint8Array = new Uint8Array(resBody.length);
            for (let i = 0; i < resBody.length; i++)
              out[i] = resBody.charCodeAt(i);
            res.body = out;
          }
          if (utils.matchContentType(contentType, `application/json`)) {
            res.apiResponseObject = utils.objectToClass(
              httpRes?.data,
              shared.ApiResponseObject
            );
          }
          if (utils.matchContentType(contentType, `text/json`)) {
            res.apiResponseObject = utils.objectToClass(
              httpRes?.data,
              shared.ApiResponseObject
            );
          }
          break;
      }

      return res;
    });
  }
}
