/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import * as utils from "../internal/utils";
import * as operations from "./models/operations";
import * as shared from "./models/shared";
import { AxiosInstance, AxiosRequestConfig, AxiosResponse } from "axios";

/**
 * Observation Heading Controller
 */
export class ObservationHeadingController {
  _defaultClient: AxiosInstance;
  _securityClient: AxiosInstance;
  _serverURL: string;
  _language: string;
  _sdkVersion: string;
  _genVersion: string;

  constructor(
    defaultClient: AxiosInstance,
    securityClient: AxiosInstance,
    serverURL: string,
    language: string,
    sdkVersion: string,
    genVersion: string
  ) {
    this._defaultClient = defaultClient;
    this._securityClient = securityClient;
    this._serverURL = serverURL;
    this._language = language;
    this._sdkVersion = sdkVersion;
    this._genVersion = genVersion;
  }

  /**
   * Get Available Observations Types For a User
   *
   * @remarks
   * Given a User ID retrieve a list of available observation types for that user (where they have observation data).
   */
  getAvailableObservationHeadings(
    req: operations.GetAvailableObservationHeadingsRequest,
    config?: AxiosRequestConfig
  ): Promise<operations.GetAvailableObservationHeadingsResponse> {
    if (!(req instanceof utils.SpeakeasyBase)) {
      req = new operations.GetAvailableObservationHeadingsRequest(req);
    }

    const baseURL: string = this._serverURL;
    const url: string = utils.generateURL(
      baseURL,
      "/user/{userId}/availableobservationheadings",
      req
    );

    const client: AxiosInstance = this._defaultClient;

    const r = client.request({
      url: url,
      method: "get",
      ...config,
    });

    return r.then((httpRes: AxiosResponse) => {
      const contentType: string = httpRes?.headers?.["content-type"] ?? "";

      if (httpRes?.status == null)
        throw new Error(`status code not found in response: ${httpRes}`);
      const res: operations.GetAvailableObservationHeadingsResponse =
        new operations.GetAvailableObservationHeadingsResponse({
          statusCode: httpRes.status,
          contentType: contentType,
          rawResponse: httpRes,
        });
      switch (true) {
        case httpRes?.status == 200:
          if (utils.matchContentType(contentType, `application/json`)) {
            res.observationHeadings = [];
            const resFieldDepth: number = utils.getResFieldDepth(res);
            res.observationHeadings = utils.objectToClass(
              httpRes?.data,
              shared.ObservationHeading,
              resFieldDepth
            );
          }
          break;
        case [401, 403, 404].includes(httpRes?.status):
          break;
      }

      return res;
    });
  }

  /**
   * Get Available Patient Entered Observations Types For a User
   *
   * @remarks
   * Given a User ID retrieve a list of available observation types for that user (where they have patient entered observation data).
   */
  getPatientEnteredObservationHeadings(
    req: operations.GetPatientEnteredObservationHeadingsRequest,
    config?: AxiosRequestConfig
  ): Promise<operations.GetPatientEnteredObservationHeadingsResponse> {
    if (!(req instanceof utils.SpeakeasyBase)) {
      req = new operations.GetPatientEnteredObservationHeadingsRequest(req);
    }

    const baseURL: string = this._serverURL;
    const url: string = utils.generateURL(
      baseURL,
      "/user/{userId}/patiententeredobservationheadings",
      req
    );

    const client: AxiosInstance = this._defaultClient;

    const r = client.request({
      url: url,
      method: "get",
      ...config,
    });

    return r.then((httpRes: AxiosResponse) => {
      const contentType: string = httpRes?.headers?.["content-type"] ?? "";

      if (httpRes?.status == null)
        throw new Error(`status code not found in response: ${httpRes}`);
      const res: operations.GetPatientEnteredObservationHeadingsResponse =
        new operations.GetPatientEnteredObservationHeadingsResponse({
          statusCode: httpRes.status,
          contentType: contentType,
          rawResponse: httpRes,
        });
      switch (true) {
        case httpRes?.status == 200:
          if (utils.matchContentType(contentType, `application/json`)) {
            res.observationHeadings = [];
            const resFieldDepth: number = utils.getResFieldDepth(res);
            res.observationHeadings = utils.objectToClass(
              httpRes?.data,
              shared.ObservationHeading,
              resFieldDepth
            );
          }
          break;
        case [401, 403, 404].includes(httpRes?.status):
          break;
      }

      return res;
    });
  }
}
