/**
 * Specifies the type of bank account. Currently returned values are `SAVER`
 *
 * @remarks
 * and `TRANSACTIONAL`.
 *
 */
export declare enum AccountTypeEnumEnum {
    Saver = "SAVER",
    Transactional = "TRANSACTIONAL"
}
