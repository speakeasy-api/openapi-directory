"""Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT."""

from __future__ import annotations
import dataclasses
from ..shared import destiny_character_destinyitempeerview as shared_destiny_character_destinyitempeerview
from ..shared import destiny_dyereference as shared_destiny_dyereference
from typing import Optional


@dataclasses.dataclass
class DestinyEntitiesCharactersDestinyCharacterRenderComponentCustomization:
    r"""Raw data about the customization options chosen for a character's face and appearance.
    You can look up the relevant class/race/gender combo in DestinyCharacterCustomizationOptionDefinition for the character, and then look up these values within the CustomizationOptions found to pull some data about their choices. Warning: not all of that data is meaningful. Some data has useful icons. Others have nothing, and are only meant for 3D rendering purposes (which we sadly do not expose yet)
    """
    
    decal_color: Optional[int] = dataclasses.field(default=None)  
    decal_index: Optional[int] = dataclasses.field(default=None)  
    eye_color: Optional[int] = dataclasses.field(default=None)  
    face: Optional[int] = dataclasses.field(default=None)  
    feature_colors: Optional[list[int]] = dataclasses.field(default=None)  
    feature_index: Optional[int] = dataclasses.field(default=None)  
    hair_colors: Optional[list[int]] = dataclasses.field(default=None)  
    hair_index: Optional[int] = dataclasses.field(default=None)  
    lip_color: Optional[int] = dataclasses.field(default=None)  
    personality: Optional[int] = dataclasses.field(default=None)  
    skin_color: Optional[int] = dataclasses.field(default=None)  
    wear_helmet: Optional[bool] = dataclasses.field(default=None)  
    

@dataclasses.dataclass
class DestinyEntitiesCharactersDestinyCharacterRenderComponentPeerView:
    r"""A minimal view of a character's equipped items, for the purpose of rendering a summary screen or showing the character in 3D."""
    
    equipment: Optional[list[shared_destiny_character_destinyitempeerview.DestinyCharacterDestinyItemPeerView]] = dataclasses.field(default=None)  
    

@dataclasses.dataclass
class DestinyEntitiesCharactersDestinyCharacterRenderComponent:
    r"""Only really useful if you're attempting to render the character's current appearance in 3D, this returns a bare minimum of information, pre-aggregated, that you'll need to perform that rendering. Note that you need to combine this with other 3D assets and data from our servers.
    Examine the Javascript returned by https://bungie.net/sharedbundle/spasm to see how we use this data, but be warned: the rabbit hole goes pretty deep.
    """
    
    custom_dyes: Optional[list[shared_destiny_dyereference.DestinyDyeReference]] = dataclasses.field(default=None)
    r"""Custom dyes, calculated by iterating over the character's equipped items. Useful for pre-fetching all of the dye data needed from our server."""  
    customization: Optional[DestinyEntitiesCharactersDestinyCharacterRenderComponentCustomization] = dataclasses.field(default=None)
    r"""This is actually something that Spasm.js *doesn't* do right now, and that we don't return assets for yet. This is the data about what character customization options you picked. You can combine this with DestinyCharacterCustomizationOptionDefinition to show some cool info, and hopefully someday to actually render a user's face in 3D. We'll see if we ever end up with time for that."""  
    peer_view: Optional[DestinyEntitiesCharactersDestinyCharacterRenderComponentPeerView] = dataclasses.field(default=None)
    r"""A minimal view of:
    - Equipped items
    - The rendering-related custom options on those equipped items
    Combined, that should be enough to render all of the items on the equipped character.
    """  
    