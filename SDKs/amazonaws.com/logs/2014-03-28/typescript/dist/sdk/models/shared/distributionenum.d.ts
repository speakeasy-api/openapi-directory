/**
 * The method used to distribute log data to the destination, which can be either random or grouped by log stream.
 */
export declare enum DistributionEnum {
    Random = "Random",
    ByLogStream = "ByLogStream"
}
