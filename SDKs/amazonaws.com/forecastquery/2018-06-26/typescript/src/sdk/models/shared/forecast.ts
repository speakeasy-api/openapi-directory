/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import {
  objectToClass,
  SpeakeasyBase,
  SpeakeasyMetadata,
} from "../../../internal/utils";
import { DataPoint } from "./datapoint";
import { Expose, Transform } from "class-transformer";

/**
 * Provides information about a forecast. Returned as part of the <a>QueryForecast</a> response.
 */
export class Forecast extends SpeakeasyBase {
  @SpeakeasyMetadata({ elemType: DataPoint, elemDepth: 2 })
  @Expose({ name: "Predictions" })
  @Transform(
    ({ value }) => {
      const obj: Record<string, DataPoint[]> = {};
      for (const key in value) {
        obj[key] = objectToClass(value[key], DataPoint);
      }
      return obj;
    },
    { toClassOnly: true }
  )
  predictions?: Record<string, DataPoint[]>;
}
