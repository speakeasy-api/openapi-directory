/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import * as utils from "../internal/utils";
import * as operations from "./models/operations";
import * as shared from "./models/shared";
import { AxiosInstance, AxiosRequestConfig, AxiosResponse } from "axios";

export class SecurityAndNetworking {
  _defaultClient: AxiosInstance;
  _securityClient: AxiosInstance;
  _serverURL: string;
  _language: string;
  _sdkVersion: string;
  _genVersion: string;

  constructor(
    defaultClient: AxiosInstance,
    securityClient: AxiosInstance,
    serverURL: string,
    language: string,
    sdkVersion: string,
    genVersion: string
  ) {
    this._defaultClient = defaultClient;
    this._securityClient = securityClient;
    this._serverURL = serverURL;
    this._language = language;
    this._sdkVersion = sdkVersion;
    this._genVersion = genVersion;
  }

  /**
   * Email Verify
   *
   * @remarks
   * SMTP based email address verification
   */
  emailVerify(
    req: operations.EmailVerifyRequest,
    config?: AxiosRequestConfig
  ): Promise<operations.EmailVerifyResponse> {
    if (!(req instanceof utils.SpeakeasyBase)) {
      req = new operations.EmailVerifyRequest(req);
    }

    const baseURL: string = this._serverURL;
    const url: string = baseURL.replace(/\/$/, "") + "/email-verify";

    const client: AxiosInstance = this._securityClient || this._defaultClient;

    const queryParams: string = utils.serializeQueryParams(req);

    const r = client.request({
      url: url + queryParams,
      method: "get",
      ...config,
    });

    return r.then((httpRes: AxiosResponse) => {
      const contentType: string = httpRes?.headers?.["content-type"] ?? "";

      if (httpRes?.status == null)
        throw new Error(`status code not found in response: ${httpRes}`);
      const res: operations.EmailVerifyResponse =
        new operations.EmailVerifyResponse({
          statusCode: httpRes.status,
          contentType: contentType,
          rawResponse: httpRes,
        });
      switch (true) {
        case httpRes?.status == 200:
          if (utils.matchContentType(contentType, `application/json`)) {
            res.emailVerifyResponse = utils.objectToClass(
              httpRes?.data,
              shared.EmailVerifyResponse
            );
          }
          break;
        default:
          if (utils.matchContentType(contentType, `application/json`)) {
            res.apiError = utils.objectToClass(httpRes?.data, shared.APIError);
          }
          break;
      }

      return res;
    });
  }

  /**
   * Host Reputation
   *
   * @remarks
   * Check the reputation of an IP address, domain name or URL against a comprehensive list of blacklists and blocklists
   */
  hostReputation(
    req: operations.HostReputationRequest,
    config?: AxiosRequestConfig
  ): Promise<operations.HostReputationResponse> {
    if (!(req instanceof utils.SpeakeasyBase)) {
      req = new operations.HostReputationRequest(req);
    }

    const baseURL: string = this._serverURL;
    const url: string = baseURL.replace(/\/$/, "") + "/host-reputation";

    const client: AxiosInstance = this._securityClient || this._defaultClient;

    const queryParams: string = utils.serializeQueryParams(req);

    const r = client.request({
      url: url + queryParams,
      method: "get",
      ...config,
    });

    return r.then((httpRes: AxiosResponse) => {
      const contentType: string = httpRes?.headers?.["content-type"] ?? "";

      if (httpRes?.status == null)
        throw new Error(`status code not found in response: ${httpRes}`);
      const res: operations.HostReputationResponse =
        new operations.HostReputationResponse({
          statusCode: httpRes.status,
          contentType: contentType,
          rawResponse: httpRes,
        });
      switch (true) {
        case httpRes?.status == 200:
          if (utils.matchContentType(contentType, `application/json`)) {
            res.hostReputationResponse = utils.objectToClass(
              httpRes?.data,
              shared.HostReputationResponse
            );
          }
          break;
        default:
          if (utils.matchContentType(contentType, `application/json`)) {
            res.apiError = utils.objectToClass(httpRes?.data, shared.APIError);
          }
          break;
      }

      return res;
    });
  }

  /**
   * IP Blocklist
   *
   * @remarks
   * The IP Blocklist API will detect potentially malicious or dangerous IP addresses
   */
  ipBlocklist(
    req: operations.IPBlocklistRequest,
    config?: AxiosRequestConfig
  ): Promise<operations.IPBlocklistResponse> {
    if (!(req instanceof utils.SpeakeasyBase)) {
      req = new operations.IPBlocklistRequest(req);
    }

    const baseURL: string = this._serverURL;
    const url: string = baseURL.replace(/\/$/, "") + "/ip-blocklist";

    const client: AxiosInstance = this._securityClient || this._defaultClient;

    const queryParams: string = utils.serializeQueryParams(req);

    const r = client.request({
      url: url + queryParams,
      method: "get",
      ...config,
    });

    return r.then((httpRes: AxiosResponse) => {
      const contentType: string = httpRes?.headers?.["content-type"] ?? "";

      if (httpRes?.status == null)
        throw new Error(`status code not found in response: ${httpRes}`);
      const res: operations.IPBlocklistResponse =
        new operations.IPBlocklistResponse({
          statusCode: httpRes.status,
          contentType: contentType,
          rawResponse: httpRes,
        });
      switch (true) {
        case httpRes?.status == 200:
          if (utils.matchContentType(contentType, `application/json`)) {
            res.ipBlocklistResponse = utils.objectToClass(
              httpRes?.data,
              shared.IPBlocklistResponse
            );
          }
          break;
        default:
          if (utils.matchContentType(contentType, `application/json`)) {
            res.apiError = utils.objectToClass(httpRes?.data, shared.APIError);
          }
          break;
      }

      return res;
    });
  }

  /**
   * IP Blocklist Download
   *
   * @remarks
   * This API is a direct feed to our IP blocklist data
   */
  ipBlocklistDownload(
    req: operations.IPBlocklistDownloadRequest,
    config?: AxiosRequestConfig
  ): Promise<operations.IPBlocklistDownloadResponse> {
    if (!(req instanceof utils.SpeakeasyBase)) {
      req = new operations.IPBlocklistDownloadRequest(req);
    }

    const baseURL: string = this._serverURL;
    const url: string = baseURL.replace(/\/$/, "") + "/ip-blocklist-download";

    const client: AxiosInstance = this._securityClient || this._defaultClient;

    const queryParams: string = utils.serializeQueryParams(req);

    const r = client.request({
      url: url + queryParams,
      method: "get",
      ...config,
    });

    return r.then((httpRes: AxiosResponse) => {
      const contentType: string = httpRes?.headers?.["content-type"] ?? "";

      if (httpRes?.status == null)
        throw new Error(`status code not found in response: ${httpRes}`);
      const res: operations.IPBlocklistDownloadResponse =
        new operations.IPBlocklistDownloadResponse({
          statusCode: httpRes.status,
          contentType: contentType,
          rawResponse: httpRes,
        });
      switch (true) {
        case httpRes?.status == 200:
          if (utils.matchContentType(contentType, `application/json`)) {
            const resBody: string = JSON.stringify(httpRes?.data, null, 0);
            const out: Uint8Array = new Uint8Array(resBody.length);
            for (let i = 0; i < resBody.length; i++)
              out[i] = resBody.charCodeAt(i);
            res.ipBlocklistDownload200ApplicationJSONBinaryString = out;
          }
          break;
        default:
          if (utils.matchContentType(contentType, `application/json`)) {
            res.apiError = utils.objectToClass(httpRes?.data, shared.APIError);
          }
          break;
      }

      return res;
    });
  }

  /**
   * IP Probe
   *
   * @remarks
   * Execute a realtime network probe against an IPv4 or IPv6 address
   */
  ipProbe(
    req: operations.IPProbeRequest,
    config?: AxiosRequestConfig
  ): Promise<operations.IPProbeResponse> {
    if (!(req instanceof utils.SpeakeasyBase)) {
      req = new operations.IPProbeRequest(req);
    }

    const baseURL: string = this._serverURL;
    const url: string = baseURL.replace(/\/$/, "") + "/ip-probe";

    const client: AxiosInstance = this._securityClient || this._defaultClient;

    const queryParams: string = utils.serializeQueryParams(req);

    const r = client.request({
      url: url + queryParams,
      method: "get",
      ...config,
    });

    return r.then((httpRes: AxiosResponse) => {
      const contentType: string = httpRes?.headers?.["content-type"] ?? "";

      if (httpRes?.status == null)
        throw new Error(`status code not found in response: ${httpRes}`);
      const res: operations.IPProbeResponse = new operations.IPProbeResponse({
        statusCode: httpRes.status,
        contentType: contentType,
        rawResponse: httpRes,
      });
      switch (true) {
        case httpRes?.status == 200:
          if (utils.matchContentType(contentType, `application/json`)) {
            res.ipProbeResponse = utils.objectToClass(
              httpRes?.data,
              shared.IPProbeResponse
            );
          }
          break;
        default:
          if (utils.matchContentType(contentType, `application/json`)) {
            res.apiError = utils.objectToClass(httpRes?.data, shared.APIError);
          }
          break;
      }

      return res;
    });
  }
}
