# DeviceCommandLogs

### Available Operations

* [GetNetworkSmDeviceDeviceCommandLogs](#getnetworksmdevicedevicecommandlogs) - Return historical records of commands sent to Systems Manager devices

## GetNetworkSmDeviceDeviceCommandLogs

Return historical records of commands sent to Systems Manager devices. Note that this will include the name of the Dashboard user who initiated the command if it was generated by a Dashboard admin rather than the automatic behavior of the system; you may wish to filter this out of any reports.

### Example Usage

```go
package main

import(
	"context"
	"log"
	"openapi"
	"openapi/pkg/models/operations"
)

func main() {
    s := sdk.New(
        sdk.WithSecurity(shared.Security{
            MerakiAPIKey: "YOUR_API_KEY_HERE",
        }),
    )

    ctx := context.Background()
    res, err := s.DeviceCommandLogs.GetNetworkSmDeviceDeviceCommandLogs(ctx, operations.GetNetworkSmDeviceDeviceCommandLogsRequest{
        DeviceID: "debitis",
        EndingBefore: sdk.String("pariatur"),
        NetworkID: "blanditiis",
        PerPage: sdk.Int64(860385),
        StartingAfter: sdk.String("minus"),
    })
    if err != nil {
        log.Fatal(err)
    }

    if res.GetNetworkSmDeviceDeviceCommandLogs200ApplicationJSONObjects != nil {
        // handle response
    }
}
```
