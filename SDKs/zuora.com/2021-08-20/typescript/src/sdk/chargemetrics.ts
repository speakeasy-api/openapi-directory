/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import * as utils from "../internal/utils";
import * as operations from "./models/operations";
import * as shared from "./models/shared";
import { AxiosInstance, AxiosRequestConfig, AxiosResponse } from "axios";

/**
 * Charge Metrics provides a service to access key metrics for rate plan charges in Zuora, for example, Gross MRR, Net MRR, Gross TCV, and Net TCV.
 *
 * @remarks
 *
 */
export class ChargeMetrics {
  _defaultClient: AxiosInstance;
  _securityClient: AxiosInstance;
  _serverURL: string;
  _language: string;
  _sdkVersion: string;
  _genVersion: string;

  constructor(
    defaultClient: AxiosInstance,
    securityClient: AxiosInstance,
    serverURL: string,
    language: string,
    sdkVersion: string,
    genVersion: string
  ) {
    this._defaultClient = defaultClient;
    this._securityClient = securityClient;
    this._serverURL = serverURL;
    this._language = language;
    this._sdkVersion = sdkVersion;
    this._genVersion = genVersion;
  }

  /**
   * List charge metrics by time range
   *
   * @remarks
   * Retrieves key charge metrics about rate plan charges that have changes in a specified time range.
   *
   * The purpose of `fromTimestamp` and `toTimestamp` is to synchronize charge metrics data incrementally.
   *
   */
  getChargeMetrics(
    req: operations.GETChargeMetricsRequest,
    config?: AxiosRequestConfig
  ): Promise<operations.GETChargeMetricsResponse> {
    if (!(req instanceof utils.SpeakeasyBase)) {
      req = new operations.GETChargeMetricsRequest(req);
    }

    const baseURL: string = this._serverURL;
    const url: string =
      baseURL.replace(/\/$/, "") + "/charge-metrics/data/charge-metrics";

    const client: AxiosInstance = this._defaultClient;

    const headers = { ...utils.getHeadersFromRequest(req), ...config?.headers };
    const queryParams: string = utils.serializeQueryParams(req);

    const r = client.request({
      url: url + queryParams,
      method: "get",
      headers: headers,
      ...config,
    });

    return r.then((httpRes: AxiosResponse) => {
      const contentType: string = httpRes?.headers?.["content-type"] ?? "";

      if (httpRes?.status == null)
        throw new Error(`status code not found in response: ${httpRes}`);
      const res: operations.GETChargeMetricsResponse =
        new operations.GETChargeMetricsResponse({
          statusCode: httpRes.status,
          contentType: contentType,
          rawResponse: httpRes,
          headers: utils.getHeadersFromResponse(httpRes.headers),
        });
      switch (true) {
        case httpRes?.status == 200:
          if (
            utils.matchContentType(
              contentType,
              `application/json; charset=utf-8`
            )
          ) {
            res.chargeMetricsResponse = utils.objectToClass(
              httpRes?.data,
              shared.ChargeMetricsResponse
            );
          }
          break;
        case [400, 429, 500].includes(httpRes?.status):
          if (
            utils.matchContentType(
              contentType,
              `application/json; charset=utf-8`
            )
          ) {
            res.commonReasonsErrorResponse = utils.objectToClass(
              httpRes?.data,
              shared.CommonReasonsErrorResponse
            );
          }
          break;
      }

      return res;
    });
  }

  /**
   * List discount allocation details by time range
   *
   * @remarks
   * Retrieves discount allocation details about rate plan charges that have changes in a specified time range.
   *
   * The purpose of `fromTimestamp` and `toTimestamp` is to synchronize discount allocation details incrementally.
   *
   */
  getChargeMetricsDiscountAllocationDetails(
    req: operations.GETChargeMetricsDiscountAllocationDetailsRequest,
    config?: AxiosRequestConfig
  ): Promise<operations.GETChargeMetricsDiscountAllocationDetailsResponse> {
    if (!(req instanceof utils.SpeakeasyBase)) {
      req = new operations.GETChargeMetricsDiscountAllocationDetailsRequest(
        req
      );
    }

    const baseURL: string = this._serverURL;
    const url: string =
      baseURL.replace(/\/$/, "") +
      "/charge-metrics/data/charge-metrics-discount-allocation-detail";

    const client: AxiosInstance = this._defaultClient;

    const headers = { ...utils.getHeadersFromRequest(req), ...config?.headers };
    const queryParams: string = utils.serializeQueryParams(req);

    const r = client.request({
      url: url + queryParams,
      method: "get",
      headers: headers,
      ...config,
    });

    return r.then((httpRes: AxiosResponse) => {
      const contentType: string = httpRes?.headers?.["content-type"] ?? "";

      if (httpRes?.status == null)
        throw new Error(`status code not found in response: ${httpRes}`);
      const res: operations.GETChargeMetricsDiscountAllocationDetailsResponse =
        new operations.GETChargeMetricsDiscountAllocationDetailsResponse({
          statusCode: httpRes.status,
          contentType: contentType,
          rawResponse: httpRes,
          headers: utils.getHeadersFromResponse(httpRes.headers),
        });
      switch (true) {
        case httpRes?.status == 200:
          if (
            utils.matchContentType(
              contentType,
              `application/json; charset=utf-8`
            )
          ) {
            res.chargeMetricsDiscountAllocationDetailResponse =
              utils.objectToClass(
                httpRes?.data,
                shared.ChargeMetricsDiscountAllocationDetailResponse
              );
          }
          break;
        case [400, 429, 500].includes(httpRes?.status):
          if (
            utils.matchContentType(
              contentType,
              `application/json; charset=utf-8`
            )
          ) {
            res.commonReasonsErrorResponse = utils.objectToClass(
              httpRes?.data,
              shared.CommonReasonsErrorResponse
            );
          }
          break;
      }

      return res;
    });
  }
}
