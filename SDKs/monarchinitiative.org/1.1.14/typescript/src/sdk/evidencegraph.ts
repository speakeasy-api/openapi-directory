/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import * as utils from "../internal/utils";
import * as operations from "./models/operations";
import * as shared from "./models/shared";
import { AxiosInstance, AxiosRequestConfig, AxiosResponse } from "axios";

/**
 * Operations on evidence graphs
 */
export class EvidenceGraph {
  _defaultClient: AxiosInstance;
  _securityClient: AxiosInstance;
  _serverURL: string;
  _language: string;
  _sdkVersion: string;
  _genVersion: string;

  constructor(
    defaultClient: AxiosInstance,
    securityClient: AxiosInstance,
    serverURL: string,
    language: string,
    sdkVersion: string,
    genVersion: string
  ) {
    this._defaultClient = defaultClient;
    this._securityClient = securityClient;
    this._serverURL = serverURL;
    this._language = language;
    this._sdkVersion = sdkVersion;
    this._genVersion = genVersion;
  }

  /**
   * Returns evidence graph object for a given association
   *
   * @remarks
   * Note that every association is assumed to have a unique ID
   */
  getEvidenceGraphObject(
    req: operations.GetEvidenceGraphObjectRequest,
    config?: AxiosRequestConfig
  ): Promise<operations.GetEvidenceGraphObjectResponse> {
    if (!(req instanceof utils.SpeakeasyBase)) {
      req = new operations.GetEvidenceGraphObjectRequest(req);
    }

    const baseURL: string = this._serverURL;
    const url: string = utils.generateURL(baseURL, "/evidence/graph/{id}", req);

    const client: AxiosInstance = this._defaultClient;

    const r = client.request({
      url: url,
      method: "get",
      ...config,
    });

    return r.then((httpRes: AxiosResponse) => {
      const contentType: string = httpRes?.headers?.["content-type"] ?? "";

      if (httpRes?.status == null)
        throw new Error(`status code not found in response: ${httpRes}`);
      const res: operations.GetEvidenceGraphObjectResponse =
        new operations.GetEvidenceGraphObjectResponse({
          statusCode: httpRes.status,
          contentType: contentType,
          rawResponse: httpRes,
        });
      switch (true) {
        case httpRes?.status == 200:
          if (utils.matchContentType(contentType, `application/json`)) {
            res.graphs = [];
            const resFieldDepth: number = utils.getResFieldDepth(res);
            res.graphs = utils.objectToClass(
              httpRes?.data,
              shared.Graph,
              resFieldDepth
            );
          }
          break;
      }

      return res;
    });
  }

  /**
   * Returns evidence as a association_results object given an association
   *
   * @remarks
   * Note that every association is assumed to have a unique ID
   */
  getEvidenceGraphTable(
    req: operations.GetEvidenceGraphTableRequest,
    config?: AxiosRequestConfig
  ): Promise<operations.GetEvidenceGraphTableResponse> {
    if (!(req instanceof utils.SpeakeasyBase)) {
      req = new operations.GetEvidenceGraphTableRequest(req);
    }

    const baseURL: string = this._serverURL;
    const url: string = utils.generateURL(
      baseURL,
      "/evidence/graph/{id}/table",
      req
    );

    const client: AxiosInstance = this._defaultClient;

    const queryParams: string = utils.serializeQueryParams(req);

    const r = client.request({
      url: url + queryParams,
      method: "get",
      ...config,
    });

    return r.then((httpRes: AxiosResponse) => {
      const contentType: string = httpRes?.headers?.["content-type"] ?? "";

      if (httpRes?.status == null)
        throw new Error(`status code not found in response: ${httpRes}`);
      const res: operations.GetEvidenceGraphTableResponse =
        new operations.GetEvidenceGraphTableResponse({
          statusCode: httpRes.status,
          contentType: contentType,
          rawResponse: httpRes,
        });
      switch (true) {
        case httpRes?.status == 200:
          if (utils.matchContentType(contentType, `application/json`)) {
            res.associationResults = [];
            const resFieldDepth: number = utils.getResFieldDepth(res);
            res.associationResults = utils.objectToClass(
              httpRes?.data,
              shared.AssociationResults,
              resFieldDepth
            );
          }
          break;
      }

      return res;
    });
  }
}
