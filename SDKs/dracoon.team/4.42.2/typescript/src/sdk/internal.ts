/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import * as utils from "../internal/utils";
import * as operations from "./models/operations";
import * as shared from "./models/shared";
import { AxiosInstance, AxiosRequestConfig, AxiosResponse } from "axios";

/**
 * Internal non-public APIs
 */
export class Internal {
  _defaultClient: AxiosInstance;
  _securityClient: AxiosInstance;
  _serverURL: string;
  _language: string;
  _sdkVersion: string;
  _genVersion: string;

  constructor(
    defaultClient: AxiosInstance,
    securityClient: AxiosInstance,
    serverURL: string,
    language: string,
    sdkVersion: string,
    genVersion: string
  ) {
    this._defaultClient = defaultClient;
    this._securityClient = securityClient;
    this._serverURL = serverURL;
    this._language = language;
    this._sdkVersion = sdkVersion;
    this._genVersion = genVersion;
  }

  /**
   * Request subscription plan
   *
   * @remarks
   * <h3 style='padding: 5px; background-color: #F6F7F8; border: 1px solid #AAA; border-radius: 5px; display: table-cell;'>&#128640; Since v4.36.0</h3>
   *
   * ### Description:
   * Get the subscription plan id of the current tenant
   *
   * ### Precondition:
   * Valid `X-SDS-Service-Token` Header
   *
   * ### Postcondition:
   * Returns SubscriptionPlanResponse model that includes subscription plan id.
   *
   * ### Further Information:
   * None.
   *
   */
  internalRequestSubscriptionPlan(
    req: operations.InternalRequestSubscriptionPlanRequest,
    config?: AxiosRequestConfig
  ): Promise<operations.InternalRequestSubscriptionPlanResponse> {
    if (!(req instanceof utils.SpeakeasyBase)) {
      req = new operations.InternalRequestSubscriptionPlanRequest(req);
    }

    const baseURL: string = this._serverURL;
    const url: string =
      baseURL.replace(/\/$/, "") + "/v4/internal/tenant/subscription_plan";

    const client: AxiosInstance = this._securityClient || this._defaultClient;

    const headers = { ...utils.getHeadersFromRequest(req), ...config?.headers };

    const r = client.request({
      url: url,
      method: "get",
      headers: headers,
      ...config,
    });

    return r.then((httpRes: AxiosResponse) => {
      const contentType: string = httpRes?.headers?.["content-type"] ?? "";

      if (httpRes?.status == null)
        throw new Error(`status code not found in response: ${httpRes}`);
      const res: operations.InternalRequestSubscriptionPlanResponse =
        new operations.InternalRequestSubscriptionPlanResponse({
          statusCode: httpRes.status,
          contentType: contentType,
          rawResponse: httpRes,
        });
      switch (true) {
        case httpRes?.status == 200:
          if (utils.matchContentType(contentType, `application/json`)) {
            res.subscriptionPlanResponse = utils.objectToClass(
              httpRes?.data,
              shared.SubscriptionPlanResponse
            );
          }
          break;
        case httpRes?.status == 401:
          if (utils.matchContentType(contentType, `application/json`)) {
            res.errorResponse = utils.objectToClass(
              httpRes?.data,
              shared.ErrorResponse
            );
          }
          break;
      }

      return res;
    });
  }

  /**
   * Set subscription plan
   *
   * @remarks
   * <h3 style='padding: 5px; background-color: #F6F7F8; border: 1px solid #AAA; border-radius: 5px; display: table-cell;'>&#128640; Since v4.36.0</h3>
   *
   * ### Description:
   * Change the subscription plan id of the current tenant
   *
   * ### Precondition:
   * Valid `X-SDS-Service-Token` Header
   *
   * ### Postcondition:
   * The subscription plan of the current tenant is set to the given value.
   * Returns SubscriptionPlanResponse model that includes subscription plan id.
   *
   * ### Further Information:
   * None.
   *
   */
  internalSetSubscriptionPlan(
    req: operations.InternalSetSubscriptionPlanRequest,
    config?: AxiosRequestConfig
  ): Promise<operations.InternalSetSubscriptionPlanResponse> {
    if (!(req instanceof utils.SpeakeasyBase)) {
      req = new operations.InternalSetSubscriptionPlanRequest(req);
    }

    const baseURL: string = this._serverURL;
    const url: string =
      baseURL.replace(/\/$/, "") + "/v4/internal/tenant/subscription_plan";

    let [reqBodyHeaders, reqBody]: [object, any] = [{}, {}];

    try {
      [reqBodyHeaders, reqBody] = utils.serializeRequestBody(
        req,
        "subscriptionPlanRequest",
        "json"
      );
    } catch (e: unknown) {
      if (e instanceof Error) {
        throw new Error(`Error serializing request body, cause: ${e.message}`);
      }
    }

    const client: AxiosInstance = this._securityClient || this._defaultClient;

    const headers = {
      ...utils.getHeadersFromRequest(req),
      ...reqBodyHeaders,
      ...config?.headers,
    };
    if (reqBody == null || Object.keys(reqBody).length === 0)
      throw new Error("request body is required");

    const r = client.request({
      url: url,
      method: "put",
      headers: headers,
      data: reqBody,
      ...config,
    });

    return r.then((httpRes: AxiosResponse) => {
      const contentType: string = httpRes?.headers?.["content-type"] ?? "";

      if (httpRes?.status == null)
        throw new Error(`status code not found in response: ${httpRes}`);
      const res: operations.InternalSetSubscriptionPlanResponse =
        new operations.InternalSetSubscriptionPlanResponse({
          statusCode: httpRes.status,
          contentType: contentType,
          rawResponse: httpRes,
        });
      switch (true) {
        case httpRes?.status == 200:
          if (utils.matchContentType(contentType, `application/json`)) {
            res.subscriptionPlanResponse = utils.objectToClass(
              httpRes?.data,
              shared.SubscriptionPlanResponse
            );
          }
          break;
        case [400, 401].includes(httpRes?.status):
          if (utils.matchContentType(contentType, `application/json`)) {
            res.errorResponse = utils.objectToClass(
              httpRes?.data,
              shared.ErrorResponse
            );
          }
          break;
      }

      return res;
    });
  }
}
