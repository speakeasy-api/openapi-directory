/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import * as utils from "../internal/utils";
import * as operations from "./models/operations";
import * as shared from "./models/shared";
import { AxiosInstance, AxiosRequestConfig, AxiosResponse } from "axios";

export class LocationEndpoints {
  _defaultClient: AxiosInstance;
  _securityClient: AxiosInstance;
  _serverURL: string;
  _language: string;
  _sdkVersion: string;
  _genVersion: string;

  constructor(
    defaultClient: AxiosInstance,
    securityClient: AxiosInstance,
    serverURL: string,
    language: string,
    sdkVersion: string,
    genVersion: string
  ) {
    this._defaultClient = defaultClient;
    this._securityClient = securityClient;
    this._serverURL = serverURL;
    this._language = language;
    this._sdkVersion = sdkVersion;
    this._genVersion = genVersion;
  }

  /**
   * Search for places. Complete words required.
   *
   * @remarks
   * ## Search for places
   *
   * You can use this endpoint to obtain `place_id` of the location you want, to be used in `point` endpoint.
   * The response also contains detailed information about the location, such as coordinates, timezone and the country the place belongs to.
   *
   * Unlike the `/find_place_prefix` endpoint, complete words are required here. You can search for cities,
   * mountains, lakes, countries, ZIP codes, etc. The response can contain multiple places, sorted by relevance.
   * You can then identify the one you want by coordinates, country, or the administrative area.
   *
   */
  findPlacesFindPlacesGet(
    req: operations.FindPlacesFindPlacesGetRequest,
    security: operations.FindPlacesFindPlacesGetSecurity,
    config?: AxiosRequestConfig
  ): Promise<operations.FindPlacesFindPlacesGetResponse> {
    if (!(req instanceof utils.SpeakeasyBase)) {
      req = new operations.FindPlacesFindPlacesGetRequest(req);
    }

    const baseURL: string = this._serverURL;
    const url: string = baseURL.replace(/\/$/, "") + "/find_places";
    if (!(security instanceof utils.SpeakeasyBase)) {
      security = new operations.FindPlacesFindPlacesGetSecurity(security);
    }
    const client: AxiosInstance = utils.createSecurityClient(
      this._defaultClient,
      security
    );

    const queryParams: string = utils.serializeQueryParams(req);

    const r = client.request({
      url: url + queryParams,
      method: "get",
      ...config,
    });

    return r.then((httpRes: AxiosResponse) => {
      const contentType: string = httpRes?.headers?.["content-type"] ?? "";

      if (httpRes?.status == null)
        throw new Error(`status code not found in response: ${httpRes}`);
      const res: operations.FindPlacesFindPlacesGetResponse =
        new operations.FindPlacesFindPlacesGetResponse({
          statusCode: httpRes.status,
          contentType: contentType,
          rawResponse: httpRes,
        });
      switch (true) {
        case httpRes?.status == 200:
          if (utils.matchContentType(contentType, `application/json`)) {
            res.findPlacesModels = [];
            const resFieldDepth: number = utils.getResFieldDepth(res);
            res.findPlacesModels = utils.objectToClass(
              httpRes?.data,
              shared.FindPlacesModel,
              resFieldDepth
            );
          }
          break;
        case [400, 402, 403, 429].includes(httpRes?.status):
          if (utils.matchContentType(contentType, `application/json`)) {
            res.generalRequestError = utils.objectToClass(
              httpRes?.data,
              shared.GeneralRequestError
            );
          }
          break;
        case httpRes?.status == 422:
          if (utils.matchContentType(contentType, `application/json`)) {
            res.httpValidationError = utils.objectToClass(
              httpRes?.data,
              shared.HTTPValidationError
            );
          }
          break;
      }

      return res;
    });
  }

  /**
   * Prefix search for places. Useful for autocomplete forms.
   *
   * @remarks
   * ## Search for places by prefix
   *
   * You can use this endpoint to obtain `place_id` of the location you want, to be used in `point` endpoint. The response also contains detailed information about the location, such as coordinates, timezone and the country the place belongs to.
   *
   * Unlike the `/find_places` endpoint, you should only specify the prefix of the place you are looking for. This is particularly useful for autocomplete forms. You can search for cities, mountains, lakes, countries, ZIP codes, etc. The response can contain multiple places, sorted by relevance. You can then identify the one you want by coordinates, country, or the administrative area.
   *
   */
  findPlacesPrefixFindPlacesPrefixGet(
    req: operations.FindPlacesPrefixFindPlacesPrefixGetRequest,
    security: operations.FindPlacesPrefixFindPlacesPrefixGetSecurity,
    config?: AxiosRequestConfig
  ): Promise<operations.FindPlacesPrefixFindPlacesPrefixGetResponse> {
    if (!(req instanceof utils.SpeakeasyBase)) {
      req = new operations.FindPlacesPrefixFindPlacesPrefixGetRequest(req);
    }

    const baseURL: string = this._serverURL;
    const url: string = baseURL.replace(/\/$/, "") + "/find_places_prefix";
    if (!(security instanceof utils.SpeakeasyBase)) {
      security = new operations.FindPlacesPrefixFindPlacesPrefixGetSecurity(
        security
      );
    }
    const client: AxiosInstance = utils.createSecurityClient(
      this._defaultClient,
      security
    );

    const queryParams: string = utils.serializeQueryParams(req);

    const r = client.request({
      url: url + queryParams,
      method: "get",
      ...config,
    });

    return r.then((httpRes: AxiosResponse) => {
      const contentType: string = httpRes?.headers?.["content-type"] ?? "";

      if (httpRes?.status == null)
        throw new Error(`status code not found in response: ${httpRes}`);
      const res: operations.FindPlacesPrefixFindPlacesPrefixGetResponse =
        new operations.FindPlacesPrefixFindPlacesPrefixGetResponse({
          statusCode: httpRes.status,
          contentType: contentType,
          rawResponse: httpRes,
        });
      switch (true) {
        case httpRes?.status == 200:
          if (utils.matchContentType(contentType, `application/json`)) {
            res.findPlacesModels = [];
            const resFieldDepth: number = utils.getResFieldDepth(res);
            res.findPlacesModels = utils.objectToClass(
              httpRes?.data,
              shared.FindPlacesModel,
              resFieldDepth
            );
          }
          break;
        case [400, 402, 403, 429].includes(httpRes?.status):
          if (utils.matchContentType(contentType, `application/json`)) {
            res.generalRequestError = utils.objectToClass(
              httpRes?.data,
              shared.GeneralRequestError
            );
          }
          break;
        case httpRes?.status == 422:
          if (utils.matchContentType(contentType, `application/json`)) {
            res.httpValidationError = utils.objectToClass(
              httpRes?.data,
              shared.HTTPValidationError
            );
          }
          break;
      }

      return res;
    });
  }

  /**
   * Returns the nearest named location for a given GPS coordinates.
   *
   * @remarks
   * ## Search for nearest place by coordinates
   *
   * You can use this endpoint to find the nearest place from given coordinates.
   *
   * *Note: If you specify coordinates of a secluded place (e.g. middle of the ocean), the nearest point can be very far from the coordinates.*
   *
   */
  nearestPlaceNearestPlaceGet(
    req: operations.NearestPlaceNearestPlaceGetRequest,
    security: operations.NearestPlaceNearestPlaceGetSecurity,
    config?: AxiosRequestConfig
  ): Promise<operations.NearestPlaceNearestPlaceGetResponse> {
    if (!(req instanceof utils.SpeakeasyBase)) {
      req = new operations.NearestPlaceNearestPlaceGetRequest(req);
    }

    const baseURL: string = this._serverURL;
    const url: string = baseURL.replace(/\/$/, "") + "/nearest_place";
    if (!(security instanceof utils.SpeakeasyBase)) {
      security = new operations.NearestPlaceNearestPlaceGetSecurity(security);
    }
    const client: AxiosInstance = utils.createSecurityClient(
      this._defaultClient,
      security
    );

    const queryParams: string = utils.serializeQueryParams(req);

    const r = client.request({
      url: url + queryParams,
      method: "get",
      ...config,
    });

    return r.then((httpRes: AxiosResponse) => {
      const contentType: string = httpRes?.headers?.["content-type"] ?? "";

      if (httpRes?.status == null)
        throw new Error(`status code not found in response: ${httpRes}`);
      const res: operations.NearestPlaceNearestPlaceGetResponse =
        new operations.NearestPlaceNearestPlaceGetResponse({
          statusCode: httpRes.status,
          contentType: contentType,
          rawResponse: httpRes,
        });
      switch (true) {
        case httpRes?.status == 200:
          if (utils.matchContentType(contentType, `application/json`)) {
            res.findPlacesModel = utils.objectToClass(
              httpRes?.data,
              shared.FindPlacesModel
            );
          }
          break;
        case [400, 402, 403, 429].includes(httpRes?.status):
          if (utils.matchContentType(contentType, `application/json`)) {
            res.generalRequestError = utils.objectToClass(
              httpRes?.data,
              shared.GeneralRequestError
            );
          }
          break;
        case httpRes?.status == 422:
          if (utils.matchContentType(contentType, `application/json`)) {
            res.httpValidationError = utils.objectToClass(
              httpRes?.data,
              shared.HTTPValidationError
            );
          }
          break;
      }

      return res;
    });
  }
}
